# v0.5.0 视觉质感升级 - 任务规划

> **版本号**：v0.5.0
> **类型**：minor（视觉质感升级）
> **前置版本**：v0.4.0
> **创建日期**：2026-07-04

---

## 任务总览

本版本拆分为 **7 大任务模块 / 22 个子任务**，按依赖关系排序：

```
┌─────────────────────────────────────────┐
│ 任务 1: CSS 变量体系升级（基础）        │ ← 阻塞后续所有任务
└─────────────────────────────────────────┘
                ↓
┌─────────────────────────────────────────┐
│ 任务 2: 全局样式重构（globals.css）     │ ← 阻塞 UI 组件
└─────────────────────────────────────────┘
                ↓
┌─────────────────────────────────────────┐
│ 任务 3: n-overrides.css（Naive UI 覆盖）│ ← 独立任务
└─────────────────────────────────────────┘
                ↓
┌─────────────────────────────────────────┐
│ 任务 4: 现有组件微调（TitleBar 等）     │ ← 依赖任务 1-2
└─────────────────────────────────────────┘
                ↓
┌─────────────────────────────────────────┐
│ 任务 5: 工作台欢迎页（Dashboard 重写）  │ ← 依赖任务 1-2
└─────────────────────────────────────────┘
                ↓
┌─────────────────────────────────────────┐
│ 任务 6: 侧栏增强（深浅态+分组+弹窗）   │ ← 依赖任务 1-2
└─────────────────────────────────────────┘
                ↓
┌─────────────────────────────────────────┐
│ 任务 7: 登录页（极光+渐变卡片）        │ ← 依赖任务 1-2
└─────────────────────────────────────────┘
                ↓
┌─────────────────────────────────────────┐
│ 任务 8: 编译验证 + 启动测试            │ ← 阻塞收尾
└─────────────────────────────────────────┘
```

---

## 任务清单

### 任务 1：升级 variables.css - 毛玻璃/装饰/Hero 变量

**ID**：`T1`
**状态**：⏳ 待开始
**优先级**：P0
**预估**：30 分钟
**依赖**：无

**目标**：在 v0.4.0 变量基础上，新增毛玻璃、装饰背景、Hero 渐变、spring 曲线等关键变量

**AC 验收**：
- [ ] 标题栏背景变量改为 `rgba` 半透明
- [ ] 侧栏背景保持深色实色（深色背景毛玻璃反而糊）
- [ ] 内容区背景改为 `rgba` 半透明
- [ ] 新增 `--tea-card-bg` / `--tea-card-border` / `--tea-card-blur`
- [ ] 新增 `--tea-modal-bg` / `--tea-modal-blur`
- [ ] 新增 `--tea-drawer-bg` / `--tea-drawer-blur`
- [ ] 新增 `--tea-overlay-bg` / `--tea-overlay-blur`
- [ ] 新增 `--tea-blur-sm/md/lg` 三档模糊参数
- [ ] 新增 `--tea-spring` 弹性曲线
- [ ] 新增 `--tea-hover-lift-sm/md` 抬升位移
- [ ] 新增 `--tea-content-decoration` 装饰背景
- [ ] 新增 `--tea-hero-gradient` 渐变变量
- [ ] 主色变体（bamboo/cinnabar）补全 titlebar-bg、hero-gradient
- [ ] 深色主题变体补全所有半透明变量
- [ ] 注释清晰，说明每个变量用途

**关键代码位置**：`tea-manage/src/styles/variables.css`

**实施步骤**：
1. 在 `:root` 中新增所有 v0.5.0 变量
2. 修改 v0.4.0 已有变量（titlebar-bg、content-bg 等）为半透明
3. 在 `[data-primary='bamboo']` 和 `[data-primary='cinnabar']` 中补全
4. 在 `[data-theme='dark']` 中补全所有半透明变量
5. 编译验证

---

### 任务 2：重构 globals.css - 双层背景 + 工具类强化

**ID**：`T2`
**状态**：⏳ 待开始
**优先级**：P0
**预估**：30 分钟
**依赖**：T1

**目标**：应用双层背景视觉，强化 TabBar/Sidebar 指示线动画，添加卡片通用类

**AC 验收**：
- [ ] `.tea-app-content` 实现双层背景（::before 底层 + ::after 装饰）
- [ ] `.tea-titlebar` 应用 `backdrop-filter: var(--tea-blur-lg)`
- [ ] 新增 `.tea-card` 通用类（半透明 + 模糊 + 边框 + 圆角 + 阴影）
- [ ] 新增 `.tea-card-hoverable` hover 时上浮 2px
- [ ] `.tea-tab-active::after` 增强：发光 + 进入动画
- [ ] `.tea-sider-item-active::before` 增强：弹性进入动画
- [ ] `.tea-sider-btn:hover` 增强：translateX(2px) translateY(-1px)
- [ ] 新增 `@keyframes teaIndicatorIn` 缩放进入
- [ ] 新增 `@keyframes teaBarIn` 弹性进入
- [ ] 新增 `@keyframes teaAccentGlow`（antd 用）

**关键代码位置**：`tea-manage/src/styles/globals.css`

**实施步骤**：
1. 找到 `.tea-app-content`，添加 ::before/::after 双层背景
2. 找到 `.tea-titlebar`，添加 backdrop-filter
3. 新增 `.tea-card` 和 `.tea-card-hoverable` 类
4. 强化 `.tea-tab-active::after`（加阴影 + 动画）
5. 强化 `.tea-sider-item-active::before`（加动画）
6. 强化 `.tea-sider-btn:hover`
7. 新增 3 个 keyframes
8. 编译验证

---

### 任务 3：新增 antd-overrides.css - Naive UI 组件美化

**ID**：`T3`
**状态**：⏳ 待开始
**优先级**：P0
**预估**：45 分钟
**依赖**：T1

**目标**：覆盖 Naive UI 组件的默认样式，添加毛玻璃、装饰光带、下划线输入框等

**AC 验收**：
- [ ] `.n-modal-content` 应用毛玻璃（半透明 + blur）
- [ ] `.n-modal-content::before` 顶部 3px 渐变光带 + 动画
- [ ] `.n-modal-mask` 应用模糊遮罩
- [ ] `.n-modal .n-input` 下边框样式（无四周）
- [ ] `.n-modal .n-input:focus` 主色下边框 + 阴影
- [ ] `.n-drawer-content` 毛玻璃背景
- [ ] `.n-dropdown-menu` 毛玻璃背景
- [ ] `.n-message-notice-content` 毛玻璃背景
- [ ] `.n-card` 毛玻璃背景（半透明 + blur）
- [ ] `.n-button--primary-type:hover` translateY(-1px) + 主色阴影
- [ ] `.n-button:active` scale(0.97)
- [ ] 深色主题下，遮罩颜色对应调整
- [ ] 顶部装饰条动画 `teaAccentGlow` 引用正确

**关键代码位置**：`tea-manage/src/styles/antd-overrides.css`（新增）

**实施步骤**：
1. 创建新文件
2. 添加文件头注释
3. 按"弹窗 → 抽屉 → 下拉 → 消息 → 卡片 → 按钮"顺序编写
4. 每个大类下分子类（容器、遮罩、内容、按钮等）
5. 在 main.ts 中引入
6. 编译验证

---

### 任务 4：现有组件微调 - TitleBar 移除品牌

**ID**：`T4`
**状态**：⏳ 待开始
**优先级**：P0
**预估**：15 分钟
**依赖**：T1, T2

**目标**：移除 TitleBar 中的品牌区，使标题栏更简洁

**AC 验收**：
- [ ] TitleBar.vue 移除 `.tea-titlebar-brand` div
- [ ] TitleBar 模板结构变为 `[折叠按钮] [TabBar] [用户菜单]`
- [ ] 移除 LeafOutline 和 NIcon 中相关 import
- [ ] 标题栏从左到右视觉简洁
- [ ] 侧边栏品牌区不变（仍显示"茶易管"）
- [ ] TypeScript 编译无错

**关键代码位置**：`tea-manage/src/components/layout/TitleBar.vue`

**实施步骤**：
1. 打开 TitleBar.vue
2. 找到 `.tea-titlebar-brand` div，删除整个区块
3. 清理无用 import
4. 验证样式不残留
5. 编译验证

---

### 任务 5：现有组件微调 - AppLayout 内容区适配

**ID**：`T5`
**状态**：⏳ 待开始
**优先级**：P1
**预估**：10 分钟
**依赖**：T1, T2

**目标**：确保内容区双层背景正确显示

**AC 验收**：
- [ ] `.tea-app-content` 设置 `position: relative`
- [ ] `.tea-app-content::before` 底层纯色
- [ ] `.tea-app-content::after` 装饰层
- [ ] 不影响 router-view 内容渲染
- [ ] 滚动时背景固定

**关键代码位置**：`tea-manage/src/components/layout/AppLayout.vue`

**实施步骤**：
1. 打开 AppLayout.vue
2. 在 `.tea-app-content` 样式中加 `position: relative`
3. 验证 globals.css 中的双层背景正确显示
4. 编译验证

---

### 任务 6：创建 DashboardHero.vue

**ID**：`T6`
**状态**：⏳ 待开始
**优先级**：P0
**预估**：30 分钟
**依赖**：T1, T2

**目标**：实现工作台顶部渐变 Hero 卡片

**AC 验收**：
- [ ] 文件创建在 `tea-manage/src/views/dashboard/components/DashboardHero.vue`
- [ ] Hero 背景使用 `var(--tea-hero-gradient)`，支持 3 主色切换
- [ ] 显示时段问候语（早上好/下午好/晚上好/夜深了）
- [ ] 显示用户名称"茶馆主"
- [ ] 显示副标题"欢迎使用茶易管..."
- [ ] 右上角"系统设置"毛玻璃按钮，点击跳 `/settings`
- [ ] Hero 区域有发光阴影
- [ ] 右上角装饰 radial-gradient 光晕
- [ ] `tea-fade-in` 进场动画
- [ ] 深色模式下渐变依然显示
- [ ] TypeScript 编译无错
- [ ] 注释完整

**关键代码位置**：`tea-manage/src/views/dashboard/components/DashboardHero.vue`（新增）

**实施步骤**：
1. 创建 components 目录
2. 编写文件（参考技术方案 6.5.2）
3. 计算时段问候逻辑
4. 应用样式
5. 编译验证

---

### 任务 7：创建 QuickActions.vue

**ID**：`T7`
**状态**：⏳ 待开始
**优先级**：P0
**预估**：30 分钟
**依赖**：T1, T2

**目标**：实现工作台快捷操作网格（6 个入口）

**AC 验收**：
- [ ] 文件创建在 `tea-manage/src/views/dashboard/components/QuickActions.vue`
- [ ] 显示 6 个快捷操作：零售收银、商品查询、采购入库、会员管理、库存管理、系统设置
- [ ] 每个卡片有彩色图标块（主色 12% 透明背景 + 主色）
- [ ] 每个卡片有标题
- [ ] 点击卡片跳转到对应路由
- [ ] 卡片支持 hover 抬升（translateY(-2px) + 阴影）
- [ ] 响应式网格：minmax(180px, 1fr)
- [ ] 卡片背景使用 `var(--tea-card-bg)` 毛玻璃
- [ ] TypeScript 编译无错
- [ ] 注释完整

**关键代码位置**：`tea-manage/src/views/dashboard/components/QuickActions.vue`（新增）

**实施步骤**：
1. 创建文件
2. 定义 actions 数组（label, icon, color, bgColor, path）
3. 实现网格渲染
4. 实现点击跳转
5. 应用样式
6. 编译验证

---

### 任务 8：创建 TodayStats.vue

**ID**：`T8`
**状态**：⏳ 待开始
**优先级**：P0
**预估**：30 分钟
**依赖**：T1, T2

**目标**：实现工作台今日概览数据卡片（4 项 mock 数据）

**AC 验收**：
- [ ] 文件创建在 `tea-manage/src/views/dashboard/components/TodayStats.vue`
- [ ] 显示 4 个数据卡片：今日订单、今日销售额、库存预警、新增会员
- [ ] 每个卡片有彩色图标、统计值、标签
- [ ] 每个卡片右上角显示趋势（+12%、-2 等）
- [ ] 标题"今日概览"后有"（演示数据）"提示
- [ ] 卡片支持 hover 抬升
- [ ] 数据使用 mock（随机数）
- [ ] 网格响应式
- [ ] TypeScript 编译无错
- [ ] 注释完整

**关键代码位置**：`tea-manage/src/views/dashboard/components/TodayStats.vue`（新增）

**实施步骤**：
1. 创建文件
2. 定义 stats 数组（4 个 mock）
3. 实现网格渲染
4. 应用样式
5. 编译验证

---

### 任务 9：重写 dashboard/index.vue

**ID**：`T9`
**状态**：⏳ 待开始
**优先级**：P0
**预估**：15 分钟
**依赖**：T6, T7, T8

**目标**：重写工作台主入口，组合 3 个子组件

**AC 验收**：
- [ ] 旧 index.vue 内容完全替换
- [ ] 模板：`DashboardHero` + 间距 + `QuickActions` + 间距 + `TodayStats`
- [ ] 间距使用 `.dashboard-spacing` 类
- [ ] 最大宽度 1200px 居中
- [ ] 三个组件正确 import
- [ ] 原有 dashboard 残留代码清空
- [ ] TypeScript 编译无错
- [ ] 注释完整

**关键代码位置**：`tea-manage/src/views/dashboard/index.vue`

**实施步骤**：
1. 打开 index.vue
2. 清空旧内容
3. 编写新模板和脚本
4. 应用样式
5. 编译验证

---

### 任务 10：main.ts 引入 antd-overrides.css

**ID**：`T10`
**状态**：⏳ 待开始
**优先级**：P0
**预估**：5 分钟
**依赖**：T3

**目标**：在 main.ts 中引入新增的 antd-overrides.css

**AC 验收**：
- [ ] main.ts 新增 `import './styles/antd-overrides.css'`
- [ ] 引入顺序：variables.css → globals.css → antd-overrides.css
- [ ] 编译无错

**关键代码位置**：`tea-manage/src/main.ts`

**实施步骤**：
1. 打开 main.ts
2. 在 globals.css 引入后添加 antd-overrides.css
3. 编译验证

---

### 任务 11：编译验证 + 启动测试

**ID**：`T11`
**状态**：⏳ 待开始
**优先级**：P0
**预估**：30 分钟
**依赖**：T1 - T10

**目标**：完成所有代码改动后，进行编译验证和功能测试

**AC 验收**：
- [ ] `pnpm tsc --noEmit` 无错
- [ ] `pnpm build` 成功
- [ ] `pnpm dev` 启动成功
- [ ] 浏览器打开 http://localhost:5173
- [ ] 标题栏半透明 + 模糊可见
- [ ] TabBar 激活指示线有动画
- [ ] Sidebar 激活项左侧竖条有动画
- [ ] 工作台 Hero 显示，渐变背景
- [ ] 快捷操作 6 个入口可点击
- [ ] 今日概览 4 张数据卡
- [ ] 任意弹窗打开：顶部装饰光带 + 下划线输入框
- [ ] 主题切换 3 主色：所有毛玻璃保持
- [ ] 深色模式：所有毛玻璃保持
- [ ] 标题栏无品牌
- [ ] 侧边栏展开时显示品牌

**关键命令**：
```bash
cd d:\AI\AI\开发\茶叶店管理\tea-manage
pnpm tsc --noEmit
npx vite --port 5174 --host 127.0.0.1
```

**测试清单**：

| 项 | 状态 |
|----|------|
| 标题栏半透明 | ☐ |
| 侧栏激活竖条 | ☐ |
| TabBar 指示线 | ☐ |
| 工作台 Hero | ☐ |
| 快捷操作 | ☐ |
| 今日概览 | ☐ |
| 弹窗装饰光带 | ☐ |
| 弹窗下划线输入 | ☐ |
| 抽屉毛玻璃 | ☐ |
| 主题切换 | ☐ |
| 深色模式 | ☐ |
| 标题栏无品牌 | ☐ |

---

### 任务 12：更新 v0.5.0 版本文档

**ID**：`T12`
**状态**：⏳ 待开始
**优先级**：P0
**预估**：20 分钟
**依赖**：T11

**目标**：完成验证后，更新 `specs/releases/v0.5.0.md` 记录完成情况

**AC 验收**：
- [ ] 创建 `specs/releases/v0.5.0.md`
- [ ] 发布状态改为"✅ 已完成"
- [ ] 实际发布时间填写
- [ ] 修改文件清单完整
- [ ] 变更描述详细
- [ ] 验收清单勾选完成

**关键代码位置**：`specs/releases/v0.5.0.md`（新建）

---

### 任务 13：侧栏浅色态变量补全（variables.css）

**ID**：`T13`
**状态**：⏳ 待开始
**优先级**：P0
**预估**：15 分钟
**依赖**：T1
**AC-F 联动**：AC-G1~G5

**目标**：在 variables.css 中新增 `[data-sidebar-style='light']` 变量块

**AC 验收**：
- [ ] 浅色态：背景 `rgba(255,255,255,0.78)` + 深色文字 + primary-light 激活态
- [ ] 深色+浅色侧栏组合：深棕半透明 + 亮文字
- [ ] 注释清晰

**关键代码位置**：`tea-manage/src/styles/variables.css`（追加）

---

### 任务 14：theme store 新增 sidebarStyle 字段

**ID**：`T14`
**状态**：⏳ 待开始
**优先级**：P0
**预估**：10 分钟
**依赖**：T13
**AC-F 联动**：AC-G6

**目标**：在 useThemeStore 的 uiSettings 中新增 `sidebarStyle: 'dark' | 'light'`，并在 applyTheme 中设置 `document.documentElement.dataset.sidebarStyle`

**AC 验收**：
- [ ] stores/theme.ts 新增字段
- [ ] localStorage 持久化
- [ ] 初始化时读 localStorage
- [ ] 切换时更新 DOM 属性

**关键代码位置**：`tea-manage/src/stores/theme.ts`

---

### 任务 15：ThemeSettings 抽屉新增"侧栏风格"切换

**ID**：`T15`
**状态**：⏳ 待开始
**优先级**：P0
**预估**：10 分钟
**依赖**：T14
**AC-F 联动**：AC-G6

**目标**：在主题设置抽屉新增 Segmented 控件（深色/浅色）

**AC 验收**：
- [ ] 在"外观"分组下新增"侧栏风格"行
- [ ] Segmented 控件选项 [深色 浅色]
- [ ] 当前值高亮
- [ ] 切换后立即生效（侧栏背景变化）

**关键代码位置**：`tea-manage/src/components/layout/ThemeSettings.vue`

---

### 任务 16：Sidebar 模板与逻辑：分组激活增强

**ID**：`T16`
**状态**：⏳ 待开始
**优先级**：P0
**预估**：20 分钟
**依赖**：T13
**AC-F 联动**：AC-H1~H3

**目标**：在 Sidebar.vue 中实现分组内子项激活时分组标题加发光竖条

**AC 验收**：
- [ ] 分组容器新增 `tea-sider-group-has-active` 类
- [ ] 模板增加 `hasActiveChild` 判断
- [ ] globals.css 中实现竖条样式
- [ ] 展开态和折叠态位置不同
- [ ] 主色切换跟随

**关键代码位置**：`tea-manage/src/components/layout/Sidebar.vue` + `tea-manage/src/styles/globals.css`

---

### 任务 17：Sidebar 折叠态 portal 弹窗

**ID**：`T17`
**状态**：⏳ 待开始
**优先级**：P0
**预估**：30 分钟
**依赖**：T16
**AC-F 联动**：AC-I1~I7

**目标**：折叠态点击分组 → 浮出 portal 弹窗菜单

**AC 验收**：
- [ ] 折叠态点击分组按钮 → 弹窗打开
- [ ] 弹窗显示分组标题
- [ ] 弹窗中子项点击 → 跳转 + 关闭
- [ ] 弹窗外点击关闭
- [ ] 弹窗 max-height 80vh
- [ ] 展开侧栏时自动关闭
- [ ] `tea-scale-in` 进入动画
- [ ] 毛玻璃风格 + 圆角 + 阴影

**关键代码位置**：`tea-manage/src/components/layout/Sidebar.vue`（在 popup 模板和逻辑中增强）

---

### 任务 18：登录页 - 路由 + 视图骨架

**ID**：`T18`
**状态**：⏳ 待开始
**优先级**：P0
**预估**：15 分钟
**依赖**：T1, T2
**AC-F 联动**：AC-J1

**目标**：新增 `/login` 路由 + Login.vue 骨架

**AC 验收**：
- [ ] router/index.ts 新增 Login 路由
- [ ] 标记 `meta.public = true`
- [ ] AppLayout 路由守卫中判断 public 跳过布局
- [ ] Login.vue 创建，骨架完整

**关键代码位置**：`tea-manage/src/router/index.ts` + `tea-manage/src/views/auth/Login.vue`

---

### 任务 19：登录页 - 极光/网格/噪点/圆点层

**ID**：`T19`
**状态**：⏳ 待开始
**优先级**：P0
**预估**：25 分钟
**依赖**：T18
**AC-F 联动**：AC-J2~J5

**目标**：实现登录页装饰层（4 层）

**AC 验收**：
- [ ] 极光层：2 层 radial-gradient + 30s drift 动画
- [ ] 网格层：60px 网格 + mask 中心可见
- [ ] 噪点层：SVG 噪点 opacity 0.2
- [ ] 圆点层：8 个发光圆点随机分布 + 3s 脉冲
- [ ] variables.css 新增 `--tea-login-*` 变量
- [ ] prefers-reduced-motion 退化

**关键代码位置**：`tea-manage/src/views/auth/Login.vue` + `tea-manage/src/styles/variables.css`

---

### 任务 20：登录页 - 渐变卡片 + Logo + 标题

**ID**：`T20`
**状态**：⏳ 待开始
**优先级**：P0
**预估**：25 分钟
**依赖**：T19
**AC-F 联动**：AC-J6~J9, J12~J13

**目标**：实现登录卡片（毛玻璃 + 顶部光带 + Logo + 渐变标题 + 进入动画）

**AC 验收**：
- [ ] 卡片：毛玻璃 + 圆角 + 阴影
- [ ] 顶部 3px 主色光带 + 2s 动画
- [ ] Logo：圆形渐变 + 阴影脉冲
- [ ] Logo 外环：1.5px 描边 + 0.8s 延迟渐显
- [ ] 标题：渐变文字（text-primary → primary）
- [ ] 副标题：茶文化文案
- [ ] 卡片 0.6s 上移淡入

**关键代码位置**：`tea-manage/src/views/auth/Login.vue`

---

### 任务 21：登录页 - 表单 + 按钮 shimmer

**ID**：`T21`
**状态**：⏳ 待开始
**优先级**：P0
**预估**：20 分钟
**依赖**：T20
**AC-F 联动**：AC-J10~J11

**目标**：实现登录表单（下划线输入框 + shimmer 按钮）

**AC 验收**：
- [ ] 账号/密码输入框下划线风格（应用 n-overrides）
- [ ] 输入框前缀图标
- [ ] 密码"显示/隐藏"切换
- [ ] 登录按钮：主色 + shimmer 光带扫过
- [ ] 登录逻辑：任意账号密码直接跳转 /（UI 壳占位）

**关键代码位置**：`tea-manage/src/views/auth/Login.vue`

---

### 任务 22：综合编译验证 + 启动测试（v0.5.0 收尾）

**ID**：`T22`
**状态**：⏳ 待开始
**优先级**：P0
**预估**：40 分钟
**依赖**：T1 - T21

**目标**：完成所有 9 块代码改动后，进行综合编译验证和功能测试

**AC 验收**：
- [ ] `pnpm tsc --noEmit` 无错
- [ ] `pnpm build` 成功
- [ ] `pnpm dev` 启动成功
- [ ] 浏览器打开 http://localhost:5173
- [ ] **毛玻璃/动效**：标题栏半透明模糊 + TabBar 指示线 + Sidebar 竖条
- [ ] **工作台**：Hero 渐变 + 时段问候 + 6 快捷 + 4 数据卡
- [ ] **n-overrides**：任意弹窗顶部光带 + 下划线输入框 + 抽屉/下拉/消息毛玻璃
- [ ] **侧栏浅色态**：可在主题设置切换深/浅
- [ ] **侧栏分组激活**：分组标题有竖条（激活子项时）
- [ ] **折叠态弹窗**：点击分组弹出 portal 弹窗
- [ ] **登录页**：极光 + 网格 + 噪点 + 圆点 + 渐变卡片 + 下划线 + shimmer 按钮
- [ ] **主题切换**：3 主色 + 浅深主题，毛玻璃效果保持
- [ ] **深色模式**：所有毛玻璃效果保持
- [ ] **标题栏无品牌** + **侧边栏展开时显示品牌**

**关键命令**：
```bash
cd d:\AI\AI\开发\茶叶店管理\tea-manage
pnpm tsc --noEmit
npx vite --port 5174 --host 127.0.0.1
```

---

## 实施顺序（推荐）

```
T1 (variables.css + login 变量 + sidebar-style 变量)
 ↓
T2 (globals.css)  并行  T3 (n-overrides.css)
 ↓                       ↓
 T4 (TitleBar)  并行  T5 (AppLayout)  并行  T6 (Hero)  并行  T7 (QuickActions)  并行  T8 (TodayStats)
 ↓
 T9 (dashboard/index.vue)
 ↓
 T10 (main.ts)
 ↓
 T13 (sidebar-style 变量) → T14 (theme store) → T15 (ThemeSettings 控件)
 ↓
 T16 (Sidebar 分组激活) → T17 (Sidebar 折叠弹窗)
 ↓
 T18 (路由 + 骨架) → T19 (装饰层) → T20 (卡片) → T21 (表单)
 ↓
 T11 (本轮编译验证) → T22 (综合验证)
 ↓
 T12 (版本文档)
```

---

## 工时估算

| 任务 | 预估 |
|------|------|
| T1 变量升级 | 30 分钟 |
| T2 globals 重构 | 30 分钟 |
| T3 n-overrides | 60 分钟 |
| T4 TitleBar 移除品牌 | 15 分钟 |
| T5 AppLayout 微调 | 10 分钟 |
| T6 DashboardHero | 30 分钟 |
| T7 QuickActions | 30 分钟 |
| T8 TodayStats | 30 分钟 |
| T9 dashboard 重写 | 15 分钟 |
| T10 main.ts 引入 | 5 分钟 |
| T11 编译验证 | 30 分钟 |
| T13 sidebar-style 变量 | 15 分钟 |
| T14 theme store | 10 分钟 |
| T15 ThemeSettings 控件 | 10 分钟 |
| T16 分组激活 | 20 分钟 |
| T17 折叠弹窗 | 30 分钟 |
| T18 登录路由 | 15 分钟 |
| T19 装饰层 | 25 分钟 |
| T20 卡片 + Logo | 25 分钟 |
| T21 表单 + 按钮 | 20 分钟 |
| T22 综合验证 | 40 分钟 |
| T12 版本文档 | 20 分钟 |
| **总计** | **约 8 小时** |
