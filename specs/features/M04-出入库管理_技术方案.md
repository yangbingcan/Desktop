# M04-出入库管理 技术方案

> **功能模块**：出入库管理
> **目标版本**：v0.2.0
> **创建日期**：2026-07-02
> **依赖模块**：M01-商品档案、M02-库存管理

---

## 一、目标

补全 M04 出入库闭环：
1. **退货出库 Command**（`return_out`）- 退给供应商场景
2. **供应商档案 CRUD**（4 个 Command）- 采购/退货时选择供应商
3. **采购入库强化** - `supplier_id` 强制非空、列表显示供应商

---

## 二、数据库设计

### 2.1 迁移策略

`SCHEMA_VERSION` 从 `1` 递增到 `2`。在 [db/mod.rs](file:///d:/AI/AI/开发/茶叶店管理/tea-manage/src-tauri/src/db/mod.rs) 中通过 `PRAGMA user_version` 判断，仅执行增量。

### 2.2 新建表（v2 迁移）

#### suppliers（供应商表）

```sql
CREATE TABLE IF NOT EXISTS suppliers (
    id TEXT PRIMARY KEY NOT NULL,
    name TEXT NOT NULL,
    contact_person TEXT,
    contact_phone TEXT,
    address TEXT,
    main_categories TEXT DEFAULT '[]',  -- JSON 数组
    remark TEXT DEFAULT '',
    is_active INTEGER NOT NULL DEFAULT 1,
    created_at TEXT NOT NULL DEFAULT (datetime('now')),
    updated_at TEXT NOT NULL DEFAULT (datetime('now'))
);
CREATE INDEX idx_supplier_name ON suppliers(name);
CREATE INDEX idx_supplier_active ON suppliers(is_active);
```

**初始数据**（迁移时插入）：

```sql
INSERT OR IGNORE INTO suppliers (id, name) VALUES
    ('sup-default', '默认供应商'),
    ('sup-self', '自营采购');
```

#### return_orders（退货出库单表）

```sql
CREATE TABLE IF NOT EXISTS return_orders (
    id TEXT PRIMARY KEY NOT NULL,
    order_no TEXT NOT NULL UNIQUE,           -- TH+日期+序号
    supplier_id TEXT NOT NULL,
    return_date TEXT NOT NULL,               -- 退货日期 YYYY-MM-DD
    return_reason TEXT NOT NULL,             -- 质量问题/数量超出/保质期/其他
    total_amount REAL NOT NULL DEFAULT 0,
    remark TEXT DEFAULT '',
    status TEXT NOT NULL DEFAULT 'completed',
    created_at TEXT NOT NULL DEFAULT (datetime('now')),
    FOREIGN KEY (supplier_id) REFERENCES suppliers(id) ON DELETE RESTRICT
);
CREATE INDEX idx_return_order_no ON return_orders(order_no);
CREATE INDEX idx_return_supplier ON return_orders(supplier_id);
CREATE INDEX idx_return_date ON return_orders(return_date);
```

#### return_items（退货出库明细表）

```sql
CREATE TABLE IF NOT EXISTS return_items (
    id TEXT PRIMARY KEY NOT NULL,
    order_id TEXT NOT NULL,
    product_id TEXT NOT NULL,
    product_name TEXT NOT NULL,              -- 冗余
    unit_id TEXT NOT NULL,
    unit_name TEXT NOT NULL,                 -- 冗余
    batch_id TEXT NOT NULL,                  -- 原批次
    quantity INTEGER NOT NULL,
    unit_price REAL NOT NULL,
    grams INTEGER NOT NULL,
    subtotal REAL NOT NULL,
    created_at TEXT NOT NULL DEFAULT (datetime('now')),
    FOREIGN KEY (order_id) REFERENCES return_orders(id) ON DELETE CASCADE,
    FOREIGN KEY (product_id) REFERENCES products(id) ON DELETE RESTRICT,
    FOREIGN KEY (batch_id) REFERENCES inventory_batches(id) ON DELETE RESTRICT
);
CREATE INDEX idx_return_item_order ON return_items(order_id);
```

### 2.3 数据迁移（v1 → v2）

```sql
-- 历史采购单 supplier_id 为空的补默认值
UPDATE purchase_orders
SET supplier_id = 'sup-default'
WHERE supplier_id IS NULL OR supplier_id = '';
```

**注意**：`purchase_orders.supplier_id` 字段类型保持 nullable，新单据由应用层校验非空（避免 SQLite 重建表的开销）。

### 2.4 修改 schema.rs

在 [schema.rs](file:///d:/AI/AI/开发/茶叶店管理/tea-manage/src-tauri/src/db/schema.rs) 中：
- `SCHEMA_VERSION` 改为 `2`
- 新增 `migrate_v2_suppliers_and_return(conn)` 函数
- `run_migrations` 增加 `if current_version < 2` 分支

---

## 三、模型设计

### 3.1 新建文件：`models/supplier.rs`

```rust
//! 供应商数据模型

use serde::{Deserialize, Serialize};
use rusqlite::Row;

/// 供应商
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct Supplier {
    pub id: String,
    pub name: String,
    pub contact_person: Option<String>,
    pub contact_phone: Option<String>,
    pub address: Option<String>,
    pub main_categories: Vec<String>,
    pub remark: String,
    pub is_active: bool,
    pub created_at: String,
    pub updated_at: String,
}

impl Supplier {
    pub fn from_row(row: &Row) -> rusqlite::Result<Self> {
        let main_categories_str: String = row.get(5)?;
        let main_categories = serde_json::from_str(&main_categories_str).unwrap_or_default();
        let is_active_int: i64 = row.get(7)?;
        Ok(Supplier {
            id: row.get(0)?,
            name: row.get(1)?,
            contact_person: row.get(2)?,
            contact_phone: row.get(3)?,
            address: row.get(4)?,
            main_categories,
            remark: row.get(6)?,
            is_active: is_active_int != 0,
            created_at: row.get(8)?,
            updated_at: row.get(9)?,
        })
    }
}

/// 供应商输入（新增/编辑）
#[derive(Debug, Clone, Deserialize)]
pub struct SupplierInput {
    pub name: String,
    pub contact_person: Option<String>,
    pub contact_phone: Option<String>,
    pub address: Option<String>,
    pub main_categories: Vec<String>,
    pub remark: Option<String>,
}
```

### 3.2 新建文件：`models/return_order.rs`

```rust
//! 退货出库单数据模型

use serde::{Deserialize, Serialize};

/// 退货明细输入
#[derive(Debug, Clone, Deserialize)]
pub struct ReturnItemInput {
    pub product_id: String,
    pub unit_id: String,
    pub batch_id: String,
    pub quantity: i64,
}

/// 退货单输入
#[derive(Debug, Clone, Deserialize)]
pub struct ReturnOrderInput {
    pub supplier_id: String,
    pub return_date: String,        // YYYY-MM-DD
    pub return_reason: String,      // 质量问题/数量超出/保质期/其他
    pub remark: Option<String>,
    pub items: Vec<ReturnItemInput>,
}

/// 退货单明细（返回）
#[derive(Debug, Clone, Serialize)]
pub struct ReturnOrderItem {
    pub id: String,
    pub order_id: String,
    pub product_id: String,
    pub product_name: String,
    pub unit_id: String,
    pub unit_name: String,
    pub batch_id: String,
    pub batch_code: String,
    pub quantity: i64,
    pub unit_price: f64,
    pub grams: i64,
    pub subtotal: f64,
}

/// 退货单（返回）
#[derive(Debug, Clone, Serialize)]
pub struct ReturnOrder {
    pub id: String,
    pub order_no: String,
    pub supplier_id: String,
    pub supplier_name: String,
    pub return_date: String,
    pub return_reason: String,
    pub total_amount: f64,
    pub remark: String,
    pub items: Vec<ReturnOrderItem>,
    pub created_at: String,
}

/// 退货单列表项（不含明细）
#[derive(Debug, Clone, Serialize)]
pub struct ReturnOrderListItem {
    pub id: String,
    pub order_no: String,
    pub supplier_name: String,
    pub return_date: String,
    pub return_reason: String,
    pub total_amount: f64,
    pub item_count: i32,
    pub created_at: String,
}

/// 分页结果
#[derive(Debug, Clone, Serialize)]
pub struct PageResult<T> {
    pub list: Vec<T>,
    pub total: u32,
    pub page: u32,
    pub page_size: u32,
}
```

### 3.3 models/mod.rs 更新

```rust
mod product;
mod category;
mod inventory;
mod sales;
mod supplier;        // 新增
mod return_order;    // 新增

pub use product::*;
pub use category::*;
pub use inventory::*;
pub use sales::*;
pub use supplier::*;
pub use return_order::*;
```

---

## 四、Command 设计

### 4.1 新建文件：`commands/suppliers.rs`

| Command | 功能 | 入参 | 出参 |
|---------|------|------|------|
| `get_suppliers` | 供应商列表（分页 + 搜索） | `page?, page_size?, keyword?` | `PageResult<Supplier>` |
| `get_all_active_suppliers` | 所有启用的供应商（下拉用） | - | `Vec<Supplier>` |
| `create_supplier` | 新增供应商 | `SupplierInput` | `Supplier` |
| `update_supplier` | 编辑供应商 | `id, SupplierInput` | `Supplier` |
| `delete_supplier` | 软删除 | `id` | `()` |
| `get_supplier` | 详情 | `id` | `Supplier` |

**删除保护**：

```rust
// 检查是否有进货/退货单引用
let count: i64 = conn.query_row(
    "SELECT (SELECT COUNT(*) FROM purchase_orders WHERE supplier_id = ?) +
            (SELECT COUNT(*) FROM return_orders WHERE supplier_id = ?)",
    params![id, id], |row| row.get(0)
)?;
if count > 0 {
    return Err("该供应商已有进货/退货记录，不能删除（可停用）".to_string());
}
```

### 4.2 新建文件：`commands/return_order.rs`

| Command | 功能 | 入参 | 出参 |
|---------|------|------|------|
| `get_return_orders` | 退货单列表（分页 + 筛选） | `page?, page_size?, supplier_id?, return_reason?, date_range?` | `PageResult<ReturnOrderListItem>` |
| `get_return_order_detail` | 退货单详情 | `id` | `ReturnOrder` |
| `create_return_order` | 创建退货单 | `ReturnOrderInput` | `ReturnOrder` |
| `delete_return_order` | 删除退货单（还原库存） | `id` | `()` |
| `get_available_batches` | 查询某商品可用批次（退货选择用） | `product_id` | `Vec<BatchOption>` |

**`BatchOption` 模型**（用于前端下拉）：

```rust
#[derive(Debug, Clone, Serialize)]
pub struct BatchOption {
    pub id: String,
    pub batch_code: String,
    pub remaining_grams: i64,
    pub purchase_price: f64,
    pub created_at: String,
}
```

### 4.3 修改 `lib.rs` 注册

```rust
mod commands;
mod commands::suppliers;      // 新增
mod commands::return_order;    // 新增

.invoke_handler(tauri::generate_handler![
    // ... 已有的 28 个
    
    // 供应商（v0.2.0 新增）
    commands::get_suppliers,
    commands::get_all_active_suppliers,
    commands::create_supplier,
    commands::update_supplier,
    commands::delete_supplier,
    commands::get_supplier,
    
    // 退货出库（v0.2.0 新增）
    commands::get_return_orders,
    commands::get_return_order_detail,
    commands::create_return_order,
    commands::delete_return_order,
    commands::get_available_batches,
])
```

### 4.4 退货单创建核心逻辑

```rust
#[tauri::command]
pub async fn create_return_order(
    db: tauri::State<'_, Database>,
    input: ReturnOrderInput,
) -> Result<ReturnOrder, String> {
    // 1. 输入校验
    if input.items.is_empty() {
        return Err("退货明细不能为空".to_string());
    }
    for (i, item) in input.items.iter().enumerate() {
        if item.quantity <= 0 {
            return Err(format!("第{}行退货数量必须大于0", i + 1));
        }
    }
    if input.return_reason.trim().is_empty() {
        return Err("请选择退货原因".to_string());
    }
    
    let conn = db.get_conn()?;
    let now = Local::now().format("%Y-%m-%d %H:%M:%S").to_string();
    let order_id = Uuid::new_v4().to_string();
    let ts = Local::now();
    let order_no = format!("TH{}{:03}", ts.format("%Y%m%d%H%M%S"), (ts.timestamp_millis() % 1000) as u16);
    
    // 2. 开启排他事务
    conn.execute("BEGIN EXCLUSIVE TRANSACTION", [])
        .map_err(|e| e.to_string())?;
    
    // 辅助：回滚闭包
    let rollback = |e: String, conn: &Connection| -> String {
        let _ = conn.execute("ROLLBACK", []);
        e
    };
    
    // 3. 校验供应商
    let supplier_name: String = conn.query_row(
        "SELECT name FROM suppliers WHERE id = ? AND is_active = 1",
        [&input.supplier_id],
        |row| row.get(0),
    ).map_err(|e| rollback(format!("供应商校验失败: {}", e), &conn))?;
    
    // 4. 处理每个明细
    let mut total_amount = 0.0;
    let mut items = Vec::new();
    
    for item in &input.items {
        // 4.1 查询原批次 + 换算关系
        let (product_name, unit_name, batch_code, remaining, purchase_price, conversion): 
            (String, String, String, i64, f64, i64) = conn.query_row(
            "SELECT p.name, su.name, b.batch_code, b.remaining_grams, b.purchase_price, su.conversion_to_base
             FROM inventory_batches b
             JOIN products p ON p.id = b.product_id
             JOIN sales_units su ON su.id = ? AND su.product_id = b.product_id
             WHERE b.id = ?",
            params![item.unit_id, item.batch_id],
            |row| Ok((row.get(0)?, row.get(1)?, row.get(2)?, row.get(3)?, row.get(4)?, row.get(5)?))
        ).map_err(|e| rollback(format!("查询批次失败: {}", e), &conn))?;
        
        let grams = conversion * item.quantity;
        
        // 4.2 校验批次库存
        if remaining < grams {
            return Err(rollback(
                format!("商品[{}]批次[{}]剩余{}g，退货需要{}g", 
                    product_name, batch_code, remaining, grams),
                &conn
            ));
        }
        
        // 4.3 扣减批次
        conn.execute(
            "UPDATE inventory_batches SET remaining_grams = remaining_grams - ? WHERE id = ?",
            params![grams, item.batch_id]
        ).map_err(|e| rollback(format!("扣减批次失败: {}", e), &conn))?;
        
        // 4.4 扣减商品总库存
        conn.execute(
            "UPDATE products SET stock_grams = stock_grams - ? WHERE id = ?",
            params![grams, item.product_id]
        ).map_err(|e| rollback(format!("扣减库存失败: {}", e), &conn))?;
        
        // 4.5 记录流水
        let new_balance: i64 = conn.query_row(
            "SELECT stock_grams FROM products WHERE id = ?",
            [&item.product_id], |row| row.get(0)
        ).map_err(|e| rollback(format!("查询结余失败: {}", e), &conn))?;
        
        let flow_id = Uuid::new_v4().to_string();
        conn.execute(
            "INSERT INTO stock_flow (id, product_id, batch_id, flow_type, change_grams, balance_grams, order_id, remark, created_at)
             VALUES (?, ?, ?, 'return_out', ?, ?, ?, ?, ?)",
            params![flow_id, item.product_id, item.batch_id, -grams, new_balance, order_id, &input.return_reason, now]
        ).map_err(|e| rollback(format!("记录流水失败: {}", e), &conn))?;
        
        // 4.6 保存明细
        let subtotal = purchase_price * item.quantity as f64;
        total_amount += subtotal;
        let item_id = Uuid::new_v4().to_string();
        conn.execute(
            "INSERT INTO return_items (id, order_id, product_id, product_name, unit_id, unit_name, batch_id, quantity, unit_price, grams, subtotal, created_at)
             VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)",
            params![item_id, order_id, item.product_id, product_name, item.unit_id, unit_name, item.batch_id, item.quantity, purchase_price, grams, subtotal, now]
        ).map_err(|e| rollback(format!("保存明细失败: {}", e), &conn))?;
        
        items.push(ReturnOrderItem {
            id: item_id,
            order_id: order_id.clone(),
            product_id: item.product_id.clone(),
            product_name,
            unit_id: item.unit_id.clone(),
            unit_name,
            batch_id: item.batch_id.clone(),
            batch_code,
            quantity: item.quantity,
            unit_price: purchase_price,
            grams,
            subtotal,
        });
    }
    
    // 5. 创建退货单
    conn.execute(
        "INSERT INTO return_orders (id, order_no, supplier_id, return_date, return_reason, total_amount, remark, created_at)
         VALUES (?, ?, ?, ?, ?, ?, ?, ?)",
        params![order_id, order_no, input.supplier_id, input.return_date, input.return_reason, total_amount, input.remark, now]
    ).map_err(|e| rollback(format!("创建退货单失败: {}", e), &conn))?;
    
    // 6. 提交
    conn.execute("COMMIT", []).map_err(|e| e.to_string())?;
    
    Ok(ReturnOrder {
        id: order_id,
        order_no,
        supplier_id: input.supplier_id,
        supplier_name,
        return_date: input.return_date,
        return_reason: input.return_reason,
        total_amount,
        remark: input.remark.unwrap_or_default(),
        items,
        created_at: now,
    })
}
```

### 4.5 退货单删除（还原库存）

```rust
#[tauri::command]
pub async fn delete_return_order(
    db: tauri::State<'_, Database>,
    order_id: String,
) -> Result<(), String> {
    let conn = db.get_conn()?;
    
    // 开启排他事务
    conn.execute("BEGIN EXCLUSIVE TRANSACTION", []).map_err(|e| e.to_string())?;
    
    // 查询明细
    let mut stmt = conn.prepare(
        "SELECT product_id, batch_id, grams FROM return_items WHERE order_id = ?"
    ).map_err(|e| e.to_string())?;
    
    let items: Vec<(String, String, i64)> = stmt.query_map([&order_id], |row| {
        Ok((row.get(0)?, row.get(1)?, row.get(2)?))
    }).map_err(|e| e.to_string())?
    .collect::<Result<Vec<_>, _>>()
    .map_err(|e| e.to_string())?;
    
    // 还原库存
    for (product_id, batch_id, grams) in items {
        conn.execute(
            "UPDATE products SET stock_grams = stock_grams + ? WHERE id = ?",
            params![grams, product_id]
        ).map_err(|e| { let _ = conn.execute("ROLLBACK", []); e.to_string() })?;
        
        conn.execute(
            "UPDATE inventory_batches SET remaining_grams = remaining_grams + ? WHERE id = ?",
            params![grams, batch_id]
        ).map_err(|e| { let _ = conn.execute("ROLLBACK", []); e.to_string() })?;
    }
    
    // 删除退货单（CASCADE 删除明细）
    conn.execute("DELETE FROM return_orders WHERE id = ?", [&order_id])
        .map_err(|e| { let _ = conn.execute("ROLLBACK", []); e.to_string() })?;
    
    conn.execute("COMMIT", []).map_err(|e| e.to_string())?;
    Ok(())
}
```

---

## 五、前端设计

### 5.1 新建文件

| 文件 | 说明 |
|------|------|
| `src/api/suppliers.ts` | 供应商 API 封装 |
| `src/api/returnOrders.ts` | 退货单 API 封装 |
| `src/stores/suppliers.ts` | 供应商 Pinia Store |
| `src/views/suppliers/index.vue` | 供应商列表（CRUD） |
| `src/views/suppliers/SupplierForm.vue` | 供应商表单（新增/编辑） |
| `src/views/returns/ReturnList.vue` | 退货单列表 |
| `src/views/returns/ReturnForm.vue` | 退货单表单（新增） |
| `src/views/returns/ReturnDetail.vue` | 退货单详情 |

### 5.2 修改文件

| 文件 | 修改 |
|------|------|
| `src/api/purchase.ts` | 增加 `supplierId` 必填校验 |
| `src/views/purchase/PurchaseForm.vue` | 供应商下拉改为必填 |
| `src/router/index.ts` | 新增 6 条路由 |

### 5.3 新增路由

```typescript
// 供应商
{ path: '/suppliers', name: 'SupplierList', component: SupplierList, meta: { title: '供应商管理' } }
{ path: '/suppliers/new', name: 'SupplierNew', component: SupplierForm, meta: { title: '新增供应商' } }
{ path: '/suppliers/:id/edit', name: 'SupplierEdit', component: SupplierForm, meta: { title: '编辑供应商' } }

// 退货
{ path: '/returns', name: 'ReturnList', component: ReturnList, meta: { title: '退货出库' } }
{ path: '/returns/new', name: 'ReturnNew', component: ReturnForm, meta: { title: '新增退货单' } }
{ path: '/returns/:id', name: 'ReturnDetail', component: ReturnDetail, meta: { title: '退货单详情' } }
```

### 5.4 退货单表单交互流程

```
[选择供应商] → [选择商品] → [选择销售单位] → [自动加载该商品可用批次下拉] 
    → [选择批次] → [输入退货数量] → [校验 ≤ 批次剩余] 
    → [选择退货原因] → [保存]
```

---

## 六、并发与一致性

### 6.1 排他事务

- 退货单创建：`BEGIN EXCLUSIVE TRANSACTION`
- 退货单删除：`BEGIN EXCLUSIVE TRANSACTION`（同时还原库存）

### 6.2 批次锁

退货单按 `batch_id` 精确锁定批次，避免与销售/报损的 FIFO 扣减冲突。

### 6.3 错误回滚

所有 SQL 失败必须 `ROLLBACK` + 返回错误信息。已在 helper 闭包 `rollback` 中统一处理。

---

## 七、测试策略

### 7.1 单元测试（Rust）

在 [commands/return_order.rs](file:///d:/AI/AI/开发/茶叶店管理/tea-manage/src-tauri/src/commands/return_order.rs) 末尾添加：

```rust
#[cfg(test)]
mod tests {
    use super::*;
    
    #[test]
    fn test_validate_return_items_empty() {
        let items: Vec<ReturnItemInput> = vec![];
        assert!(validate_return_items(&items).is_err());
    }
    
    #[test]
    fn test_validate_return_items_zero_quantity() {
        let items = vec![ReturnItemInput {
            product_id: "p1".to_string(),
            unit_id: "u1".to_string(),
            batch_id: "b1".to_string(),
            quantity: 0,
        }];
        assert!(validate_return_items(&items).is_err());
    }
    
    #[test]
    fn test_validate_return_reason_empty() {
        assert!(validate_return_reason("").is_err());
        assert!(validate_return_reason("质量问题").is_ok());
    }
}
```

### 7.2 集成测试

手动测试场景：

| 场景 | 预期 |
|------|------|
| 退货数量超过批次剩余 | 报错"批次剩余 Xg，退货需要 Yg" |
| 退货后库存为 0 | 商品列表显示 0，可继续操作 |
| 退货后删除单据 | 库存正确还原 |
| 供应商有 5 条进货单 | 删除供应商时被拒绝 |

---

## 八、迁移清单

| 文件 | 变更类型 | 说明 |
|------|---------|------|
| `db/schema.rs` | 修改 | SCHEMA_VERSION → 2，新增 v2 迁移函数 |
| `models/mod.rs` | 修改 | 注册新模块 |
| `models/supplier.rs` | 新建 | 供应商模型 |
| `models/return_order.rs` | 新建 | 退货单模型 |
| `commands/mod.rs` | 修改 | 注册新模块 |
| `commands/suppliers.rs` | 新建 | 6 个供应商 Command |
| `commands/return_order.rs` | 新建 | 5 个退货 Command |
| `lib.rs` | 修改 | 注册 11 个新 Command |
| `api/suppliers.ts` | 新建 | 前端 API |
| `api/returnOrders.ts` | 新建 | 前端 API |
| `stores/suppliers.ts` | 新建 | Pinia Store |
| `views/suppliers/index.vue` | 新建 | 列表 |
| `views/suppliers/SupplierForm.vue` | 新建 | 表单 |
| `views/returns/ReturnList.vue` | 新建 | 列表 |
| `views/returns/ReturnForm.vue` | 新建 | 表单 |
| `views/returns/ReturnDetail.vue` | 新建 | 详情 |
| `router/index.ts` | 修改 | 新增 6 条路由 |
| `views/purchase/PurchaseForm.vue` | 修改 | 供应商下拉必填 |

**共 11 个新文件 + 5 个修改文件**

---

## 九、风险与回退

| 风险 | 概率 | 应对 |
|------|------|------|
| 数据库迁移失败 | 低 | `user_version` 未推进，下次启动自动重试 |
| 退货单删除后批次已部分消耗 | 中 | 退货时记录 batch_id，删除时按 batch_id + grams 精确还原 |
| 前端 `supplier_id` 必填校验遗漏 | 中 | 前后端双重校验 |

**回退方案**：
- 如 v0.2.0 退货功能有严重 BUG，可在前端隐藏路由 + 后端不注册 Command 实现"软回退"
- 数据库表保留不影响主流程

---

## 十、下一阶段

进入 [M04-出入库管理_任务规划.md](file:///d:/AI/AI/开发/茶叶店管理/specs/features/M04-出入库管理_任务规划.md) 拆解为 TDD 任务。
