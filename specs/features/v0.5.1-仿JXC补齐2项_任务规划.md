# v0.5.1 任务规划 - 仿 JXC 补齐 2 项

> **版本号**：v0.5.1
> **前置版本**：v0.5.0
> **总任务数**：8 个
> **总预估工时**：约 1.5 小时
> **TDD 模式**：否（纯 UI 视觉，无可单元测试的逻辑；登录页 localStorage 工具用手动验证替代）

---

## 一、任务依赖图

```
T1 侧栏连线 CSS ───────────────────────────┐
                                            ├──→ T6 综合编译验证
T2 Login 10→15 光点 + 主题切换按钮 ───────┤
                                            ├──→ T7 浏览器综合验证
T3 Login 记住密码 + localStorage 工具 ─────┤
                                            ├──→ T8 release 文档
T4 Login onMounted 改造 ──────────────────┤
T5 Login handleLogin 改造 ────────────────┘
```

T1 独立；T2/T3/T4/T5 都在 Login.vue 同一文件内，可顺序进行；T6/T7/T8 收尾。

---

## 二、任务清单

### T1：侧栏树状分组连线（CSS-only）

| 字段 | 内容 |
|------|------|
| 文件 | `tea-manage/src/components/layout/Sidebar.vue` |
| 位置 | `<style scoped>` 末尾 |
| 类型 | CSS-only |
| 依赖 | 无 |
| 预估 | 10 分钟 |
| 验收 | 展开态 3 个分组内子项左边各显示 1px dashed 主色竖线，折叠态不显示 |

**具体改动**：
```scss
/* 展开态：分组容器相对定位（如果还没有） */
.tea-sider:not(.tea-sider-collapsed) .tea-sider-group {
  position: relative;
}

/* 展开态：分组子项列表左侧 dashed 竖线 */
.tea-sider:not(.tea-sider-collapsed) .tea-sider-group::after {
  content: '';
  position: absolute;
  left: 16px;
  top: 30px;
  bottom: 4px;
  width: 1px;
  border-left: 1px dashed var(--tea-primary);
  opacity: 0.3;
  pointer-events: none;
  z-index: 0;
  transition: opacity var(--tea-transition-normal),
              border-color var(--tea-transition-normal);
}

/* 深色模式适配 */
[data-theme='dark'] .tea-sider:not(.tea-sider-collapsed) .tea-sider-group::after {
  opacity: 0.45;
}
```

**风险检查**：
- 不能与现有 `.tea-sider-group-has-active::before` 冲突（已确认：active 竖条在 .tea-sider-section 上，新竖线在 .tea-sider-group 上）
- 不能与现有折叠态 `.tea-sider-group::after` 冲突（已确认：折叠态的 ::after 用 .tea-sider-collapsed 选择器，新规则用 .tea-sider:not(.tea-sider-collapsed) 排除）

---

### T2：Login 光点 10 → 15 + 主题切换按钮

| 字段 | 内容 |
|------|------|
| 文件 | `tea-manage/src/views/auth/Login.vue` |
| 位置 | `<script setup>` 顶部 + `<template>` 卡片右上角 + `<style scoped>` |
| 类型 | UI + 状态 |
| 依赖 | 无 |
| 预估 | 15 分钟 |
| 验收 | 登录页渲染 15 个光斑；卡片右上角有主题切换按钮，点击切换深浅色 |

**具体改动**：

**改动 1**（光点数量）：
```diff
- for (let i = 0; i < 10; i++) {
+ for (let i = 0; i < 15; i++) {
```

**改动 2**（template 加主题按钮）：
```html
<button
  class="tea-login-theme-toggle"
  :title="themeMode === 'dark' ? '切换浅色' : '切换深色'"
  @click="toggleTheme"
>
  <n-icon size="14">
    <component :is="themeMode === 'dark' ? SunnyOutline : MoonOutline" />
  </n-icon>
</button>
```
位置：放在 `.tea-login-card` 内、`.tea-login-card-content` 之前（即 absolute 定位浮在卡片上）

**改动 3**（script 引入）：
```typescript
import { SunnyOutline, MoonOutline } from '@vicons/ionicons5'
import { useThemeStore } from '@/stores/theme'

const themeStore = useThemeStore()
const themeMode = computed(() => themeStore.themeMode)
const toggleTheme = () => themeStore.toggleTheme()
```

**改动 4**（CSS）：
```scss
.tea-login-theme-toggle {
  position: absolute;
  top: 12px;
  right: 12px;
  width: 32px;
  height: 32px;
  display: flex;
  align-items: center;
  justify-content: center;
  background: var(--tea-hover-bg);
  border: 1px solid var(--tea-border);
  border-radius: 50%;
  color: var(--tea-text-secondary);
  cursor: pointer;
  transition: all var(--tea-transition-fast);
  z-index: 11;
}

.tea-login-theme-toggle:hover {
  color: var(--tea-primary);
  background: var(--tea-primary-supply);
  transform: translateY(-1px);
}
```

---

### T3：Login 记住密码 UI + localStorage 工具

| 字段 | 内容 |
|------|------|
| 文件 | `tea-manage/src/views/auth/Login.vue` |
| 位置 | `<template>` 表单内按钮上方 + `<script setup>` 工具函数 |
| 类型 | UI + 状态 + 工具 |
| 依赖 | 无 |
| 预估 | 20 分钟 |
| 验收 | 表单有"记住密码"checkbox，勾选状态可切换 |

**具体改动**：

**改动 1**（template 加 checkbox）：
```html
<div class="tea-login-remember">
  <label class="tea-login-remember-label">
    <input
      v-model="rememberPassword"
      type="checkbox"
      class="tea-login-remember-checkbox"
    />
    <span>记住密码</span>
  </label>
</div>
```
位置：`.tea-login-form` 内、`.tea-login-btn` 之前

**改动 2**（script 加状态）：
```typescript
const rememberPassword = ref(false)
```

**改动 3**（script 加工具函数）：
```typescript
const STORAGE_KEY_USERNAME = 'tea-last-username'
const STORAGE_KEY_REMEMBER = 'tea-remember'
const STORAGE_KEY_PASSWORD = 'tea-last-password'

function encodeBase64(str: string): string {
  try {
    return btoa(unescape(encodeURIComponent(str)))
  } catch {
    return ''
  }
}

function decodeBase64(str: string): string {
  try {
    return decodeURIComponent(escape(atob(str)))
  } catch {
    return ''
  }
}

function loadRememberedLogin(): { username: string; password: string; remember: boolean } {
  try {
    const remember = localStorage.getItem(STORAGE_KEY_REMEMBER) === 'true'
    const username = localStorage.getItem(STORAGE_KEY_USERNAME) || ''
    const password = remember
      ? decodeBase64(localStorage.getItem(STORAGE_KEY_PASSWORD) || '')
      : ''
    return { username, password, remember }
  } catch {
    return { username: '', password: '', remember: false }
  }
}

function saveRememberedLogin(username: string, password: string, remember: boolean) {
  try {
    if (remember && username) {
      localStorage.setItem(STORAGE_KEY_REMEMBER, 'true')
      localStorage.setItem(STORAGE_KEY_USERNAME, username)
      localStorage.setItem(STORAGE_KEY_PASSWORD, encodeBase64(password))
    } else {
      localStorage.setItem(STORAGE_KEY_REMEMBER, 'false')
      localStorage.setItem(STORAGE_KEY_USERNAME, username)
      localStorage.removeItem(STORAGE_KEY_PASSWORD)
    }
  } catch {
    /* 忽略 */
  }
}
```

**改动 4**（CSS）：
```scss
.tea-login-remember {
  display: flex;
  justify-content: flex-end;
  margin-top: -8px;
}

.tea-login-remember-label {
  display: flex;
  align-items: center;
  gap: 6px;
  font-size: 12px;
  color: var(--tea-text-tertiary);
  cursor: pointer;
  user-select: none;
}

.tea-login-remember-checkbox {
  width: 14px;
  height: 14px;
  accent-color: var(--tea-primary);
  cursor: pointer;
}
```

---

### T4：Login onMounted 改造（硬编码 demo → localStorage 回填）

| 字段 | 内容 |
|------|------|
| 文件 | `tea-manage/src/views/auth/Login.vue` |
| 位置 | `<script setup>` onMounted |
| 类型 | 逻辑 |
| 依赖 | T3（工具函数） |
| 预估 | 10 分钟 |
| 验收 | 首次进入登录页有 demo 填充；二次进入如有 localStorage 数据则回填 |

**具体改动**：

```diff
- onMounted(() => {
-   // 默认填充演示账号
-   form.value.username = 'demo'
-   form.value.password = 'demo'
- })
+ onMounted(() => {
+   // 从 localStorage 恢复登录信息
+   const saved = loadRememberedLogin()
+   if (saved.username) {
+     form.value.username = saved.username
+     if (saved.remember && saved.password) {
+       form.value.password = saved.password
+       rememberPassword.value = true
+     }
+   } else {
+     // 首次进入填充演示账号
+     form.value.username = 'demo'
+     form.value.password = 'demo'
+   }
+ })
```

---

### T5：Login handleLogin 改造（保存到 localStorage）

| 字段 | 内容 |
|------|------|
| 文件 | `tea-manage/src/views/auth/Login.vue` |
| 位置 | `<script setup>` handleLogin |
| 类型 | 逻辑 |
| 依赖 | T3（工具函数） |
| 预估 | 5 分钟 |
| 验收 | 点击登录后，localStorage 有保存的用户名/密码/remember 状态 |

**具体改动**：

```diff
  async function handleLogin() {
    if (loading.value) return
    loading.value = true
+   // 保存登录信息到 localStorage
+   saveRememberedLogin(form.value.username, form.value.password, rememberPassword.value)
    // 模拟一点点延迟，让按钮 shimmer 走完
    setTimeout(() => {
      loading.value = false
      router.push('/')
    }, 400)
  }
```

---

### T6：综合编译验证

| 字段 | 内容 |
|------|------|
| 命令 | `cd tea-manage && npm run build` |
| 类型 | 验证 |
| 依赖 | T1~T5 |
| 预估 | 1 分钟 |
| 验收 | vue-tsc 通过 + vite build 成功 |

---

### T7：浏览器综合验证（Playwright）

| 字段 | 内容 |
|------|------|
| 命令 | `npm run dev` + 浏览器走 7 步 |
| 类型 | 验证 |
| 依赖 | T6 |
| 预估 | 15 分钟 |
| 验收 | 7 步全部通过 |

**7 步验证**：
1. 访问 /，截图展开态
2. 视觉确认：侧栏每个分组内子项左边有 1px dashed 竖线
3. 折叠侧栏，竖线消失
4. 访问 /login，截图
5. 视觉确认：15 个光斑 + 主题切换按钮 + 记住密码 checkbox
6. 点击主题切换按钮，登录页立即变深色
7. 勾选记住密码 + 输入 demo/demo + 点击登录，刷新 /login 验证回填

---

### T8：更新 v0.5.1 release 文档

| 字段 | 内容 |
|------|------|
| 文件 | `specs/releases/v0.5.1.md` |
| 类型 | 文档 |
| 依赖 | T7 |
| 预估 | 10 分钟 |
| 验收 | 文档完整 + 综合验证清单已勾选 |

---

## 三、任务统计

| 任务 | 类型 | 预估 | 状态 |
|------|------|------|------|
| T1 侧栏连线 | CSS-only | 10 分钟 | ⏳ |
| T2 15 光点 + 主题按钮 | UI + 状态 | 15 分钟 | ⏳ |
| T3 记住密码 + 工具 | UI + 工具 | 20 分钟 | ⏳ |
| T4 onMounted 改造 | 逻辑 | 10 分钟 | ⏳ |
| T5 handleLogin 改造 | 逻辑 | 5 分钟 | ⏳ |
| T6 编译验证 | 验证 | 1 分钟 | ⏳ |
| T7 浏览器验证 | 验证 | 15 分钟 | ⏳ |
| T8 release 文档 | 文档 | 10 分钟 | ⏳ |
| **合计** | - | **约 86 分钟 ≈ 1.5 小时** | - |

---

## 四、关联文档

- 需求澄清：[v0.5.1-仿JXC补齐2项_需求澄清.md](./v0.5.1-仿JXC补齐2项_需求澄清.md)
- 技术方案：[v0.5.1-仿JXC补齐2项_技术方案.md](./v0.5.1-仿JXC补齐2项_技术方案.md)
- 前置版本：[v0.5.0 版本发布文档](../releases/v0.5.0.md)
