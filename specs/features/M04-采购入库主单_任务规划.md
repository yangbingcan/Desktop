# 任务规划 - 采购入库主单持久化

> **功能模块**：M04 出入库管理 - 采购入库单闭环
> **目标版本**：v0.3.0
> **创建日期**：2026-07-02
> **关联文档**：
> - 需求：[M04-采购入库主单.md](file:///d:/AI/AI/开发/茶叶店管理/specs/features/M04-采购入库主单.md)
> - 技术方案：[M04-采购入库主单_技术方案.md](file:///d:/AI/AI/开发/茶叶店管理/specs/features/M04-采购入库主单_技术方案.md)

---

## 一、任务概览

| 项目 | 内容 |
|------|------|
| 功能名称 | 采购入库主单持久化 + 列表 API + 详情页 |
| 任务总数 | 12 个任务（含 2 个版本控制任务） |
| 预计工时 | 4 工作日（后端 2.5 天 + 前端 1.5 天） |
| 依赖任务 | 5 个（v0.2.0 已完成） |
| 阻塞 AC | AC-P-02、AC-P-03（v0.2.0 遗留） |
| 数据库变更 | ⚠️ 涉及（新增 2 张表 + 6 个索引） |

---

## 二、阶段划分

### 阶段一：数据层（后端）
- Task-01: 编写数据库迁移脚本（migrate_v3_purchase_orders）
- Task-02: 升级 SCHEMA_VERSION 到 3

### 阶段二：模型层（后端）
- Task-03: 扩展 PurchaseInput 增 paymentStatus 字段
- Task-04: 新增 PurchaseOrderListItem 结构体

### 阶段三：Command 层（后端）
- Task-05: 重构 purchase_in Command（事务中插入主单+明细）
- Task-06: 新增 get_purchase_orders Command
- Task-07: 新增 get_purchase_order_detail Command
- Task-08: 在 commands/mod.rs 注册新 Commands

### 阶段四：API 层（前端）
- Task-09: 新增 src/api/purchases.ts

### 阶段五：表现层（前端）
- Task-10: 新增 PurchaseDetail.vue
- Task-11: 重构 PurchaseList.vue（真实列表+筛选+分页）
- Task-12: 更新 router/index.ts（detail 路由）

### 阶段六：质量保障（后端）
- Task-13: 编写单元测试（5 个测试用例）

### 阶段七：版本控制（强制）
- Task-14: 更新版本号（package.json + Cargo.toml + project_config.md）
- Task-15: 更新 changelog.json 新增 v0.3.0 条目

---

## 三、任务详情

---

### Task-01: 编写数据库迁移脚本（migrate_v3_purchase_orders）

**任务说明**
新增 `migrate_v3_purchase_orders` 函数，创建 `purchase_orders` 和 `purchase_items` 两张表及相关索引。

**涉及文件**
- [schema.rs](file:///d:/AI/AI/开发/茶叶店管理/tea-manage/src-tauri/src/db/schema.rs)

**测试文件**
- 无（数据库迁移不写单独测试，依赖后续 Command 测试验证）

**验证标准**
- [ ] 迁移函数代码完整，无编译错误
- [ ] 所有 SQL 使用 `IF NOT EXISTS` 保证幂等
- [ ] 所有外键约束正确

**依赖任务**
- 无

**对应验收标准**
- AC-08: 数据库 schema 升级到 v3

---

#### 步骤 1.1: 在 schema.rs 添加 migrate_v3_purchase_orders 函数

**前置条件**
- [ ] 已阅读 [schema.rs](file:///d:/AI/AI/开发/茶叶店管理/tea-manage/src-tauri/src/db/schema.rs) 现有迁移模式
- [ ] 了解 v0.2.0 的 `migrate_v2_suppliers_and_return` 实现

**执行动作**
在 `schema.rs` 末尾（约 line 160 之后）新增函数：

```rust
/// v3 迁移：新增采购入库主单 + 明细
/// 
/// M04 出入库闭环需要：
/// 1. purchase_orders - 采购入库主单（持久化主单+付款状态）
/// 2. purchase_items - 采购入库明细（持久化商品行）
/// 
/// 数据迁移策略：v2 时期的采购入库没有主单，不回填
pub fn migrate_v3_purchase_orders(conn: &Connection) -> SqliteResult<()> {
    // 1. 采购主单表
    conn.execute(
        "CREATE TABLE IF NOT EXISTS purchase_orders (
            id TEXT PRIMARY KEY NOT NULL,
            order_no TEXT NOT NULL UNIQUE,
            supplier_id TEXT NOT NULL,
            handler TEXT,
            total_amount REAL NOT NULL DEFAULT 0,
            payment_status TEXT NOT NULL DEFAULT 'unpaid'
                CHECK (payment_status IN ('unpaid', 'partial', 'paid')),
            remark TEXT NOT NULL DEFAULT '',
            created_at TEXT NOT NULL DEFAULT (datetime('now')),
            FOREIGN KEY (supplier_id) REFERENCES suppliers(id) ON DELETE RESTRICT
        )",
        [],
    )?;

    // 主单索引
    conn.execute(
        "CREATE UNIQUE INDEX IF NOT EXISTS idx_purchase_order_no ON purchase_orders(order_no)",
        [],
    )?;
    conn.execute(
        "CREATE INDEX IF NOT EXISTS idx_purchase_supplier ON purchase_orders(supplier_id)",
        [],
    )?;
    conn.execute(
        "CREATE INDEX IF NOT EXISTS idx_purchase_date ON purchase_orders(created_at)",
        [],
    )?;
    conn.execute(
        "CREATE INDEX IF NOT EXISTS idx_purchase_payment_status ON purchase_orders(payment_status)",
        [],
    )?;

    // 2. 采购明细表
    conn.execute(
        "CREATE TABLE IF NOT EXISTS purchase_items (
            id TEXT PRIMARY KEY NOT NULL,
            order_id TEXT NOT NULL,
            product_id TEXT NOT NULL,
            product_name TEXT NOT NULL,
            unit_id TEXT NOT NULL,
            unit_name TEXT NOT NULL,
            quantity INTEGER NOT NULL,
            grams INTEGER NOT NULL,
            unit_price REAL NOT NULL,
            subtotal REAL NOT NULL,
            batch_id TEXT NOT NULL,
            batch_code TEXT NOT NULL,
            created_at TEXT NOT NULL DEFAULT (datetime('now')),
            FOREIGN KEY (order_id) REFERENCES purchase_orders(id) ON DELETE CASCADE,
            FOREIGN KEY (product_id) REFERENCES products(id) ON DELETE RESTRICT,
            FOREIGN KEY (unit_id) REFERENCES sales_units(id) ON DELETE RESTRICT,
            FOREIGN KEY (batch_id) REFERENCES inventory_batches(id) ON DELETE RESTRICT
        )",
        [],
    )?;

    conn.execute(
        "CREATE INDEX IF NOT EXISTS idx_purchase_item_order ON purchase_items(order_id)",
        [],
    )?;

    Ok(())
}
```

**预期结果**
- 函数编译通过（`cargo check`）
- 函数签名 `pub fn migrate_v3_purchase_orders(conn: &Connection) -> SqliteResult<()>`

**验证方法**
```powershell
# 在项目根目录
cd d:\AI\AI\开发\茶叶店管理\tea-manage\src-tauri
cargo check
```
预期：无编译错误，警告数量不增加。

**回滚方案**
- 删除整个 `migrate_v3_purchase_orders` 函数
- 不修改 `run_migrations`（在 Task-02 中才修改）

**输出物**
- `schema.rs` 中新增的 `migrate_v3_purchase_orders` 函数（约 60 行）

---

### Task-02: 升级 SCHEMA_VERSION 到 3 并注册迁移

**任务说明**
将 `SCHEMA_VERSION` 从 2 升级到 3，并在 `run_migrations` 中注册 v3 迁移。

**涉及文件**
- [schema.rs](file:///d:/AI/AI/开发/茶叶店管理/tea-manage/src-tauri/src/db/schema.rs)

**验证标准**
- [ ] `SCHEMA_VERSION = 3`
- [ ] `run_migrations` 中新增 `if current_version < 3 { migrate_v3_purchase_orders(conn)?; }`

**依赖任务**
- Task-01

**对应验收标准**
- AC-08: 数据库 schema 升级到 v3

---

#### 步骤 2.1: 修改 SCHEMA_VERSION 常量

**前置条件**
- [ ] Task-01 完成

**执行动作**
修改 [schema.rs:14](file:///d:/AI/AI/开发/茶叶店管理/tea-manage/src-tauri/src/db/schema.rs#L14)：

```rust
// 原来
const SCHEMA_VERSION: u32 = 2;

// 改为
/// 当前数据库 Schema 版本号
/// 每次涉及表结构变更时递增，迁移逻辑据此判断需要执行哪些变更
/// 
/// 版本历史：
/// - v1: 初始版本（商品/库存/会员/销售）
/// - v2: 新增供应商表、退货出库单表、退货出库明细表（M04 出入库闭环）
/// - v3: 新增采购入库主单表 + 采购明细表（M04 出入库闭环完善）
const SCHEMA_VERSION: u32 = 3;
```

**预期结果**
- 常量值变为 3
- 注释增加 v3 说明

**验证方法**
```powershell
# 查看文件
Get-Content d:\AI\AI\开发\茶叶店管理\tea-manage\src-tauri\src\db\schema.rs -TotalCount 20
```

**回滚方案**
- 还原 `SCHEMA_VERSION: u32 = 2;`
- 还原注释

**输出物**
- 修改后的 `schema.rs` 第 14 行

---

#### 步骤 2.2: 在 run_migrations 注册 v3 迁移

**前置条件**
- [ ] 步骤 2.1 完成

**执行动作**
修改 [schema.rs:20-41](file:///d:/AI/AI/开发/茶叶店管理/tea-manage/src-tauri/src/db/schema.rs#L20-L41) 的 `run_migrations` 函数：

```rust
pub fn run_migrations(conn: &Connection) -> SqliteResult<()> {
    let current_version: u32 = conn.pragma_query_value(None, "user_version", |row| row.get(0))?;

    if current_version < 1 {
        // v1: 初始版本 - 创建所有表
        create_tables(conn)?;
    }

    if current_version < 2 {
        // v2: M04 出入库闭环 - 供应商表 + 退货出库单 + 退货出库明细
        migrate_v2_suppliers_and_return(conn)?;
    }

    if current_version < 3 {
        // v3: M04 出入库闭环完善 - 采购入库主单 + 采购明细
        migrate_v3_purchase_orders(conn)?;
    }

    if current_version < SCHEMA_VERSION {
        // 创建复合索引（v1 已建，此处兼容后续 v0 升级到 v1 场景）
        create_composite_indexes(conn)?;
        // 标记为最新版本
        conn.pragma_update(None, "user_version", SCHEMA_VERSION)?;
    }

    Ok(())
}
```

**预期结果**
- `run_migrations` 中包含 v3 的 if 分支
- 函数体保持原有的迁移顺序（v1 → v2 → v3）

**验证方法**
```powershell
cd d:\AI\AI\开发\茶叶店管理\tea-manage\src-tauri
cargo check
```
预期：无错误，函数可以正常调用。

**回滚方案**
- 删除新增的 `if current_version < 3 { ... }` 分支

**输出物**
- 修改后的 `run_migrations` 函数

---

#### 步骤 2.3: 验证迁移可幂等执行

**前置条件**
- [ ] 步骤 2.2 完成

**执行动作**
启动应用一次（首次运行会执行 v3 迁移），然后再次启动（验证幂等）：

```powershell
# 第一次启动
cd d:\AI\AI\开发\茶叶店管理\tea-manage
npm run tauri dev
# 应用启动后查看是否有迁移日志，正常退出

# 第二次启动（应无任何迁移操作，直接使用 user_version=3）
npm run tauri dev
# 应用启动后查看是否还有迁移日志
```

**预期结果**
- 首次启动：`user_version` 从 2 升到 3，创建 2 张表 + 6 个索引
- 二次启动：无任何迁移操作（因 `current_version >= 3`）
- 验证：使用 SQLite 工具查看 `PRAGMA user_version` 和表结构

**验证方法**
```powershell
# 找到数据库文件（应用启动后会创建）
# 路径：%APPDATA%\tea-manage\tea.db 或项目内 .db 文件
# 使用 sqlite3 CLI 或 DB Browser for SQLite 检查：
# PRAGMA user_version;  -- 应返回 3
# .tables purchase_orders purchase_items  -- 两张表都存在
# .schema purchase_orders  -- 字段符合技术方案
# .schema purchase_items
```

**回滚方案**
- 手动删除数据库文件（仅用于开发测试）
- 重新启动应用会重新执行迁移

**输出物**
- 数据库中实际存在的 `purchase_orders` 和 `purchase_items` 表

---

### Task-03: 扩展 PurchaseInput 增 paymentStatus 字段

**任务说明**
修改 Rust 侧的 `PurchaseInput` 结构体，新增 `payment_status` 可选字段。

**涉及文件**
- [models/inventory.rs](file:///d:/AI/AI/开发/茶叶店管理/tea-manage/src-tauri/src/models/inventory.rs#L91-L98)

**测试文件**
- 无（依赖 Command 层的单元测试覆盖）

**验证标准**
- [ ] `PurchaseInput` 包含 `payment_status: Option<String>`
- [ ] 编译通过
- [ ] serde 自动反序列化（前端传 camelCase 字段）

**依赖任务**
- 无

**对应验收标准**
- AC-01: 创建采购入库单时持久化主单（含付款状态）

---

#### 步骤 3.1: 修改 PurchaseInput 结构体

**前置条件**
- [ ] 已阅读 [models/inventory.rs](file:///d:/AI/AI/开发/茶叶店管理/tea-manage/src-tauri/src/models/inventory.rs) 现有定义

**执行动作**
修改 [models/inventory.rs:91-98](file:///d:/AI/AI/开发/茶叶店管理/tea-manage/src-tauri/src/models/inventory.rs#L91-L98)：

```rust
/// 采购入库输入
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct PurchaseInput {
    pub supplier_id: Option<String>,
    pub handler: Option<String>,
    pub items: Vec<PurchaseItemInput>,
    pub remark: Option<String>,
    /// 付款状态：unpaid / partial / paid
    /// 默认为 unpaid
    pub payment_status: Option<String>,
}
```

**预期结果**
- `PurchaseInput` 包含 `payment_status: Option<String>` 字段
- serde `rename_all = "camelCase"` 已存在，会自动映射 `paymentStatus` 字段名

**验证方法**
```powershell
cd d:\AI\AI\开发\茶叶店管理\tea-manage\src-tauri
cargo check
```
预期：无错误。

**回滚方案**
- 删除 `payment_status` 字段
- 还原原结构体

**输出物**
- 修改后的 `PurchaseInput` 结构体

---

### Task-04: 新增 PurchaseOrderListItem 结构体

**任务说明**
为列表查询新增专用结构体 `PurchaseOrderListItem`，包含供应商名称、付款状态、商品数等列表展示字段。

**涉及文件**
- [models/inventory.rs](file:///d:/AI/AI/开发/茶叶店管理/tea-manage/src-tauri/src/models/inventory.rs)

**验证标准**
- [ ] 结构体编译通过
- [ ] 字段与前端 `PurchaseOrderListItem` 类型一致

**依赖任务**
- 无

**对应验收标准**
- AC-04: 列表显示供应商名称
- AC-05: 列表显示付款状态

---

#### 步骤 4.1: 在 models/inventory.rs 末尾新增结构体

**前置条件**
- [ ] Task-03 完成

**执行动作**
在 [models/inventory.rs](file:///d:/AI/AI/开发/茶叶店管理/tea-manage/src-tauri/src/models/inventory.rs) 末尾（约 line 158 之后）新增：

```rust
/// 采购单列表项（包含 JOIN 后的展示字段）
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct PurchaseOrderListItem {
    pub id: String,
    pub order_no: String,
    pub supplier_id: String,
    pub supplier_name: String,
    pub handler: Option<String>,
    pub total_amount: f64,
    pub payment_status: String,
    /// 该采购单包含的商品行数
    pub item_count: i64,
    pub remark: String,
    pub created_at: String,
}
```

**预期结果**
- `PurchaseOrderListItem` 编译通过
- 字段顺序清晰，便于调试

**验证方法**
```powershell
cd d:\AI\AI\开发\茶叶店管理\tea-manage\src-tauri
cargo check
```

**回滚方案**
- 删除新增的结构体

**输出物**
- `PurchaseOrderListItem` 结构体（约 12 行）

---

### Task-05: 重构 purchase_in Command

**任务说明**
重构 `purchase_in` 函数，在原有事务中**追加**主单和明细插入逻辑。

**涉及文件**
- [commands/inventory.rs:260-404](file:///d:/AI/AI/开发/茶叶店管理/tea-manage/src-tauri/src/commands/inventory.rs#L260-L404)

**测试文件**
- `src-tauri/src/commands/inventory.rs`（在 Task-13 中新增 `#[cfg(test)] mod tests`）

**验证标准**
- [ ] 函数体保持原有逻辑（批次/库存/流水）
- [ ] **新增**：插入 `purchase_items` 明细
- [ ] **新增**：插入 `purchase_orders` 主单
- [ ] 失败时回滚（已有 ROLLBACK 逻辑不变）
- [ ] 单元测试通过（Task-13）

**依赖任务**
- Task-01（迁移脚本）
- Task-02（SCHEMA_VERSION 升级）
- Task-03（PurchaseInput）

**对应验收标准**
- AC-01: 创建采购入库单时同时持久化主单和明细
- AC-08: 数据库 schema 升级到 v3

---

#### 步骤 5.1: 改造事务为排他事务

**前置条件**
- [ ] Task-01、Task-02、Task-03 全部完成

**执行动作**
将 [inventory.rs:271](file:///d:/AI/AI/开发/茶叶店管理/tea-manage/src-tauri/src/commands/inventory.rs#L271) 的 `BEGIN TRANSACTION` 改为 `BEGIN EXCLUSIVE TRANSACTION`（与退货单保持一致）：

```rust
// 原代码（line 271）
conn.execute("BEGIN TRANSACTION", [])
    .map_err(|e| e.to_string())?;

// 改为
conn.execute("BEGIN EXCLUSIVE TRANSACTION", [])
    .map_err(|e| e.to_string())?;
```

**预期结果**
- 事务隔离级别提升为 EXCLUSIVE
- 防止并发创建相同 order_no

**验证方法**
```powershell
cd d:\AI\AI\开发\茶叶厂管理\tea-manage\src-tauri
cargo check
```

**回滚方案**
- 还原为 `BEGIN TRANSACTION`

**输出物**
- 修改后的事务开启语句

---

#### 步骤 5.2: 改造 supplier_id 校验逻辑

**前置条件**
- [ ] 步骤 5.1 完成

**执行动作**
在 `purchase_in` 函数体中，遍历 `for item in &input.items` 之前，新增供应商校验：

```rust
// 校验供应商存在且启用
let supplier_id_str = match &input.supplier_id {
    Some(s) if !s.trim().is_empty() => s.clone(),
    _ => {
        conn.execute("ROLLBACK", []).map_err(|e| e.to_string())?;
        return Err("请选择供应商".to_string());
    }
};

let supplier_exists: i64 = conn.query_row(
    "SELECT COUNT(*) FROM suppliers WHERE id = ? AND is_active = 1",
    [&supplier_id_str],
    |row| row.get(0),
).map_err(|e| {
    let _ = conn.execute("ROLLBACK", []);
    format!("查询供应商失败: {}", e)
})?;

if supplier_exists == 0 {
    let _ = conn.execute("ROLLBACK", []).map_err(|e| e.to_string())?;
    return Err("供应商不存在或已停用".to_string());
}
```

并将下面循环内的所有 `input.supplier_id` 引用改为 `supplier_id_str`（统一使用变量）。

**预期结果**
- supplier_id 为 None 或空字符串时返回错误
- supplier_id 不存在或已停用时返回错误
- 错误时正确回滚

**验证方法**
- 运行 cargo check
- 后续 Task-13 单元测试覆盖

**回滚方案**
- 删除新增的 supplier 校验代码块
- 还原 input.supplier_id 引用

**输出物**
- supplier 校验代码块（约 18 行）

---

#### 步骤 5.3: 在循环内增加 purchase_items 插入逻辑

**前置条件**
- [ ] 步骤 5.2 完成

**执行动作**
在 `for item in &input.items` 循环的**末尾**（在 `items.push(...)` 之前），新增明细插入逻辑。

定位位置：[inventory.rs:377-388](file:///d:/AI/AI/开发/茶叶店管理/tea-manage/src-tauri/src/commands/inventory.rs#L377-L388) 之前的 `items.push(...)` 处。

**执行动作**（在该位置**前**插入新代码）：

```rust
// 4.X 插入 purchase_items 明细
let item_id = Uuid::new_v4().to_string();
conn.execute(
    "INSERT INTO purchase_items
        (id, order_id, product_id, product_name, unit_id, unit_name,
         quantity, grams, unit_price, subtotal, batch_id, batch_code, created_at)
     VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)",
    params![
        item_id, order_id, item.product_id, product_name,
        item.unit_id, unit_name,
        item.quantity, grams, item.unit_price, subtotal,
        batch_id, batch_code, now
    ],
).map_err(|e| {
    let _ = conn.execute("ROLLBACK", []);
    format!("插入明细失败: {}", e)
})?;
```

**预期结果**
- 每个明细行都会写入 `purchase_items` 表
- 失败时 ROLLBACK

**验证方法**
- cargo check
- Task-13 单元测试验证

**回滚方案**
- 删除新增的 INSERT purchase_items 代码块

**输出物**
- 循环内的明细插入代码（约 15 行）

---

#### 步骤 5.4: 在循环外增加 purchase_orders 主单插入逻辑

**前置条件**
- [ ] 步骤 5.3 完成

**执行动作**
在 `for item in &input.items` 循环**之后**、`conn.execute("COMMIT", ...)` 之前，新增主单插入逻辑。

定位位置：[inventory.rs:390-392](file:///d:/AI/AI/开发/茶叶店管理/tea-manage/src-tauri/src/commands/inventory.rs#L390-L392) 的 `conn.execute("COMMIT", ...)` 处。

**执行动作**（在该位置**前**插入新代码）：

```rust
// 5. 插入 purchase_orders 主单
let payment_status = input.payment_status.as_deref().unwrap_or("unpaid");
// 校验 payment_status 合法性
if !["unpaid", "partial", "paid"].contains(&payment_status) {
    let _ = conn.execute("ROLLBACK", []).map_err(|e| e.to_string())?;
    return Err(format!("无效的付款状态: {}", payment_status));
}

conn.execute(
    "INSERT INTO purchase_orders
        (id, order_no, supplier_id, handler, total_amount, payment_status, remark, created_at)
     VALUES (?, ?, ?, ?, ?, ?, ?, ?)",
    params![
        order_id, order_no, supplier_id_str, input.handler,
        total_amount, payment_status,
        input.remark.clone().unwrap_or_default(), now
    ],
).map_err(|e| {
    let _ = conn.execute("ROLLBACK", []);
    format!("创建主单失败: {}", e)
})?;
```

**预期结果**
- 主单记录写入 `purchase_orders` 表
- payment_status 默认为 'unpaid'，非法值报错
- 失败时 ROLLBACK

**验证方法**
- cargo check
- Task-13 单元测试验证

**回滚方案**
- 删除新增的 INSERT purchase_orders 代码块

**输出物**
- 主单插入代码（约 20 行）

---

#### 步骤 5.5: 调整 Return 结构以填充 supplier_name

**前置条件**
- [ ] 步骤 5.4 完成

**执行动作**
在 supplier 校验块之后，查询 supplier_name：

```rust
let supplier_name: String = conn.query_row(
    "SELECT name FROM suppliers WHERE id = ?",
    [&supplier_id_str],
    |row| row.get(0),
).unwrap_or_else(|_| "未知供应商".to_string());
```

并将函数返回的 `PurchaseOrder` 结构中的 `supplier_id: input.supplier_id` 改为 `supplier_id: Some(supplier_id_str)`，并新增 `supplier_name` 字段。

**注意**：`PurchaseOrder` 结构目前没有 `supplier_name` 字段，需要在 [models/inventory.rs](file:///d:/AI/AI/开发/茶叶店管理/tea-manage/src-tauri/src/models/inventory.rs) 中扩展：

```rust
/// 采购单据
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct PurchaseOrder {
    pub id: String,
    pub order_no: String,
    pub supplier_id: Option<String>,
    pub supplier_name: String,  // ✨ 新增
    pub handler: Option<String>,
    pub total_amount: f64,
    pub payment_status: String,  // ✨ 新增
    pub remark: Option<String>,
    pub items: Vec<PurchaseOrderItem>,
    pub created_at: String,
}
```

**预期结果**
- PurchaseOrder 扩展后兼容前端 TS 类型
- 旧调用方需同步更新（前端 types/index.ts 在 Task-09 中更新）

**验证方法**
- cargo check
- 编译可能因 PurchaseOrder 字段调整出现错误，需要同步更新调用方

**回滚方案**
- 恢复 PurchaseOrder 原结构
- 删除 supplier_name 查询

**输出物**
- `PurchaseOrder` 扩展后的结构体
- `supplier_name` 查询代码

---

### Task-06: 新增 get_purchase_orders Command

**任务说明**
在 `commands/inventory.rs` 中新增 `get_purchase_orders` 函数，支持分页、按供应商/付款状态/日期范围筛选。

**涉及文件**
- [commands/inventory.rs](file:///d:/AI/AI/开发/茶叶店管理/tea-manage/src-tauri/src/commands/inventory.rs)

**测试文件**
- `src-tauri/src/commands/inventory.rs`（Task-13 中新增）

**验证标准**
- [ ] 函数签名与 TS 类型一致
- [ ] 动态 SQL 构建正确
- [ ] 单元测试通过（Task-13）

**依赖任务**
- Task-04（PurchaseOrderListItem）
- Task-01（数据库迁移）

**对应验收标准**
- AC-02: 采购入库单能按日期范围筛选
- AC-03: 采购入库单能按供应商筛选
- AC-04: 列表显示供应商名称
- AC-05: 列表显示付款状态
- AC-07: 采购入库单分页正常

---

#### 步骤 6.1: 实现 get_purchase_orders 函数

**前置条件**
- [ ] Task-04 完成
- [ ] 已参考 [return_order.rs:285-382](file:///d:/AI/AI/开发/茶叶店管理/tea-manage/src-tauri/src/commands/return_order.rs#L285-L382) 的动态 SQL 模式

**执行动作**
在 `commands/inventory.rs` 末尾（约 line 567 之后）新增：

```rust
/// 获取采购入库单列表（分页 + 筛选：日期/供应商/付款状态）
#[tauri::command]
pub async fn get_purchase_orders(
    db: tauri::State<'_, Database>,
    page: Option<i64>,
    page_size: Option<i64>,
    supplier_id: Option<String>,
    payment_status: Option<String>,
    date_start: Option<String>,  // YYYY-MM-DD
    date_end: Option<String>,    // YYYY-MM-DD
) -> Result<PageResult<PurchaseOrderListItem>, String> {
    let conn = db.get_conn()?;
    let page = page.unwrap_or(1);
    let page_size = page_size.unwrap_or(20);
    let offset = (page - 1) * page_size;

    // 构建动态 SQL
    let mut where_clauses: Vec<String> = Vec::new();
    let mut params_vec: Vec<Box<dyn rusqlite::ToSql>> = Vec::new();

    if let Some(ref sid) = supplier_id {
        if !sid.trim().is_empty() {
            where_clauses.push("po.supplier_id = ?".to_string());
            params_vec.push(Box::new(sid.clone()));
        }
    }
    if let Some(ref ps) = payment_status {
        if !ps.trim().is_empty() {
            where_clauses.push("po.payment_status = ?".to_string());
            params_vec.push(Box::new(ps.clone()));
        }
    }
    if let Some(ref ds) = date_start {
        if !ds.trim().is_empty() {
            where_clauses.push("po.created_at >= ?".to_string());
            params_vec.push(Box::new(ds.clone()));
        }
    }
    if let Some(ref de) = date_end {
        if !de.trim().is_empty() {
            // 截止日期包含当天，加 1 天比较
            where_clauses.push("po.created_at < ?".to_string());
            let next_day = add_one_day(de);
            params_vec.push(Box::new(next_day));
        }
    }

    let where_sql = if where_clauses.is_empty() {
        String::new()
    } else {
        format!("WHERE {}", where_clauses.join(" AND "))
    };

    // 查询总数
    let count_sql = format!(
        "SELECT COUNT(*) FROM purchase_orders po {}",
        where_sql
    );
    let count_params: Vec<&dyn rusqlite::ToSql> =
        params_vec.iter().map(|p| p.as_ref()).collect();
    let total: i64 = conn.query_row(&count_sql, count_params.as_slice(), |row| row.get(0))
        .map_err(|e| e.to_string())?;

    // 查询列表
    let list_sql = format!(
        "SELECT po.id, po.order_no, po.supplier_id, s.name AS supplier_name,
                po.handler, po.total_amount, po.payment_status, po.remark, po.created_at,
                COUNT(pi.id) AS item_count
         FROM purchase_orders po
         LEFT JOIN suppliers s ON s.id = po.supplier_id
         LEFT JOIN purchase_items pi ON pi.order_id = po.id
         {}
         GROUP BY po.id
         ORDER BY po.created_at DESC
         LIMIT ? OFFSET ?",
        where_sql
    );
    params_vec.push(Box::new(page_size));
    params_vec.push(Box::new(offset));
    let list_params: Vec<&dyn rusqlite::ToSql> =
        params_vec.iter().map(|p| p.as_ref()).collect();

    let mut stmt = conn.prepare(&list_sql).map_err(|e| e.to_string())?;
    let orders: Vec<PurchaseOrderListItem> = stmt.query_map(list_params.as_slice(), |row| {
        Ok(PurchaseOrderListItem {
            id: row.get(0)?,
            order_no: row.get(1)?,
            supplier_id: row.get(2)?,
            supplier_name: row.get(3)?,
            handler: row.get(4)?,
            total_amount: row.get(5)?,
            payment_status: row.get(6)?,
            item_count: row.get(7)?,
            remark: row.get(8)?,
            created_at: row.get(9)?,
        })
    }).map_err(|e| e.to_string())?
    .collect::<Result<Vec<_>, _>>()
    .map_err(|e| e.to_string())?;

    Ok(PageResult {
        list: orders,
        total: total as u32,
        page: page as u32,
        page_size: page_size as u32,
    })
}

/// 辅助函数：日期加 1 天（YYYY-MM-DD → YYYY-MM-DD）
fn add_one_day(date_str: &str) -> String {
    // 简单实现：依赖 chrono
    use chrono::NaiveDate;
    match NaiveDate::parse_from_str(date_str, "%Y-%m-%d") {
        Ok(d) => d.succ_opt().map(|nd| nd.format("%Y-%m-%d").to_string()).unwrap_or_else(|| date_str.to_string()),
        Err(_) => date_str.to_string(),
    }
}
```

**预期结果**
- 函数编译通过
- 动态 SQL 正确构建
- 日期范围查询使用 `created_at < next_day` 模式，包含截止日

**验证方法**
```powershell
cd d:\AI\AI\开发\茶叶店管理\tea-manage\src-tauri
cargo check
```

**回滚方案**
- 删除整个 `get_purchase_orders` 和 `add_one_day` 函数

**输出物**
- `get_purchase_orders` 函数（约 90 行）
- `add_one_day` 辅助函数（约 7 行）

---

### Task-07: 新增 get_purchase_order_detail Command

**任务说明**
在 `commands/inventory.rs` 中新增 `get_purchase_order_detail` 函数，查询单条采购单完整信息。

**涉及文件**
- [commands/inventory.rs](file:///d:/AI/AI/开发/茶叶店管理/tea-manage/src-tauri/src/commands/inventory.rs)

**测试文件**
- Task-13 单元测试

**验证标准**
- [ ] 返回 PurchaseOrder（含 items）
- [ ] 错误时返回友好提示

**依赖任务**
- Task-05（PurchaseOrder 扩展）

**对应验收标准**
- AC-06: 采购入库单详情页可访问

---

#### 步骤 7.1: 实现 get_purchase_order_detail 函数

**前置条件**
- [ ] Task-05 完成

**执行动作**
在 `commands/inventory.rs` 末尾新增：

```rust
/// 获取采购入库单详情（含明细）
#[tauri::command]
pub async fn get_purchase_order_detail(
    db: tauri::State<'_, Database>,
    order_id: String,
) -> Result<PurchaseOrder, String> {
    let conn = db.get_conn()?;

    // 查询主单 + 供应商名称
    let (order_no, supplier_id, supplier_name, handler, total_amount, payment_status, remark, created_at):
        (String, String, String, Option<String>, f64, String, String, String) = conn.query_row(
        "SELECT po.order_no, po.supplier_id, s.name, po.handler, po.total_amount,
                po.payment_status, po.remark, po.created_at
         FROM purchase_orders po
         LEFT JOIN suppliers s ON s.id = po.supplier_id
         WHERE po.id = ?",
        [&order_id],
        |row| Ok((
            row.get(0)?, row.get(1)?, row.get(2)?, row.get(3)?,
            row.get(4)?, row.get(5)?, row.get(6)?, row.get(7)?,
        )),
    ).map_err(|e| format!("采购单不存在: {}", e))?;

    // 查询明细
    let mut stmt = conn.prepare(
        "SELECT product_id, product_name, unit_id, unit_name,
                quantity, grams, unit_price, subtotal, batch_id, batch_code
         FROM purchase_items
         WHERE order_id = ?
         ORDER BY created_at ASC"
    ).map_err(|e| e.to_string())?;

    let items: Vec<PurchaseOrderItem> = stmt.query_map(
        [&order_id],
        |row| Ok(PurchaseOrderItem {
            product_id: row.get(0)?,
            product_name: row.get(1)?,
            unit_id: row.get(2)?,
            unit_name: row.get(3)?,
            quantity: row.get(4)?,
            grams: row.get(5)?,
            unit_price: row.get(6)?,
            subtotal: row.get(7)?,
            batch_id: row.get(8)?,
            batch_code: row.get(9)?,
        }),
    ).map_err(|e| e.to_string())?
    .collect::<Result<Vec<_>, _>>()
    .map_err(|e| e.to_string())?;

    Ok(PurchaseOrder {
        id: order_id,
        order_no,
        supplier_id: Some(supplier_id),
        supplier_name,
        handler,
        total_amount,
        payment_status,
        remark: if remark.is_empty() { None } else { Some(remark) },
        items,
        created_at,
    })
}
```

**注意**：`PurchaseOrderItem` 结构当前没有 `unit_id` 和 `batch_id` 字段，需要在 [models/inventory.rs](file:///d:/AI/AI/开发/茶叶店管理/tea-manage/src-tauri/src/models/inventory.rs) 中确认/扩展：

```rust
/// 采购单据明细
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct PurchaseOrderItem {
    pub product_id: String,
    pub product_name: String,
    pub unit_id: String,    // ✨ 新增
    pub unit_name: String,
    pub quantity: i64,
    pub grams: i64,
    pub unit_price: f64,
    pub subtotal: f64,
    pub batch_id: String,   // ✨ 新增
    pub batch_code: String,
}
```

**预期结果**
- 函数编译通过
- 返回完整 PurchaseOrder 结构

**验证方法**
```powershell
cd d:\AI\AI\开发\茶叶店管理\tea-manage\src-tauri
cargo check
```

**回滚方案**
- 删除 `get_purchase_order_detail` 函数
- 还原 PurchaseOrderItem 结构

**输出物**
- `get_purchase_order_detail` 函数（约 50 行）

---

### Task-08: 在 commands/mod.rs 注册新 Commands

**任务说明**
将新 Commands 在 `commands/mod.rs` 中导出（虽然 `pub use inventory::*` 已经包含，但需确认）。

**涉及文件**
- [commands/mod.rs](file:///d:/AI/AI/开发/茶叶店管理/tea-manage/src-tauri/src/commands/mod.rs)

**验证标准**
- [ ] 所有新函数通过 `pub use inventory::*` 自动导出
- [ ] cargo build 全部通过

**依赖任务**
- Task-05, 06, 07

**对应验收标准**
- 无（基础设施任务）

---

#### 步骤 8.1: 验证 mod.rs 已正确导出

**前置条件**
- [ ] Task-05, 06, 07 完成

**执行动作**
无需修改（`pub use inventory::*` 已自动导出 inventory 模块所有 pub 函数）。仅需验证：

```powershell
cd d:\AI\AI\开发\茶叶店管理\tea-manage\src-tauri
cargo build
```

**预期结果**
- 编译通过
- 所有新 Commands 可被 Tauri 调用

**验证方法**
- 启动应用，前端调用 `invoke('get_purchase_orders', ...)` 不报"command not found"

**回滚方案**
- 无

**输出物**
- 无（验证任务）

---

### Task-09: 新增 src/api/purchases.ts

**任务说明**
在前端 `src/api/` 目录下新增 `purchases.ts`，封装 Tauri Commands。

**涉及文件**
- [src/api/purchases.ts](file:///d:/AI/AI/开发/茶叶店管理/tea-manage/src/api/purchases.ts)（新建）

**验证标准**
- [ ] TypeScript 编译通过
- [ ] 函数签名与后端一致

**依赖任务**
- Task-05, 06, 07

**对应验收标准**
- AC-01 ~ AC-07（前端调用层）

---

#### 步骤 9.1: 创建 purchases.ts 文件

**前置条件**
- [ ] Task-05, 06, 07 完成
- [ ] 已参考 [src/api/returnOrders.ts](file:///d:/AI/AI/开发/茶叶店管理/tea-manage/src/api/returnOrders.ts) 现有 API 风格

**执行动作**
创建文件 `tea-manage/src/api/purchases.ts`：

```typescript
/**
 * @file 采购入库单 API
 * @description Tauri Commands 封装 - 采购单列表/详情
 * @since v0.3.0
 */
import { invoke } from '@tauri-apps/api/core'
import type { PageResult, PurchaseOrder, PurchaseOrderListItem } from '@/types'

/**
 * 获取采购入库单列表（分页 + 筛选）
 * @param page 页码（从 1 开始）
 * @param pageSize 每页条数
 * @param supplierId 供应商 ID（可选）
 * @param paymentStatus 付款状态（可选）：unpaid / partial / paid
 * @param dateStart 起始日期 YYYY-MM-DD（含）
 * @param dateEnd 截止日期 YYYY-MM-DD（含）
 */
export async function getPurchaseOrders(
    page?: number,
    pageSize?: number,
    supplierId?: string,
    paymentStatus?: string,
    dateStart?: string,
    dateEnd?: string
): Promise<PageResult<PurchaseOrderListItem>> {
    return await invoke('get_purchase_orders', {
        page: page || 1,
        page_size: pageSize || 20,
        supplier_id: supplierId || null,
        payment_status: paymentStatus || null,
        date_start: dateStart || null,
        date_end: dateEnd || null
    })
}

/**
 * 获取采购单详情
 */
export async function getPurchaseOrderDetail(orderId: string): Promise<PurchaseOrder> {
    return await invoke('get_purchase_order_detail', { orderId })
}
```

**预期结果**
- TypeScript 编译通过
- 函数可被前端组件调用

**验证方法**
```powershell
cd d:\AI\AI\开发\茶叶店管理\tea-manage
npx vue-tsc --noEmit
```
预期：无错误。

**回滚方案**
- 删除 `purchases.ts`

**输出物**
- `src/api/purchases.ts`（约 35 行）

---

#### 步骤 9.2: 同步更新前端 types/index.ts

**前置条件**
- [ ] Task-09 步骤 9.1 完成

**执行动作**
修改 [src/types/index.ts](file:///d:/AI/AI/开发/茶叶店管理/tea-manage/src/types/index.ts)：

```typescript
// 修改 PurchaseInput（line 170-175）
export interface PurchaseInput {
    supplierId?: string
    handler?: string
    items: PurchaseItemInput[]
    remark?: string
    paymentStatus?: 'unpaid' | 'partial' | 'paid'  // ✨ 新增
}

// 修改 PurchaseOrder（line 186-195）
export interface PurchaseOrder {
    id: string
    orderNo: string
    supplierId: string | null
    supplierName: string          // ✨ 新增
    handler: string | null
    totalAmount: number
    paymentStatus: string         // ✨ 新增
    remark: string | null
    items: PurchaseOrderItem[]
    createdAt: string
}

// 修改 PurchaseOrderItem（line 198-208）
export interface PurchaseOrderItem {
    productId: string
    productName: string
    unitId: string                // ✨ 新增
    unitName: string
    quantity: number
    grams: number
    unitPrice: number
    subtotal: number
    batchId: string               // ✨ 新增
    batchCode: string
}

// 新增 PurchaseOrderListItem（在 PurchaseOrder 之后）
/** 采购单列表项 */
export interface PurchaseOrderListItem {
    id: string
    orderNo: string
    supplierId: string
    supplierName: string
    handler: string | null
    totalAmount: number
    paymentStatus: string
    itemCount: number
    remark: string
    createdAt: string
}
```

**预期结果**
- types/index.ts 编译通过
- 旧调用方需要同步更新（如 PurchaseForm.vue）

**验证方法**
```powershell
cd d:\AI\AI\开发\茶叶店管理\tea-manage
npx vue-tsc --noEmit
```

**回滚方案**
- 还原 types/index.ts 原始内容

**输出物**
- 修改后的 types/index.ts（增 4 处）

---

#### 步骤 9.3: 更新 PurchaseForm.vue 适配新字段

**前置条件**
- [ ] Task-09 步骤 9.2 完成

**执行动作**
检查并更新 [src/views/purchase/PurchaseForm.vue](file:///d:/AI/AI/开发/茶叶店管理/tea-manage/src/views/purchase/PurchaseForm.vue)：
- 如有调用 `purchaseIn` 传 `supplierId` 等字段，确保新增 `paymentStatus` 字段
- 提交时增加 `paymentStatus: 'unpaid' | 'partial' | 'paid'`

**预期结果**
- 提交采购单时带上 `paymentStatus` 字段
- TypeScript 编译通过

**验证方法**
- 启动应用
- 创建一笔采购单
- 检查数据库 `purchase_orders.payment_status` 字段值

**回滚方案**
- 还原 PurchaseForm.vue

**输出物**
- 修改后的 PurchaseForm.vue（如有改动）

---

### Task-10: 新增 PurchaseDetail.vue

**任务说明**
创建采购单详情页，展示完整单据信息和商品明细。

**涉及文件**
- [src/views/purchase/PurchaseDetail.vue](file:///d:/AI/AI/开发/茶叶店管理/tea-manage/src/views/purchase/PurchaseDetail.vue)（新建）

**验证标准**
- [ ] 页面正确加载详情
- [ ] 显示基础信息和明细表
- [ ] 错误状态友好

**依赖任务**
- Task-09（purchases.ts API）

**对应验收标准**
- AC-06: 采购入库单详情页可访问

---

#### 步骤 10.1: 创建 PurchaseDetail.vue

**前置条件**
- [ ] Task-09 完成
- [ ] 已参考 [ReturnDetail.vue](file:///d:/AI/AI/开发/茶叶店管理/tea-manage/src/views/returns/ReturnDetail.vue)

**执行动作**
创建文件 `tea-manage/src/views/purchase/PurchaseDetail.vue`：

```vue
<!--
  @file 采购入库单详情页面
  @description 查看采购单完整信息和商品明细
  @since v0.3.0
-->
<template>
    <div class="p-6">
        <div class="flex-between mb-4">
            <h1 class="text-xl font-bold">采购单详情</h1>
            <n-button @click="$router.back()">返回</n-button>
        </div>

        <n-card v-if="loading" class="text-center py-8">
            <n-spin />
        </n-card>

        <template v-else-if="order">
            <!-- 顶部信息 -->
            <n-card class="mb-4">
                <n-descriptions :column="3" bordered>
                    <n-descriptions-item label="采购单号">{{ order.orderNo }}</n-descriptions-item>
                    <n-descriptions-item label="供应商">{{ order.supplierName }}</n-descriptions-item>
                    <n-descriptions-item label="经手人">{{ order.handler || '-' }}</n-descriptions-item>
                    <n-descriptions-item label="创建时间">{{ order.createdAt }}</n-descriptions-item>
                    <n-descriptions-item label="付款状态">
                        <n-tag :type="getPaymentColor(order.paymentStatus) as any" size="small" round>
                            {{ getPaymentLabel(order.paymentStatus) }}
                        </n-tag>
                    </n-descriptions-item>
                    <n-descriptions-item label="总金额">
                        <span class="text-lg font-bold text-orange-600">
                            ¥{{ order.totalAmount.toFixed(2) }}
                        </span>
                    </n-descriptions-item>
                    <n-descriptions-item label="备注" :span="3">
                        {{ order.remark || '-' }}
                    </n-descriptions-item>
                </n-descriptions>
            </n-card>

            <!-- 明细 -->
            <n-card title="商品明细" class="mb-4">
                <n-data-table
                    :columns="itemColumns"
                    :data="order.items"
                    :bordered="false"
                />
            </n-card>
        </template>

        <n-empty v-else description="采购单不存在" />
    </div>
</template>

<script setup lang="ts">
/**
 * 采购单详情逻辑
 * - 加载单据信息
 * - 展示明细表格
 */
import { ref, onMounted, computed } from 'vue'
import { useRoute } from 'vue-router'
import { NCard, NDescriptions, NDescriptionsItem, NDataTable, NTag, NSpin, NEmpty, NButton, useMessage } from 'naive-ui'
import type { DataTableColumns } from 'naive-ui'
import type { PurchaseOrder, PurchaseOrderItem } from '@/types'
import { getPurchaseOrderDetail } from '@/api/purchases'

const route = useRoute()
const message = useMessage()

const loading = ref(false)
const order = ref<PurchaseOrder | null>(null)

const orderId = computed(() => route.params.id as string)

async function loadDetail() {
    loading.value = true
    try {
        order.value = await getPurchaseOrderDetail(orderId.value)
    } catch (e: any) {
        message.error(`加载采购单失败: ${e}`)
    } finally {
        loading.value = false
    }
}

function getPaymentLabel(status: string): string {
    switch (status) {
        case 'paid': return '已付款'
        case 'partial': return '部分付款'
        case 'unpaid': return '未付款'
        default: return status
    }
}

function getPaymentColor(status: string): string {
    switch (status) {
        case 'paid': return 'success'
        case 'partial': return 'warning'
        case 'unpaid': return 'error'
        default: return 'default'
    }
}

const itemColumns = computed<DataTableColumns<PurchaseOrderItem>>(() => [
    { title: '序号', key: 'index', width: 60, render: (_row, index) => `${index + 1}` },
    { title: '商品名称', key: 'productName', width: 180 },
    { title: '销售单位', key: 'unitName', width: 80 },
    { title: '数量', key: 'quantity', width: 80 },
    { title: '实际克数', key: 'grams', width: 100, render: (row) => `${row.grams}g` },
    { title: '进价', key: 'unitPrice', width: 100, render: (row) => `¥${row.unitPrice.toFixed(2)}` },
    { title: '小计', key: 'subtotal', width: 120, render: (row) => `¥${row.subtotal.toFixed(2)}` },
    { title: '批次号', key: 'batchCode', width: 160 }
])

onMounted(() => {
    loadDetail()
})
</script>

<style scoped>
.flex-between {
    display: flex;
    justify-content: space-between;
    align-items: center;
}
.mb-4 {
    margin-bottom: 16px;
}
.text-center {
    text-align: center;
}
.py-8 {
    padding: 32px 0;
}
.text-lg {
    font-size: 18px;
}
.font-bold {
    font-weight: 700;
}
.text-orange-600 {
    color: #ea580c;
}
</style>
```

**预期结果**
- 页面正确加载采购单详情
- 表格、商品明细、付款状态标签正常显示
- 错误状态（订单不存在）友好

**验证方法**
```powershell
cd d:\AI\AI\开发\茶叶店管理\tea-manage
npx vue-tsc --noEmit
```

**回滚方案**
- 删除 `PurchaseDetail.vue`

**输出物**
- `src/views/purchase/PurchaseDetail.vue`（约 140 行）

---

### Task-11: 重构 PurchaseList.vue

**任务说明**
重写 `PurchaseList.vue`，调用真实 API 加载列表，支持筛选和分页。

**涉及文件**
- [src/views/purchase/PurchaseList.vue](file:///d:/AI/AI/开发/茶叶店管理/tea-manage/src/views/purchase/PurchaseList.vue)（重写）

**验证标准**
- [ ] 调用 `getPurchaseOrders` API
- [ ] 显示日期/供应商/付款状态筛选器
- [ ] 表格列包含供应商名称、付款状态、金额、操作
- [ ] 分页器正常工作

**依赖任务**
- Task-09（API）
- Task-10（Detail 页面）

**对应验收标准**
- AC-02 ~ AC-05（列表展示）
- AC-07（分页）

---

#### 步骤 11.1: 重写 PurchaseList.vue

**前置条件**
- [ ] Task-09、Task-10 完成
- [ ] 已参考 [ReturnList.vue](file:///d:/AI/AI/开发/茶叶店管理/tea-manage/src/views/returns/ReturnList.vue)

**执行动作**
完整重写 `PurchaseList.vue`：

```vue
<!--
  @file 采购入库单列表
  @description 采购入库单查询和管理，支持日期/供应商/付款状态筛选
  @since v0.3.0
-->
<template>
    <div class="p-6">
        <div class="flex-between mb-4">
            <h1 class="text-xl font-bold">采购入库单</h1>
            <n-button type="primary" @click="$router.push('/purchase/new')">
                <template #icon>
                    <i class="i-ic-add"></i>
                </template>
                新增入库单
            </n-button>
        </div>

        <!-- 筛选区 -->
        <n-card class="mb-4">
            <n-form inline :model="filters" @submit.prevent="handleSearch">
                <n-form-item label="入库日期">
                    <n-date-picker
                        v-model:value="filters.dateRange"
                        type="daterange"
                        clearable
                        style="width: 240px"
                    />
                </n-form-item>
                <n-form-item label="供应商">
                    <n-select
                        v-model:value="filters.supplierId"
                        :options="supplierOptions"
                        filterable
                        clearable
                        placeholder="全部供应商"
                        style="width: 200px"
                    />
                </n-form-item>
                <n-form-item label="付款状态">
                    <n-select
                        v-model:value="filters.paymentStatus"
                        :options="paymentOptions"
                        clearable
                        placeholder="全部状态"
                        style="width: 140px"
                    />
                </n-form-item>
                <n-form-item>
                    <n-button type="primary" @click="handleSearch">查询</n-button>
                    <n-button class="ml-2" @click="handleReset">重置</n-button>
                </n-form-item>
            </n-form>
        </n-card>

        <!-- 列表 -->
        <n-card>
            <n-data-table
                :columns="columns"
                :data="orders"
                :loading="loading"
                :pagination="pagination"
                :bordered="false"
                :single-line="false"
                @update:page="handlePageChange"
            />
        </n-card>
    </div>
</template>

<script setup lang="ts">
/**
 * 采购单列表逻辑
 * - 按日期/供应商/付款状态筛选
 * - 跳转到详情
 */
import { ref, reactive, onMounted, h, computed } from 'vue'
import { useRouter } from 'vue-router'
import { NButton, NTag, NDatePicker, useMessage } from 'naive-ui'
import type { DataTableColumns } from 'naive-ui'
import type { PurchaseOrderListItem } from '@/types'
import { getPurchaseOrders } from '@/api/purchases'
import { useSupplierStore } from '@/stores'

const router = useRouter()
const message = useMessage()
const supplierStore = useSupplierStore()

const loading = ref(false)
const orders = ref<PurchaseOrderListItem[]>([])

const filters = reactive({
    /** 入库日期范围 [start, end]，时间戳（毫秒） */
    dateRange: null as [number, number] | null,
    supplierId: null as string | null,
    paymentStatus: null as string | null
})

const pagination = reactive({
    page: 1,
    pageSize: 20,
    total: 0,
    showSizePicker: true,
    pageSizes: [10, 20, 50, 100]
})

const supplierOptions = computed(() =>
    supplierStore.suppliers.map(s => ({ label: s.name, value: s.id }))
)

const paymentOptions = [
    { label: '未付款', value: 'unpaid' },
    { label: '部分付款', value: 'partial' },
    { label: '已付款', value: 'paid' }
]

/** 将时间戳（毫秒）转换为 YYYY-MM-DD 字符串 */
function formatDate(ts: number): string {
    const d = new Date(ts)
    const y = d.getFullYear()
    const m = String(d.getMonth() + 1).padStart(2, '0')
    const day = String(d.getDate()).padStart(2, '0')
    return `${y}-${m}-${day}`
}

/** 加载采购单列表 */
async function loadList() {
    loading.value = true
    try {
        // 解析日期范围
        let dateStart: string | undefined
        let dateEnd: string | undefined
        if (filters.dateRange && filters.dateRange.length === 2) {
            dateStart = formatDate(filters.dateRange[0])
            dateEnd = formatDate(filters.dateRange[1])
        }

        const result = await getPurchaseOrders(
            pagination.page, pagination.pageSize,
            filters.supplierId || undefined,
            filters.paymentStatus || undefined,
            dateStart,
            dateEnd
        )
        orders.value = result.list
        pagination.total = result.total
    } catch (e: any) {
        message.error(`加载采购单失败: ${e}`)
    } finally {
        loading.value = false
    }
}

function handleSearch() {
    pagination.page = 1
    loadList()
}

function handleReset() {
    filters.dateRange = null
    filters.supplierId = null
    filters.paymentStatus = null
    pagination.page = 1
    loadList()
}

function handlePageChange(page: number) {
    pagination.page = page
    loadList()
}

/** 跳转到详情 */
function goDetail(id: string) {
    router.push(`/purchase/${id}`)
}

function getPaymentColor(status: string): string {
    switch (status) {
        case 'paid': return 'success'
        case 'partial': return 'warning'
        case 'unpaid': return 'error'
        default: return 'default'
    }
}

function getPaymentLabel(status: string): string {
    switch (status) {
        case 'paid': return '已付款'
        case 'partial': return '部分付款'
        case 'unpaid': return '未付款'
        default: return status
    }
}

// ========== 表格列 ==========
const columns = computed<DataTableColumns<PurchaseOrderListItem>>(() => [
    { title: '单号', key: 'orderNo', width: 180, resizable: true },
    { title: '日期', key: 'createdAt', width: 160 },
    { title: '供应商', key: 'supplierName', width: 150 },
    { title: '经手人', key: 'handler', width: 100, render: (row) => row.handler || '-' },
    {
        title: '付款状态',
        key: 'paymentStatus',
        width: 100,
        render: (row) => h(
            NTag, { type: getPaymentColor(row.paymentStatus) as any, size: 'small', round: true },
            { default: () => getPaymentLabel(row.paymentStatus) }
        )
    },
    { title: '商品数', key: 'itemCount', width: 80, render: (row) => `${row.itemCount} 项` },
    {
        title: '总金额',
        key: 'totalAmount',
        width: 120,
        render: (row) => `¥${row.totalAmount.toFixed(2)}`
    },
    {
        title: '操作',
        key: 'actions',
        width: 100,
        fixed: 'right',
        render: (row) => h(
            NButton,
            { text: true, type: 'primary', size: 'small', onClick: () => goDetail(row.id) },
            { default: () => '详情' }
        )
    }
])

onMounted(async () => {
    // 确保供应商列表已加载
    if (supplierStore.suppliers.length === 0) {
        await supplierStore.loadSuppliers(1, 100)
    }
    loadList()
})
</script>

<style scoped>
.flex-between {
    display: flex;
    justify-content: space-between;
    align-items: center;
}
.ml-2 {
    margin-left: 8px;
}
</style>
```

**预期结果**
- 列表调用真实 API
- 筛选器、分页器正常工作
- 付款状态有颜色标签
- 详情按钮跳转正确

**验证方法**
- 启动应用
- 进入采购入库单列表
- 验证：列表能加载、筛选能工作、点击详情能跳转

**回滚方案**
- 还原原占位 PurchaseList.vue

**输出物**
- 重写后的 PurchaseList.vue（约 230 行）

---

### Task-12: 更新 router/index.ts（detail 路由）

**任务说明**
将 `/purchase/:id` 路由指向新建的 `PurchaseDetail.vue` 组件。

**涉及文件**
- [src/router/index.ts](file:///d:/AI/AI/开发/茶叶店管理/tea-manage/src/router/index.ts#L74-L78)

**验证标准**
- [ ] 路由正确指向 PurchaseDetail
- [ ] 页面标题正确

**依赖任务**
- Task-10（PurchaseDetail.vue）

**对应验收标准**
- AC-06: 详情页可访问

---

#### 步骤 12.1: 修改 /purchase/:id 路由

**前置条件**
- [ ] Task-10 完成

**执行动作**
修改 [src/router/index.ts:74-78](file:///d:/AI/AI/开发/茶叶店管理/tea-manage/src/router/index.ts#L74-L78)：

```typescript
// 原代码
{
    path: '/purchase/:id',
    name: 'PurchaseDetail',
    component: PurchaseForm,  // ✨ 改为 PurchaseDetail
    meta: { title: '入库单详情' }
},

// 改为
{
    path: '/purchase/:id',
    name: 'PurchaseDetail',
    component: () => import('@/views/purchase/PurchaseDetail.vue'),
    meta: { title: '采购单详情' }
}
```

**预期结果**
- `/purchase/:id` 路由指向 PurchaseDetail
- 标题为"采购单详情"

**验证方法**
- 启动应用
- 从列表点击"详情"按钮
- 验证：进入详情页，显示采购单信息

**回滚方案**
- 还原原路由配置

**输出物**
- 修改后的 router/index.ts

---

### Task-13: 编写单元测试（5 个测试用例）

**任务说明**
在 `commands/inventory.rs` 末尾新增 `#[cfg(test)] mod tests`，覆盖 `purchase_in` 和 `get_purchase_orders`。

**涉及文件**
- [commands/inventory.rs](file:///d:/AI/AI/开发/茶叶店管理/tea-manage/src-tauri/src/commands/inventory.rs)
- [db/mod.rs](file:///d:/AI/AI/开发/茶叶店管理/tea-manage/src-tauri/src/db/mod.rs)（新增 `new_for_test`）

**测试文件**
- 同上（Rust 单元测试惯例）

**实施要点**
- Tauri State 注入复杂，测试中采用**重构后 impl 函数**方案：
  - `purchase_in_impl(&Connection, PurchaseInput) -> Result<PurchaseOrder, String>`
  - `query_purchase_orders(&Connection, ...) -> Result<PageResult<...>, String>`
  - `query_purchase_order_detail(&Connection, String) -> Result<PurchaseOrder, String>`
  - Tauri Command 函数委托给上述 impl
- 新增 `Database::new_for_test(Connection)` 构造函数，支持传入 in-memory 连接
- 使用 `Connection::open_in_memory()` + `run_migrations` 创建测试数据库

**发现的 3 个生产 Bug（已在 Task-13 修复）**
- 🐛 **Bug 1（FK 约束）**：`purchase_items` 在 `purchase_orders` 之前插入，违反外键约束。修复：调整插入顺序。
- 🐛 **Bug 2（order_no 冲突）**：单号生成只用毫秒（0-999），同毫秒多次入库会冲突。修复：增加 4 位随机后缀。
- 🐛 **Bug 3（列序错位）**：`get_purchase_orders` 的 SQL 列顺序与 `row.get(N)` 索引不一致，会导致错位读取。修复：调整 SQL 列顺序与代码一致。

**验证标准**
- [x] 所有测试通过（5/5）
- [x] 测试覆盖：成功路径、回滚路径、筛选场景、详情查询、日期范围

**依赖任务**
- Task-05, 06, 07

**对应验收标准**
- AC-09: 单元测试覆盖核心逻辑

---

#### 步骤 13.1: 添加单元测试模块

**前置条件**
- [x] Task-05, 06, 07 完成

**执行动作**
已在 [commands/inventory.rs:853-1280](file:///d:/AI/AI/开发/茶叶店管理/tea-manage/src-tauri/src/commands/inventory.rs#L853-L1280) 末尾新增 5 个测试：

```rust
#[cfg(test)]
mod tests {
    /// 测试 1: 成功路径（主单+明细+批次+库存+流水全部写入）
    fn test_purchase_in_success();
    
    /// 测试 2: 无效供应商回滚（不存在/已停用）
    fn test_purchase_in_rollback_on_invalid_supplier();
    
    /// 测试 3: 按供应商/付款状态筛选
    fn test_query_purchase_orders_filter_by_supplier();
    
    /// 测试 4: 日期范围筛选（含当天）
    fn test_query_purchase_orders_date_range();
    
    /// 测试 5: 详情查询（含明细）
    fn test_query_purchase_order_detail();
}
```

**预期结果**
- ✅ `cargo test --lib` 全部通过（5/5）

**验证方法**
```powershell
cd d:\AI\AI\开发\茶叶店管理\tea-manage\src-tauri
cargo test --lib
```

**回滚方案**
- 删除整个 `mod tests`

**输出物**
- `commands/inventory.rs` 末尾的 `mod tests`（约 420 行，含 5 个测试函数）

---

### Task-14: 更新版本号（强制）

**任务说明**
将版本号从 v0.2.0 升级到 v0.3.0，确保所有位置一致。

**涉及文件**
- [package.json](file:///d:/AI/AI/开发/茶叶店管理/tea-manage/package.json)
- [Cargo.toml](file:///d:/AI/AI/开发/茶叶店管理/tea-manage/src-tauri/Cargo.toml)
- [project_config.md](file:///d:/AI/AI/开发/茶叶店管理/specs/project_config.md)（如配置了 APP_VERSION）

**验证标准**
- [ ] package.json version = "0.3.0"
- [ ] Cargo.toml version = "0.3.0"
- [ ] project_config.md APP_VERSION 一致

**依赖任务**
- Task-15 之前

**对应验收标准**
- 无（版本控制强制要求）

---

#### 步骤 14.1: 更新 package.json

**前置条件**
- [ ] 所有代码修改完成

**执行动作**
修改 [package.json](file:///d:/AI/AI/开发/茶叶店管理/tea-manage/package.json) 的 `version` 字段：

```json
{
  "name": "tea-manage",
  "version": "0.3.0",  // 原 0.2.0 → 0.3.0
  ...
}
```

**预期结果**
- package.json version = "0.3.0"

**验证方法**
- 查看文件确认

**回滚方案**
- 还原为 0.2.0

**输出物**
- 修改后的 package.json

---

#### 步骤 14.2: 更新 Cargo.toml

**前置条件**
- [ ] 步骤 14.1 完成

**执行动作**
修改 [src-tauri/Cargo.toml](file:///d:/AI/AI/开发/茶叶店管理/tea-manage/src-tauri/Cargo.toml)：

```toml
[package]
name = "tea-manage"
version = "0.3.0"  # 原 0.2.0 → 0.3.0
...
```

**预期结果**
- Cargo.toml version = "0.3.0"

**验证方法**
- 查看文件确认

**回滚方案**
- 还原为 0.2.0

**输出物**
- 修改后的 Cargo.toml

---

#### 步骤 14.3: 更新 project_config.md（如有 APP_VERSION 字段）

**前置条件**
- [ ] 步骤 14.2 完成

**执行动作**
检查 [specs/project_config.md](file:///d:/AI/AI/开发/茶叶店管理/specs/project_config.md)，如包含 APP_VERSION 字段则修改为 `0.3.0`。

**预期结果**
- project_config.md 版本号与 package.json/Cargo.toml 一致

**验证方法**
- 文件内搜索 "0.2.0" 全部替换为 "0.3.0"

**回滚方案**
- 还原为 0.2.0

**输出物**
- 修改后的 project_config.md

---

### Task-15: 更新 changelog.json 和 release 文档（强制）

**任务说明**
为 v0.3.0 添加 changelog 条目，并创建 [specs/releases/v0.3.0.md](file:///d:/AI/AI/开发/茶叶店管理/specs/releases/v0.3.0.md) 发布文档。

**涉及文件**
- [src/data/changelog.json](file:///d:/AI/AI/开发/茶叶店管理/tea-manage/src/data/changelog.json)（如存在）
- [specs/releases/v0.3.0.md](file:///d:/AI/AI/开发/茶叶店管理/specs/releases/v0.3.0.md)（新建）

**验证标准**
- [ ] changelog.json 包含 v0.3.0 条目
- [ ] v0.3.0.md 创建并包含完整变更说明

**依赖任务**
- Task-14

**对应验收标准**
- 无（版本控制强制要求）

---

#### 步骤 15.1: 在 changelog.json 添加 v0.3.0 条目

**前置条件**
- [ ] Task-14 完成

**执行动作**
检查 [src/data/changelog.json](file:///d:/AI/AI/开发/茶叶店管理/tea-manage/src/data/changelog.json) 是否存在，如存在则新增：

```json
[
  ...,
  {
    "version": "0.3.0",
    "date": "2026-07-02",
    "type": "minor",
    "title": "采购入库单持久化 + 列表 + 详情",
    "changes": [
      "fix: 采购入库单正确持久化 purchase_orders + purchase_items 主单和明细（v0.2.0 时期主单未保存）",
      "feat: 采购入库单列表查询，支持日期范围/供应商/付款状态筛选",
      "feat: 采购入库单详情页 /purchase/:id",
      "feat: 数据库 schema 升级到 v3，新增 purchase_orders 和 purchase_items 表"
    ]
  }
]
```

**预期结果**
- changelog.json 包含 v0.3.0 条目
- JSON 格式正确

**验证方法**
- 解析 JSON 无错误

**回滚方案**
- 删除新增的条目

**输出物**
- 修改后的 changelog.json

---

#### 步骤 15.2: 创建 specs/releases/v0.3.0.md

**前置条件**
- [ ] 步骤 15.1 完成

**执行动作**
创建文件 `specs/releases/v0.3.0.md`：

```markdown
# v0.3.0 发布文档

> **版本号**：v0.3.0
> **发布日期**：2026-07-02
> **版本类型**：minor（数据库 schema 升级 + P0 修复）

---

## 一、版本基本信息

| 项目 | 内容 |
|------|------|
| 版本号 | v0.3.0 |
| 上一版本 | v0.2.0 |
| 发布日期 | 2026-07-02 |
| 计划发布时间 | - |
| 实际发布时间 | - |
| 数据库 SCHEMA_VERSION | 2 → 3 |

---

## 二、变更摘要

### 2.1 修复（Bug Fixes）

| 编号 | 描述 | 影响 |
|------|------|------|
| AC-P-02 | 采购入库单无法查询历史记录 | 阻塞数据闭环 |
| AC-P-03 | 采购入库单无法按供应商筛选 | 阻塞供应商对账 |

### 2.2 新增（Features）

| 编号 | 描述 |
|------|------|
| M04-PO-01 | 采购入库单正确持久化主单和明细 |
| M04-PO-02 | 采购入库单列表 API + 前端 UI |
| M04-PO-03 | 采购入库单详情页 |

### 2.3 数据库变更

- 新增表 `purchase_orders`（采购主单）
- 新增表 `purchase_items`（采购明细）
- 新增 6 个索引
- SCHEMA_VERSION 升级：2 → 3

---

## 三、迁移指南

### 3.1 数据迁移

v0.3.0 自动执行 `migrate_v3_purchase_orders` 迁移：

1. 创建 `purchase_orders` 表（含 4 个索引）
2. 创建 `purchase_items` 表（含 1 个索引）
3. 设置 `user_version = 3`

**重要**：v0.2.0 时期的采购入库数据**不会自动回填**到 `purchase_orders` 表，原因是缺乏完整的主单信息（handler、payment_status、remark）。

### 3.2 兼容性

- v0.2.0 用户的现有数据（products / inventory_batches / stock_flow / sales_* / members / suppliers / return_*）完全保留
- 前端 API 兼容：`purchase_in` 入参新增 `paymentStatus` 可选字段（默认为 'unpaid'）
- 老数据（v0.2.0 时期的批次/流水）可继续使用，不受影响

---

## 四、修改文件清单

| 文件 | 改动 |
|------|------|
| src-tauri/src/db/schema.rs | 新增 migrate_v3_purchase_orders + 升级 SCHEMA_VERSION |
| src-tauri/src/models/inventory.rs | PurchaseInput 增 paymentStatus 字段 + 新增 PurchaseOrderListItem + PurchaseOrder 扩展 |
| src-tauri/src/commands/inventory.rs | 重构 purchase_in + 新增 get_purchase_orders + get_purchase_order_detail |
| src/types/index.ts | 类型同步 |
| src/api/purchases.ts | 新增（封装 Tauri Commands） |
| src/views/purchase/PurchaseDetail.vue | 新增 |
| src/views/purchase/PurchaseList.vue | 重写（占位 → 真实列表） |
| src/views/purchase/PurchaseForm.vue | 同步 paymentStatus 字段 |
| src/router/index.ts | /purchase/:id 指向 PurchaseDetail |
| package.json | version: 0.2.0 → 0.3.0 |
| src-tauri/Cargo.toml | version: 0.2.0 → 0.3.0 |

---

## 五、验证清单

- [ ] AC-01: 创建采购单时持久化主单和明细
- [ ] AC-02: 按日期范围筛选
- [ ] AC-03: 按供应商筛选
- [ ] AC-04: 列表显示供应商名称
- [ ] AC-05: 列表显示付款状态
- [ ] AC-06: 详情页可访问
- [ ] AC-07: 分页正常
- [ ] AC-08: 数据库 schema 升级到 v3
- [ ] AC-09: 单元测试覆盖

---

## 六、风险与回滚

| 风险 | 应对 |
|------|------|
| purchase_in 函数变长 | 函数内部分段，加注释 |
| 单元测试中数据库准备复杂 | 简化方案：先做集成测试 |
| v0.2.0 用户数据迁移 | 幂等迁移，老数据保留 |
| 详情页加载慢 | 索引已建，单条查询 < 10ms |

**回滚方案**：如需回滚到 v0.2.0：
1. 卸载 v0.3.0 应用
2. 安装 v0.2.0 安装包
3. 数据库自动从 v3 回滚到 v2（需手动执行迁移脚本）
4. v0.3.0 新增的采购单数据**保留**（不删除），但前端不会显示

---

## 七、下一版本

进入 v0.3.1 规划：储值余额功能（任务组 B）。
```

**预期结果**
- 发布文档创建完成
- 包含完整的变更说明、迁移指南、回滚方案

**验证方法**
- 文件存在
- 内容符合模板

**回滚方案**
- 删除 v0.3.0.md（如未发布）

**输出物**
- `specs/releases/v0.3.0.md`（约 100 行）

---

## 四、依赖关系图

```
Task-01 (迁移脚本)
  ↓
Task-02 (升级 SCHEMA_VERSION)
  ↓
Task-03 (扩展 PurchaseInput) ──→ Task-04 (新增 ListItem) ──→ Task-05 (重构 purchase_in)
                                                                  ↓
                                                            Task-06 (get_purchase_orders)
                                                                  ↓
                                                            Task-07 (get_purchase_order_detail)
                                                                  ↓
                                                            Task-08 (验证 mod.rs 导出)
                                                                  ↓
                                                            Task-09 (前端 API)
                                                                  ↓
                                                            Task-10 (PurchaseDetail.vue)
                                                                  ↓
                                                            Task-11 (PurchaseList.vue 重构)
                                                                  ↓
                                                            Task-12 (路由更新)
                                                                  ↓
                                                            Task-13 (单元测试)
                                                                  ↓
                                                            Task-14 (版本号更新)
                                                                  ↓
                                                            Task-15 (changelog + release 文档)
```

---

## 五、验收标准映射

| AC 编号 | 描述 | 对应任务 |
|---------|------|----------|
| AC-01 | 创建采购单时持久化主单和明细 | Task-05, Task-09 |
| AC-02 | 按日期范围筛选 | Task-06, Task-11 |
| AC-03 | 按供应商筛选 | Task-06, Task-11 |
| AC-04 | 列表显示供应商名称 | Task-04, Task-06, Task-11 |
| AC-05 | 列表显示付款状态 | Task-04, Task-06, Task-11 |
| AC-06 | 详情页可访问 | Task-07, Task-10, Task-12 |
| AC-07 | 分页正常 | Task-06, Task-11 |
| AC-08 | 数据库 schema 升级到 v3 | Task-01, Task-02 |
| AC-09 | 单元测试覆盖 | Task-13 |

---

## 六、风险提示

| 风险 | 影响 | 应对措施 |
|------|------|----------|
| `purchase_in` 函数变长 | 中 | 函数内部分段（步骤 1-6），加注释 |
| Tauri State 单元测试复杂 | 中 | 先做集成测试（启动 Tauri runtime），或抽象 Database trait |
| PurchaseOrder/PurchaseOrderItem 字段调整 | 中 | 同步更新所有调用方（PurchaseForm.vue、types/index.ts） |
| v0.2.0 历史采购数据无主单 | 低 | 不回填，文档说明；老批次可继续使用 |
| 详情页路由指向错误 | 低 | 任务间明确依赖，Task-12 强制要求 Task-10 完成 |

---

## 七、执行检查清单

在执行每个任务前，请确认：

- [ ] 已阅读并理解任务说明
- [ ] 已检查前置条件
- [ ] 已准备好回滚方案
- [ ] 已了解验证方法
- [ ] 已确认依赖任务已完成
- [ ] 已同步更新版本控制任务（Task-14, 15）

---

## 八、执行建议顺序

1. **阶段一**（Task-01, 02）：1 小时
2. **阶段二**（Task-03, 04）：0.5 小时
3. **阶段三**（Task-05, 06, 07, 08）：3 小时
4. **阶段四**（Task-09）：0.5 小时
5. **阶段五**（Task-10, 11, 12）：3 小时
6. **阶段六**（Task-13）：4 小时
7. **阶段七**（Task-14, 15）：0.5 小时

**总工时**：约 12.5 小时（1.5 工作日）

---

## 九、下一阶段

进入 [功能验收](file:///d:/AI/AI/开发/茶叶店管理/.trae/skills/feature-acceptance/SKILL.md) 阶段：使用 `feature-acceptance` 技能逐条验证 AC-01 ~ AC-09。
