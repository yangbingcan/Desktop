# 任务规划 - 储值余额功能

> **功能模块**：M05 会员管理 - 储值余额子模块
> **目标版本**：v0.3.1
> **创建日期**：2026-07-02
> **关联文档**：
> - 需求：[M06-储值余额功能.md](file:///d:/AI/AI/开发/茶叶店管理/specs/features/M06-储值余额功能.md)
> - 技术方案：[M06-储值余额功能_技术方案.md](file:///d:/AI/AI/开发/茶叶店管理/specs/features/M06-储值余额功能_技术方案.md)

---

## 一、任务概览

| 项目 | 内容 |
|------|------|
| 功能名称 | 储值余额（充值/扣款/退款/流水） |
| 任务总数 | 17 个任务（含 2 个版本控制任务） |
| 预计工时 | 12 小时（1.5 工作日） |
| 依赖任务 | 0（v0.3.0 已完成） |
| 数据库变更 | ⚠️ 涉及（SCHEMA_VERSION 3 → 4） |
| 新增表 | 1 张（`member_balance_logs`） |

---

## 二、阶段划分

### 阶段一：数据层（后端）
- Task-01: 编写数据库迁移脚本（migrate_v4_member_balance_logs）
- Task-02: 升级 SCHEMA_VERSION 到 4 + 注册迁移

### 阶段二：模型层（后端）
- Task-03: 新增 BalanceLog/RechargeInput/RefundInput/Result 模型

### 阶段三：Command 层（后端）
- Task-04: 新建 commands/members.rs 模块
- Task-05: 实现 recharge_member_balance（impl + Command）
- Task-06: 实现 refund_member_balance（impl + Command）
- Task-07: 实现 get_member_balance_logs（impl + Command）
- Task-08: 实现 get_member_last_payment_method（impl + Command）
- Task-09: 改造 create_sale_order（嵌入余额扣款 + 流水）

### 阶段四：API 层（前端）
- Task-10: 新增 4 个 API 函数（recharge/refund/getLogs/getLastMethod）

### 阶段五：表现层（前端）
- Task-11: 重构 MemberDetail.vue 充值弹窗
- Task-12: 新增 MemberDetail.vue 退款弹窗
- Task-13: 新增 MemberDetail.vue 储值流水标签页
- Task-14: 收银台余额扣款校验（validatePayMethod）

### 阶段六：质量保障（后端）
- Task-15: 编写单元测试（8 个测试用例）

### 阶段七：版本控制（强制）
- Task-16: 更新版本号（package.json + Cargo.toml + project_config.md）
- Task-17: 更新 changelog.json + 创建 v0.3.1.md 发布文档

---

## 三、任务详情

---

### Task-01: 编写数据库迁移脚本（migrate_v4_member_balance_logs）

**任务说明**
新增 `migrate_v4_member_balance_logs` 函数，创建 `member_balance_logs` 表 + 4 个索引 + 2 个外键 + 2 个 CHECK 约束。

**涉及文件**
- [db/schema.rs](file:///d:/AI/AI/开发/茶叶店管理/tea-manage/src-tauri/src/db/schema.rs)

**验证标准**
- [ ] 迁移函数代码完整，无编译错误
- [ ] 所有 SQL 使用 `IF NOT EXISTS` 保证幂等
- [ ] CHECK 约束和外键约束正确

**依赖任务**: 无

**对应验收标准**: AC-R-02 / AC-R-11（数据持久化）

#### 步骤 1.1: 在 schema.rs 添加 migrate_v4_member_balance_logs 函数

**前置条件**
- [ ] 已阅读 [schema.rs](file:///d:/AI/AI/开发/茶叶店管理/tea-manage/src-tauri/src/db/schema.rs) 现有 v3 迁移实现

**执行动作**
在 `schema.rs` 末尾（约 v3 迁移函数后）新增：

```rust
/// v4 迁移：新增会员储值余额流水表
///
/// M06 储值余额功能需要：
/// 1. member_balance_logs - 储值/扣款/退款流水
///
/// 数据迁移策略：全新表，无需回填
pub fn migrate_v4_member_balance_logs(conn: &Connection) -> SqliteResult<()> {
    conn.execute(
        "CREATE TABLE IF NOT EXISTS member_balance_logs (
            id TEXT PRIMARY KEY NOT NULL,
            member_id TEXT NOT NULL,
            change_type TEXT NOT NULL
                CHECK (change_type IN ('recharge', 'consume', 'refund')),
            change_amount REAL NOT NULL,
            balance_after REAL NOT NULL,
            payment_method TEXT NOT NULL
                CHECK (payment_method IN ('cash', 'wechat', 'alipay')),
            operator TEXT NOT NULL,
            related_order_id TEXT,
            bonus_amount REAL NOT NULL DEFAULT 0,
            fee_amount REAL NOT NULL DEFAULT 0,
            remark TEXT NOT NULL DEFAULT '',
            created_at TEXT NOT NULL DEFAULT (datetime('now')),
            FOREIGN KEY (member_id) REFERENCES members(id) ON DELETE RESTRICT,
            FOREIGN KEY (related_order_id) REFERENCES sales_orders(id) ON DELETE SET NULL
        )",
        [],
    )?;

    conn.execute(
        "CREATE INDEX IF NOT EXISTS idx_balance_log_member ON member_balance_logs(member_id)",
        [],
    )?;
    conn.execute(
        "CREATE INDEX IF NOT EXISTS idx_balance_log_type ON member_balance_logs(change_type)",
        [],
    )?;
    conn.execute(
        "CREATE INDEX IF NOT EXISTS idx_balance_log_created ON member_balance_logs(created_at)",
        [],
    )?;
    conn.execute(
        "CREATE INDEX IF NOT EXISTS idx_balance_log_order ON member_balance_logs(related_order_id)",
        [],
    )?;

    Ok(())
}
```

**预期结果**
- 函数编译通过（`cargo check`）
- 函数签名 `pub fn migrate_v4_member_balance_logs(conn: &Connection) -> SqliteResult<()>`

**验证方法**
```powershell
cd d:\AI\AI\开发\茶叶店管理\tea-manage\src-tauri
cargo check
```
预期：无错误。

**回滚方案**: 删除 `migrate_v4_member_balance_logs` 函数

**输出物**: `schema.rs` 中新增的函数（约 40 行）

---

### Task-02: 升级 SCHEMA_VERSION 到 4 并注册迁移

**任务说明**
将 `SCHEMA_VERSION` 从 3 升级到 4，并在 `run_migrations` 中注册 v4 迁移。

**涉及文件**: [schema.rs](file:///d:/AI/AI/开发/茶叶店管理/tea-manage/src-tauri/src/db/schema.rs)

**验证标准**
- [ ] `SCHEMA_VERSION = 4`
- [ ] `run_migrations` 中新增 `if current_version < 4 { migrate_v4_member_balance_logs(conn)?; }`

**依赖任务**: Task-01

**对应验收标准**: AC-R-08（数据库 schema 升级到 v4）

#### 步骤 2.1: 修改 SCHEMA_VERSION 常量

**前置条件**: Task-01 完成

**执行动作**
修改 [schema.rs:15](file:///d:/AI/AI/开发/茶叶店管理/tea-manage/src-tauri/src/db/schema.rs#L15)：

```rust
// 原代码
const SCHEMA_VERSION: u32 = 3;

// 改为
const SCHEMA_VERSION: u32 = 4;
```

并更新版本历史注释：
```rust
/// - v4: 新增会员储值余额流水表（M06 储值余额功能）
```

**预期结果**: 常量值变为 4

**验证方法**:
```powershell
Get-Content d:\AI\AI\开发\茶叶店管理\tea-manage\src-tauri\src\db\schema.rs -TotalCount 20
```

**回滚方案**: 还原 `SCHEMA_VERSION: u32 = 3;`

**输出物**: 修改后的 `schema.rs` 第 15 行

#### 步骤 2.2: 在 run_migrations 注册 v4 迁移

**前置条件**: 步骤 2.1 完成

**执行动作**
修改 `run_migrations` 函数，在 v3 分支后新增：

```rust
if current_version < 4 {
    // v4: M06 储值余额功能 - 会员储值流水表
    migrate_v4_member_balance_logs(conn)?;
}
```

**预期结果**: `run_migrations` 包含 v4 的 if 分支

**验证方法**:
```powershell
cd d:\AI\AI\开发\茶叶店管理\tea-manage\src-tauri
cargo check
```

**回滚方案**: 删除 v4 分支

**输出物**: 修改后的 `run_migrations` 函数

---

### Task-03: 新增余额相关数据模型

**任务说明**
在 `models/` 目录下新增 `member_balance.rs` 文件，定义 RechargeInput/RefundInput/RechargeResult/RefundResult/BalanceLog 等结构体。

**涉及文件**: `src-tauri/src/models/member_balance.rs`（新建）

**验证标准**
- [ ] 所有结构体编译通过
- [ ] serde rename_all = "camelCase" 正确
- [ ] 在 `models/mod.rs` 中导出

**依赖任务**: Task-01

#### 步骤 3.1: 创建 models/member_balance.rs

**前置条件**: Task-01 完成

**执行动作**
创建文件 `src-tauri/src/models/member_balance.rs`：

```rust
//! 会员储值余额数据模型
//!
//! v0.3.1 M06 储值余额功能

use serde::{Deserialize, Serialize};

/// 储值流水类型
#[derive(Debug, Clone, Serialize, Deserialize, PartialEq)]
#[serde(rename_all = "lowercase")]
pub enum BalanceChangeType {
    Recharge,
    Consume,
    Refund,
}

impl BalanceChangeType {
    pub fn as_str(&self) -> &'static str {
        match self {
            Self::Recharge => "recharge",
            Self::Consume => "consume",
            Self::Refund => "refund",
        }
    }
}

/// 储值流水
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct BalanceLog {
    pub id: String,
    pub member_id: String,
    pub change_type: String,            // recharge | consume | refund
    pub change_amount: f64,
    pub balance_after: f64,
    pub payment_method: String,         // cash | wechat | alipay
    pub operator: String,
    pub related_order_id: Option<String>,
    pub bonus_amount: f64,
    pub fee_amount: f64,
    pub remark: String,
    pub created_at: String,
}

/// 充值输入
#[derive(Debug, Clone, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct RechargeInput {
    pub member_id: String,
    pub amount: f64,                    // > 0
    pub payment_method: String,         // cash | wechat | alipay
    pub operator: String,
    #[serde(default)]
    pub remark: Option<String>,
    #[serde(default)]
    pub bonus_amount: f64,              // 预留
}

/// 充值结果
#[derive(Debug, Clone, Serialize)]
#[serde(rename_all = "camelCase")]
pub struct RechargeResult {
    pub log_id: String,
    pub new_balance: f64,
    pub created_at: String,
}

/// 退款输入
#[derive(Debug, Clone, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct RefundInput {
    pub member_id: String,
    pub amount: f64,                    // > 0 且 <= balance
    pub payment_method: String,
    pub operator: String,
    pub remark: String,                 // 必填，>= 5 字符
}

/// 退款结果
#[derive(Debug, Clone, Serialize)]
#[serde(rename_all = "camelCase")]
pub struct RefundResult {
    pub log_id: String,
    pub new_balance: f64,
    pub created_at: String,
}
```

#### 步骤 3.2: 在 models/mod.rs 中导出

修改 `src-tauri/src/models/mod.rs`：

```rust
// 在已有 mod 声明后新增
pub mod member_balance;
```

**预期结果**: `cargo check` 全部通过

**验证方法**:
```powershell
cd d:\AI\AI\开发\茶叶店管理\tea-manage\src-tauri
cargo check
```

**回滚方案**: 删除整个 `member_balance.rs` 和 mod.rs 中的 `pub mod member_balance;`

**输出物**:
- `src-tauri/src/models/member_balance.rs`（约 80 行）
- `models/mod.rs` 1 行新增

---

### Task-04: 新建 commands/members.rs 模块

**任务说明**
虽然已有 `sales.rs` 包含 `get_member_by_phone`/`create_member` 等命令，但**储值相关**命令（recharge/refund/getLogs）应单独放在 `commands/members.rs` 中，便于职责分离。

**涉及文件**
- `src-tauri/src/commands/members.rs`（新建）
- `src-tauri/src/commands/mod.rs`（修改，注册新模块）

**验证标准**
- [ ] `commands/members.rs` 编译通过（即使只有空 mod）
- [ ] `mod members;` 和 `pub use members::*;` 已添加

**依赖任务**: Task-03

#### 步骤 4.1: 创建 commands/members.rs 空文件

**前置条件**: Task-03 完成

**执行动作**
创建文件 `src-tauri/src/commands/members.rs`：

```rust
//! 会员储值余额 Tauri Commands
//!
//! 提供会员充值、退款、流水查询
//! v0.3.1 M06 储值余额功能
```

#### 步骤 4.2: 在 commands/mod.rs 注册

修改 `src-tauri/src/commands/mod.rs`：

```rust
mod products;
mod categories;
mod inventory;
mod sales;
mod suppliers;
mod return_order;
mod members;  // ✨ 新增

pub use products::*;
pub use categories::*;
pub use inventory::*;
pub use sales::*;
pub use suppliers::*;
pub use return_order::*;
pub use members::*;  // ✨ 新增
```

**预期结果**: 编译通过

**验证方法**: `cargo check`

**回滚方案**: 删除新文件 + 还原 mod.rs

**输出物**:
- `commands/members.rs`（约 3 行）
- `commands/mod.rs` 2 行新增

---

### Task-05: 实现 recharge_member_balance（impl + Command）

**任务说明**
实现 `recharge_member_balance_impl` 业务函数（含事务、校验、流水插入），并提供 Tauri Command 包装。

**涉及文件**: `src-tauri/src/commands/members.rs`

**测试文件**: `src-tauri/src/commands/members.rs`（Task-15 中新增 `#[cfg(test)] mod tests`）

**验证标准**
- [ ] 函数编译通过
- [ ] 金额 ≤ 0 时返回错误
- [ ] 事务中：先查余额 → 更新余额 → 插入流水
- [ ] 单元测试通过（Task-15 测试 1+2）

**依赖任务**: Task-01, Task-03, Task-04

**对应验收标准**: AC-R-02, AC-R-03

#### 步骤 5.1: 实现 recharge_member_balance_impl

**前置条件**: Task-04 完成

**执行动作**
在 `commands/members.rs` 中新增：

```rust
use crate::db::Database;
use crate::models::{RechargeInput, RechargeResult};
use chrono::Local;
use rusqlite::{params, Connection};
use uuid::Uuid;

/// 充值金额校验
fn validate_recharge_amount(amount: f64) -> Result<(), String> {
    if amount <= 0.0 {
        return Err("充值金额必须大于 0".to_string());
    }
    Ok(())
}

/// 支付方式校验
fn validate_payment_method(method: &str) -> Result<(), String> {
    if !["cash", "wechat", "alipay"].contains(&method) {
        return Err(format!("无效的支付方式: {}", method));
    }
    Ok(())
}

/// 会员充值业务实现
pub fn recharge_member_balance_impl(
    conn: &Connection,
    input: RechargeInput,
) -> Result<RechargeResult, String> {
    // 1. 校验
    validate_recharge_amount(input.amount)?;
    validate_payment_method(&input.payment_method)?;

    let now = Local::now().format("%Y-%m-%d %H:%M:%S").to_string();
    let log_id = Uuid::new_v4().to_string();

    // 2. 排他事务
    conn.execute("BEGIN EXCLUSIVE TRANSACTION", [])
        .map_err(|e| e.to_string())?;

    let rollback = |e: String, conn: &Connection| -> String {
        let _ = conn.execute("ROLLBACK", []);
        e
    };

    // 3. 查询会员当前余额
    let current_balance: f64 = conn.query_row(
        "SELECT balance FROM members WHERE id = ? AND is_active = 1",
        [&input.member_id],
        |row| row.get(0),
    ).map_err(|e| rollback(format!("会员不存在或已停用: {}", e), conn))?;

    // 4. 计算新余额
    let new_balance = current_balance + input.amount;

    // 5. 更新余额
    conn.execute(
        "UPDATE members SET balance = ?, updated_at = ? WHERE id = ?",
        params![new_balance, now, &input.member_id],
    ).map_err(|e| rollback(format!("更新余额失败: {}", e), conn))?;

    // 6. 插入流水
    conn.execute(
        "INSERT INTO member_balance_logs
            (id, member_id, change_type, change_amount, balance_after,
             payment_method, operator, related_order_id, bonus_amount, remark, created_at)
         VALUES (?, ?, 'recharge', ?, ?, ?, ?, NULL, ?, ?, ?)",
        params![
            log_id,
            input.member_id,
            input.amount,
            new_balance,
            input.payment_method,
            input.operator,
            input.bonus_amount,
            input.remark.clone().unwrap_or_default(),
            now
        ],
    ).map_err(|e| rollback(format!("插入流水失败: {}", e), conn))?;

    // 7. 提交
    conn.execute("COMMIT", []).map_err(|e| e.to_string())?;

    Ok(RechargeResult {
        log_id,
        new_balance,
        created_at: now,
    })
}

/// 会员充值 Tauri Command
#[tauri::command]
pub async fn recharge_member_balance(
    db: tauri::State<'_, Database>,
    input: RechargeInput,
) -> Result<RechargeResult, String> {
    let conn = db.get_conn()?;
    recharge_member_balance_impl(&conn, input)
}
```

**预期结果**: 函数编译通过

**验证方法**:
```powershell
cd d:\AI\AI\开发\茶叶店管理\tea-manage\src-tauri
cargo check
```

**回滚方案**: 删除 `recharge_member_balance_impl` 和 `recharge_member_balance`

**输出物**: `commands/members.rs` 新增约 90 行

---

### Task-06: 实现 refund_member_balance（impl + Command）

**任务说明**
实现退款业务逻辑：校验金额≤余额、备注≥5字符、退款到指定支付方式、记录流水。

**涉及文件**: `src-tauri/src/commands/members.rs`

**验证标准**
- [ ] 校验 amount > 0 且 ≤ balance
- [ ] 校验 remark.len() ≥ 5
- [ ] 事务中：扣余额 → 插流水
- [ ] 单元测试通过（Task-15 测试 3+4+5）

**依赖任务**: Task-05

**对应验收标准**: AC-R-11, AC-R-12, AC-R-13

#### 步骤 6.1: 实现 refund_member_balance_impl

**前置条件**: Task-05 完成

**执行动作**
在 `commands/members.rs` 中新增（与 recharge 类似的结构）：

```rust
use crate::models::{RefundInput, RefundResult};

/// 退款金额校验
fn validate_refund_amount(amount: f64, balance: f64) -> Result<(), String> {
    if amount <= 0.0 {
        return Err("退款金额必须大于 0".to_string());
    }
    if amount > balance {
        return Err(format!("退款金额不能超过余额 ¥{:.2}", balance));
    }
    Ok(())
}

/// 退款备注校验
fn validate_refund_remark(remark: &str) -> Result<(), String> {
    if remark.trim().chars().count() < 5 {
        return Err("退款原因至少 5 个字符".to_string());
    }
    Ok(())
}

/// 会员退款业务实现
pub fn refund_member_balance_impl(
    conn: &Connection,
    input: RefundInput,
) -> Result<RefundResult, String> {
    // 1. 校验
    validate_payment_method(&input.payment_method)?;
    validate_refund_remark(&input.remark)?;

    let now = Local::now().format("%Y-%m-%d %H:%M:%S").to_string();
    let log_id = Uuid::new_v4().to_string();

    // 2. 排他事务
    conn.execute("BEGIN EXCLUSIVE TRANSACTION", [])
        .map_err(|e| e.to_string())?;

    let rollback = |e: String, conn: &Connection| -> String {
        let _ = conn.execute("ROLLBACK", []);
        e
    };

    // 3. 查询当前余额
    let current_balance: f64 = conn.query_row(
        "SELECT balance FROM members WHERE id = ? AND is_active = 1",
        [&input.member_id],
        |row| row.get(0),
    ).map_err(|e| rollback(format!("会员不存在或已停用: {}", e), conn))?;

    // 4. 校验退款金额
    validate_refund_amount(input.amount, current_balance)
        .map_err(|e| rollback(e, conn))?;

    // 5. 计算新余额
    let new_balance = current_balance - input.amount;

    // 6. 扣减余额
    conn.execute(
        "UPDATE members SET balance = ?, updated_at = ? WHERE id = ?",
        params![new_balance, now, &input.member_id],
    ).map_err(|e| rollback(format!("扣减余额失败: {}", e), conn))?;

    // 7. 插入流水（负数表示扣减）
    conn.execute(
        "INSERT INTO member_balance_logs
            (id, member_id, change_type, change_amount, balance_after,
             payment_method, operator, related_order_id, remark, created_at)
         VALUES (?, ?, 'refund', ?, ?, ?, ?, NULL, ?, ?)",
        params![
            log_id,
            input.member_id,
            -input.amount,
            new_balance,
            input.payment_method,
            input.operator,
            input.remark,
            now
        ],
    ).map_err(|e| rollback(format!("插入退款流水失败: {}", e), conn))?;

    // 8. 提交
    conn.execute("COMMIT", []).map_err(|e| e.to_string())?;

    Ok(RefundResult {
        log_id,
        new_balance,
        created_at: now,
    })
}

#[tauri::command]
pub async fn refund_member_balance(
    db: tauri::State<'_, Database>,
    input: RefundInput,
) -> Result<RefundResult, String> {
    let conn = db.get_conn()?;
    refund_member_balance_impl(&conn, input)
}
```

**预期结果**: 函数编译通过

**验证方法**: `cargo check`

**回滚方案**: 删除 refund 相关函数

**输出物**: `commands/members.rs` 新增约 90 行

---

### Task-07: 实现 get_member_balance_logs（impl + Command）

**任务说明**
查询会员流水，支持分页和类型筛选（recharge/consume/refund）。

**涉及文件**: `src-tauri/src/commands/members.rs`

**验证标准**
- [ ] 函数编译通过
- [ ] 按 member_id 过滤
- [ ] 可选按 change_type 过滤
- [ ] 按 created_at DESC 排序
- [ ] 单元测试通过（Task-15 测试 8）

**依赖任务**: Task-05

**对应验收标准**: AC-R-14, AC-R-15

#### 步骤 7.1: 实现 get_member_balance_logs_impl

**前置条件**: Task-05 完成

**执行动作**
在 `commands/members.rs` 中新增：

```rust
use crate::models::{BalanceLog, PageResult};

/// 获取会员储值流水
pub fn get_member_balance_logs_impl(
    conn: &Connection,
    member_id: String,
    page: Option<i64>,
    page_size: Option<i64>,
    change_type: Option<String>,
) -> Result<PageResult<BalanceLog>, String> {
    let page = page.unwrap_or(1);
    let page_size = page_size.unwrap_or(20);
    let offset = (page - 1) * page_size;

    // 构建动态 SQL
    let mut where_clauses: Vec<String> = vec!["member_id = ?".to_string()];
    let mut params_vec: Vec<Box<dyn rusqlite::ToSql>> = vec![
        Box::new(member_id.clone()),
    ];

    if let Some(ref ct) = change_type {
        if !ct.trim().is_empty() {
            where_clauses.push("change_type = ?".to_string());
            params_vec.push(Box::new(ct.clone()));
        }
    }

    let where_sql = format!("WHERE {}", where_clauses.join(" AND "));

    // 查询总数
    let count_sql = format!("SELECT COUNT(*) FROM member_balance_logs {}", where_sql);
    let count_params: Vec<&dyn rusqlite::ToSql> =
        params_vec.iter().map(|p| p.as_ref()).collect();
    let total: i64 = conn.query_row(&count_sql, count_params.as_slice(), |row| row.get(0))
        .map_err(|e| e.to_string())?;

    // 查询列表
    let list_sql = format!(
        "SELECT id, member_id, change_type, change_amount, balance_after,
                payment_method, operator, related_order_id, bonus_amount,
                fee_amount, remark, created_at
         FROM member_balance_logs {}
         ORDER BY created_at DESC LIMIT ? OFFSET ?",
        where_sql
    );
    params_vec.push(Box::new(page_size));
    params_vec.push(Box::new(offset));
    let list_params: Vec<&dyn rusqlite::ToSql> =
        params_vec.iter().map(|p| p.as_ref()).collect();

    let mut stmt = conn.prepare(&list_sql).map_err(|e| e.to_string())?;
    let logs: Vec<BalanceLog> = stmt.query_map(list_params.as_slice(), |row| {
        Ok(BalanceLog {
            id: row.get(0)?,
            member_id: row.get(1)?,
            change_type: row.get(2)?,
            change_amount: row.get(3)?,
            balance_after: row.get(4)?,
            payment_method: row.get(5)?,
            operator: row.get(6)?,
            related_order_id: row.get(7)?,
            bonus_amount: row.get(8)?,
            fee_amount: row.get(9)?,
            remark: row.get(10)?,
            created_at: row.get(11)?,
        })
    }).map_err(|e| e.to_string())?
    .collect::<Result<Vec<_>, _>>()
    .map_err(|e| e.to_string())?;

    Ok(PageResult {
        list: logs,
        total: total as u32,
        page: page as u32,
        page_size: page_size as u32,
    })
}

#[tauri::command]
pub async fn get_member_balance_logs(
    db: tauri::State<'_, Database>,
    member_id: String,
    page: Option<i64>,
    page_size: Option<i64>,
    change_type: Option<String>,
) -> Result<PageResult<BalanceLog>, String> {
    let conn = db.get_conn()?;
    get_member_balance_logs_impl(&conn, member_id, page, page_size, change_type)
}
```

**预期结果**: 编译通过

**验证方法**: `cargo check`

**回滚方案**: 删除 get_member_balance_logs 相关函数

**输出物**: `commands/members.rs` 新增约 60 行

---

### Task-08: 实现 get_member_last_payment_method（辅助接口）

**任务说明**
查询会员最近一次充值的支付方式（退款时默认选择）。

**涉及文件**: `src-tauri/src/commands/members.rs`

**验证标准**
- [ ] 函数编译通过
- [ ] 找不到时返回 None

**依赖任务**: Task-01

**对应验收标准**: AC-R-10（退款弹窗默认退款方式）

#### 步骤 8.1: 实现 get_member_last_payment_method_impl

**前置条件**: Task-01 完成

**执行动作**
在 `commands/members.rs` 中新增：

```rust
/// 获取会员最近一次充值的支付方式
pub fn get_member_last_payment_method_impl(
    conn: &Connection,
    member_id: String,
) -> Result<Option<String>, String> {
    let result: Option<String> = conn.query_row(
        "SELECT payment_method FROM member_balance_logs
         WHERE member_id = ? AND change_type = 'recharge'
         ORDER BY created_at DESC LIMIT 1",
        [&member_id],
        |row| row.get(0),
    ).ok();

    Ok(result)
}

#[tauri::command]
pub async fn get_member_last_payment_method(
    db: tauri::State<'_, Database>,
    member_id: String,
) -> Result<Option<String>, String> {
    let conn = db.get_conn()?;
    get_member_last_payment_method_impl(&conn, member_id)
}
```

**预期结果**: 编译通过

**验证方法**: `cargo check`

**回滚方案**: 删除该函数

**输出物**: `commands/members.rs` 新增约 20 行

---

### Task-09: 改造 create_sale_order（嵌入余额扣款 + 流水）

**任务说明**
在 `create_sale_order` 事务中，当 `pay_method == "memberBalance"` 时扣减会员余额并插入 consume 流水。

**涉及文件**: `src-tauri/src/commands/sales.rs`

**验证标准**
- [ ] 嵌入到现有 BEGIN EXCLUSIVE TRANSACTION 内
- [ ] 校验余额 ≥ actual_amount
- [ ] UPDATE members.balance + INSERT member_balance_logs
- [ ] 单元测试通过（Task-15 测试 6+7）

**依赖任务**: Task-01, Task-05

**对应验收标准**: AC-R-05, AC-R-06, AC-R-08, AC-R-09

#### 步骤 9.1: 在 create_sale_order 中嵌入余额扣款逻辑

**前置条件**: Task-01, Task-05 完成

**执行动作**
修改 [sales.rs:411-422](file:///d:/AI/AI/开发/茶叶店管理/tea-manage/src-tauri/src/commands/sales.rs#L411-L422) 的"CR-08 balance 保护"代码块：

```rust
// 替换原 balance < 0 保护代码

// v0.3.1: 当 pay_method='memberBalance' 时扣减余额 + 记录流水
if input.pay_method == "memberBalance" {
    if let Some(ref mid) = input.member_id {
        // 1. 查询当前余额
        let current_balance: f64 = conn.query_row(
            "SELECT balance FROM members WHERE id = ?",
            [mid],
            |row| row.get(0),
        ).map_err(|e| rollback_on_err(format!("查询会员余额失败: {}", e), &conn))?;

        // 2. 校验余额充足
        if current_balance < actual_amount {
            return Err(rollback_on_err(
                format!("会员余额不足，当前余额 ¥{:.2}，需要支付 ¥{:.2}", current_balance, actual_amount),
                &conn,
            ));
        }

        // 3. 扣减余额
        let new_balance = current_balance - actual_amount;
        conn.execute(
            "UPDATE members SET balance = ?, updated_at = ? WHERE id = ?",
            params![new_balance, now, mid],
        ).map_err(|e| rollback_on_err(format!("扣减余额失败: {}", e), &conn))?;

        // 4. 记录 consume 流水
        let log_id = Uuid::new_v4().to_string();
        conn.execute(
            "INSERT INTO member_balance_logs
                (id, member_id, change_type, change_amount, balance_after,
                 payment_method, operator, related_order_id, created_at)
             VALUES (?, ?, 'consume', ?, ?, 'memberBalance', '收银台', ?, ?)",
            params![log_id, mid, -actual_amount, new_balance, order_id, now],
        ).map_err(|e| rollback_on_err(format!("记录余额流水失败: {}", e), &conn))?;
    }
}
```

**预期结果**:
- 当 `pay_method='memberBalance'` 且 `member_id` 存在时，扣减余额并写流水
- 余额不足时报错并回滚
- 其他支付方式不受影响

**验证方法**: `cargo check` + 单元测试

**回滚方案**: 还原原"CR-08 balance 保护"代码

**输出物**: `sales.rs` 修改约 40 行

---

### Task-10: 新增 4 个前端 API 函数

**任务说明**
在 `src/api/members.ts` 中新增 4 个 Tauri Command 封装函数。

**涉及文件**: `src/api/members.ts`

**验证标准**
- [ ] TypeScript 编译通过
- [ ] 函数签名与后端一致

**依赖任务**: Task-05, 06, 07, 08

**对应验收标准**: AC-R-01 ~ AC-R-15（前端调用层）

#### 步骤 10.1: 在 members.ts 末尾新增 API

**前置条件**: Task-05 ~ 08 完成

**执行动作**
在 `src/api/members.ts` 末尾新增：

```typescript
/**
 * @file 储值余额 API
 * @description Tauri Commands 封装 - 充值/退款/流水查询
 * @since v0.3.1
 */
import type { RechargeInput, RechargeResult, RefundInput, RefundResult, BalanceLog } from '@/types'

/** 会员充值 */
export async function rechargeMemberBalance(input: RechargeInput): Promise<RechargeResult> {
    return await invoke('recharge_member_balance', { input })
}

/** 会员退款 */
export async function refundMemberBalance(input: RefundInput): Promise<RefundResult> {
    return await invoke('refund_member_balance', { input })
}

/** 获取储值流水（分页 + 类型筛选） */
export async function getMemberBalanceLogs(
    memberId: string,
    page?: number,
    pageSize?: number,
    changeType?: string
): Promise<PageResult<BalanceLog>> {
    return await invoke('get_member_balance_logs', {
        memberId,
        page: page || 1,
        page_size: pageSize || 20,
        change_type: changeType || null
    })
}

/** 获取会员最近一次充值的支付方式 */
export async function getMemberLastPaymentMethod(memberId: string): Promise<string | null> {
    return await invoke('get_member_last_payment_method', { memberId })
}
```

#### 步骤 10.2: 在 src/types/index.ts 新增类型

修改 `src/types/index.ts` 新增：

```typescript
// 储值相关类型
export interface RechargeInput {
    memberId: string
    amount: number
    paymentMethod: 'cash' | 'wechat' | 'alipay'
    operator: string
    remark?: string
    bonusAmount?: number
}

export interface RechargeResult {
    logId: string
    newBalance: number
    createdAt: string
}

export interface RefundInput {
    memberId: string
    amount: number
    paymentMethod: 'cash' | 'wechat' | 'alipay'
    operator: string
    remark: string
}

export interface RefundResult {
    logId: string
    newBalance: number
    createdAt: string
}

export interface BalanceLog {
    id: string
    memberId: string
    changeType: 'recharge' | 'consume' | 'refund'
    changeAmount: number
    balanceAfter: number
    paymentMethod: string
    operator: string
    relatedOrderId: string | null
    bonusAmount: number
    feeAmount: number
    remark: string
    createdAt: string
}
```

**预期结果**:
- TypeScript 编译通过
- 4 个 API 函数可调用

**验证方法**:
```powershell
cd d:\AI\AI\开发\茶叶店管理\tea-manage
npx vue-tsc --noEmit
```

**回滚方案**: 还原 members.ts 和 types/index.ts

**输出物**:
- `src/api/members.ts` 新增约 35 行
- `src/types/index.ts` 新增约 55 行

---

### Task-11: 重构 MemberDetail.vue 充值弹窗

**任务说明**
将原占位 `handleRecharge` 完整实现：调用 `rechargeMemberBalance` API、刷新会员详情。

**涉及文件**: `src/views/members/MemberDetail.vue`

**验证标准**
- [ ] 充值弹窗包含：金额、支付方式、备注
- [ ] 调用 API 成功后刷新 balance
- [ ] 错误提示友好

**依赖任务**: Task-10

**对应验收标准**: AC-R-01, AC-R-02

#### 步骤 11.1: 重构充值弹窗

**前置条件**: Task-10 完成

**执行动作**
修改 `MemberDetail.vue`：
1. 引入 `rechargeMemberBalance`
2. 重构 `showRechargeModal` 的弹窗 UI（增加支付方式、备注）
3. 重构 `handleRecharge` 函数

```vue
<!-- 替换原充值弹窗 -->
<n-modal v-model:show="showRechargeModal">
    <n-card title="会员充值" style="width: 450px">
        <n-form-item label="充值金额" required>
            <n-input-number
                v-model:value="rechargeAmount"
                :min="0.01"
                :precision="2"
                placeholder="请输入充值金额"
                style="width: 100%"
            />
        </n-form-item>
        <n-form-item label="支付方式" required>
            <n-select
                v-model:value="rechargePaymentMethod"
                :options="PAYMENT_OPTIONS"
                placeholder="选择支付方式"
            />
        </n-form-item>
        <n-form-item label="备注">
            <n-input
                v-model:value="rechargeRemark"
                type="textarea"
                placeholder="可选，填写充值原因或来源"
                :rows="2"
            />
        </n-form-item>
        <template #footer>
            <n-space justify="end">
                <n-button @click="showRechargeModal = false">取消</n-button>
                <n-button
                    type="primary"
                    :loading="rechargeLoading"
                    :disabled="rechargeAmount <= 0"
                    @click="handleRecharge"
                >
                    确认充值
                </n-button>
            </n-space>
        </template>
    </n-card>
</n-modal>
```

```typescript
// 在 script 中新增状态
const rechargePaymentMethod = ref('cash')
const rechargeRemark = ref('')
const rechargeLoading = ref(false)
const PAYMENT_OPTIONS = [
    { label: '现金', value: 'cash' },
    { label: '微信转账', value: 'wechat' },
    { label: '支付宝转账', value: 'alipay' }
]

// 重构 handleRecharge
async function handleRecharge() {
    if (rechargeAmount.value <= 0) {
        message.warning('请输入有效的充值金额')
        return
    }
    rechargeLoading.value = true
    try {
        const result = await rechargeMemberBalance({
            memberId: memberId.value,
            amount: rechargeAmount.value,
            paymentMethod: rechargePaymentMethod.value,
            operator: '当前店员',  // 实际从登录态获取
            remark: rechargeRemark.value || undefined
        })
        message.success(`充值成功，新余额 ¥${result.newBalance.toFixed(2)}`)
        // 刷新会员详情
        const detail = await memberStore.getMemberDetailById(memberId.value)
        member.value = detail.member
        // 重置表单
        rechargeAmount.value = 0
        rechargeRemark.value = ''
        showRechargeModal.value = false
    } catch (e: any) {
        message.error(`充值失败: ${e}`)
    } finally {
        rechargeLoading.value = false
    }
}
```

**预期结果**:
- 充值弹窗支持完整功能
- 充值成功后刷新页面

**验证方法**: 启动应用，测试充值流程

**回滚方案**: 还原原占位实现

**输出物**: `MemberDetail.vue` 修改约 60 行

---

### Task-12: 新增 MemberDetail.vue 退款弹窗

**任务说明**
在 MemberDetail.vue 新增"退款"按钮和退款弹窗（仅余额 > 0 时显示）。

**涉及文件**: `src/views/members/MemberDetail.vue`

**验证标准**
- [ ] 仅在余额 > 0 时显示"退款"按钮
- [ ] 默认支付方式 = 最近一次充值方式
- [ ] 默认退款金额 = 当前余额
- [ ] 备注 ≥ 5 字符才能提交
- [ ] 退款成功后刷新页面

**依赖任务**: Task-10, Task-11

**对应验收标准**: AC-R-10, AC-R-11, AC-R-12, AC-R-13

#### 步骤 12.1: 添加退款按钮 + 弹窗

**前置条件**: Task-11 完成

**执行动作**
在 MemberDetail.vue 添加：

```vue
<!-- 在"充值"按钮后新增"退款"按钮 -->
<n-button
    v-if="member && member.balance > 0"
    type="warning"
    @click="openRefundModal"
>
    退款
</n-button>

<!-- 退款弹窗 -->
<n-modal v-model:show="showRefundModal">
    <n-card title="会员退款" style="width: 450px">
        <n-alert type="info" :show-icon="false" class="mb-3">
            当前余额：¥{{ member?.balance?.toFixed(2) }}
        </n-alert>
        <n-form-item label="退款金额" required>
            <n-input-number
                v-model:value="refundAmount"
                :min="0.01"
                :max="member?.balance || 0"
                :precision="2"
                style="width: 100%"
            />
        </n-form-item>
        <n-form-item label="退款方式" required>
            <n-select
                v-model:value="refundPaymentMethod"
                :options="PAYMENT_OPTIONS"
            />
        </n-form-item>
        <n-form-item label="退款原因" required>
            <n-input
                v-model:value="refundRemark"
                type="textarea"
                placeholder="必填，至少 5 个字符"
                :rows="2"
            />
        </n-form-item>
        <template #footer>
            <n-space justify="end">
                <n-button @click="showRefundModal = false">取消</n-button>
                <n-button
                    type="warning"
                    :loading="refundLoading"
                    :disabled="!canRefund"
                    @click="handleRefund"
                >
                    确认退款
                </n-button>
            </n-space>
        </template>
    </n-card>
</n-modal>
```

```typescript
// script 新增
const showRefundModal = ref(false)
const refundAmount = ref(0)
const refundPaymentMethod = ref('cash')
const refundRemark = ref('')
const refundLoading = ref(false)

const canRefund = computed(() =>
    refundAmount.value > 0
    && refundAmount.value <= (member.value?.balance || 0)
    && refundRemark.value.trim().length >= 5
)

async function openRefundModal() {
    // 默认退款金额 = 当前余额
    refundAmount.value = member.value?.balance || 0
    // 默认支付方式 = 最近一次充值方式
    try {
        const lastMethod = await getMemberLastPaymentMethod(memberId.value)
        if (lastMethod) {
            refundPaymentMethod.value = lastMethod
        }
    } catch (e) {
        // 忽略，使用默认 cash
    }
    refundRemark.value = ''
    showRefundModal.value = true
}

async function handleRefund() {
    if (!canRefund.value) {
        message.warning('请检查退款金额和备注')
        return
    }
    refundLoading.value = true
    try {
        const result = await refundMemberBalance({
            memberId: memberId.value,
            amount: refundAmount.value,
            paymentMethod: refundPaymentMethod.value,
            operator: '当前店员',
            remark: refundRemark.value
        })
        message.success(`退款成功，剩余余额 ¥${result.newBalance.toFixed(2)}`)
        // 刷新
        const detail = await memberStore.getMemberDetailById(memberId.value)
        member.value = detail.member
        showRefundModal.value = false
    } catch (e: any) {
        message.error(`退款失败: ${e}`)
    } finally {
        refundLoading.value = false
    }
}
```

**预期结果**:
- 退款按钮在余额 > 0 时显示
- 退款弹窗默认金额/支付方式正确
- 退款成功后刷新页面

**验证方法**: 启动应用，测试退款流程

**回滚方案**: 删除退款相关代码

**输出物**: `MemberDetail.vue` 新增约 100 行

---

### Task-13: 新增 MemberDetail.vue 储值流水标签页

**任务说明**
在 MemberDetail.vue 用 n-tabs 包装"消费记录"和"储值流水"两个标签页。

**涉及文件**: `src/views/members/MemberDetail.vue`

**验证标准**
- [ ] 储值流水表格显示所有变动记录
- [ ] 变动金额正负号显示
- [ ] 变动类型有颜色标签
- [ ] 支持按类型筛选

**依赖任务**: Task-12

**对应验收标准**: AC-R-14, AC-R-15

#### 步骤 13.1: 重构为 n-tabs 布局 + 新增储值流水表格

**前置条件**: Task-12 完成

**执行动作**
将原"消费记录"卡片改为 n-tabs：

```vue
<n-card class="mb-4">
    <n-tabs v-model:value="activeTab" type="line">
        <n-tab-pane name="consumption" tab="消费记录">
            <n-data-table :columns="consumptionColumns" :data="consumptions" />
        </n-tab-pane>
        <n-tab-pane name="balance" tab="储值流水">
            <!-- 筛选器 -->
            <n-space class="mb-3">
                <n-select
                    v-model:value="balanceLogFilter"
                    :options="BALANCE_LOG_FILTER_OPTIONS"
                    clearable
                    placeholder="全部类型"
                    style="width: 160px"
                    @update:value="loadBalanceLogs"
                />
            </n-space>
            <!-- 流水表格 -->
            <n-data-table
                :columns="balanceLogColumns"
                :data="balanceLogs"
                :loading="balanceLogLoading"
                :pagination="balanceLogPagination"
                @update:page="handleBalanceLogPageChange"
            />
        </n-tab-pane>
    </n-tabs>
</n-card>
```

```typescript
// script 新增
const activeTab = ref('consumption')
const balanceLogFilter = ref<string | null>(null)
const balanceLogs = ref<BalanceLog[]>([])
const balanceLogLoading = ref(false)
const balanceLogPagination = reactive({
    page: 1, pageSize: 20, total: 0
})

const BALANCE_LOG_FILTER_OPTIONS = [
    { label: '充值', value: 'recharge' },
    { label: '扣款', value: 'consume' },
    { label: '退款', value: 'refund' }
]

const balanceLogColumns = [
    {
        title: '变动类型',
        key: 'changeType',
        width: 100,
        render: (row: BalanceLog) => h(
            NTag, { type: getChangeTypeColor(row.changeType) as any, size: 'small' },
            { default: () => getChangeTypeLabel(row.changeType) }
        )
    },
    {
        title: '变动金额',
        key: 'changeAmount',
        width: 120,
        render: (row: BalanceLog) => h(
            'span',
            { class: row.changeAmount > 0 ? 'text-green-600' : 'text-red-600' },
            { default: () => `${row.changeAmount > 0 ? '+' : ''}¥${row.changeAmount.toFixed(2)}` }
        )
    },
    { title: '支付方式', key: 'paymentMethod', width: 100 },
    { title: '变动后余额', key: 'balanceAfter', width: 120,
      render: (row: BalanceLog) => `¥${row.balanceAfter.toFixed(2)}` },
    { title: '操作人', key: 'operator', width: 100 },
    { title: '关联订单', key: 'relatedOrderId', width: 150 },
    { title: '备注', key: 'remark', width: 200 },
    { title: '时间', key: 'createdAt', width: 160 }
]

function getChangeTypeColor(type: string): string {
    return { recharge: 'success', consume: 'warning', refund: 'error' }[type] || 'default'
}
function getChangeTypeLabel(type: string): string {
    return { recharge: '充值', consume: '扣款', refund: '退款' }[type] || type
}

async function loadBalanceLogs() {
    balanceLogLoading.value = true
    try {
        const result = await getMemberBalanceLogs(
            memberId.value,
            balanceLogPagination.page,
            balanceLogPagination.pageSize,
            balanceLogFilter.value || undefined
        )
        balanceLogs.value = result.list
        balanceLogPagination.total = result.total
    } catch (e) {
        message.error('加载流水失败')
    } finally {
        balanceLogLoading.value = false
    }
}

function handleBalanceLogPageChange(page: number) {
    balanceLogPagination.page = page
    loadBalanceLogs()
}

// onMounted 中调用 loadBalanceLogs()
```

**预期结果**:
- 标签页切换正常
- 储值流水表格正确显示
- 筛选/分页功能正常

**验证方法**: 启动应用，切换标签页测试

**回滚方案**: 还原原消费记录卡片

**输出物**: `MemberDetail.vue` 新增约 120 行

---

### Task-14: 收银台余额扣款校验

**任务说明**
在 `src/views/sales/index.vue` 收银台结算前增加余额校验（会员未识别/余额不足）。

**涉及文件**: `src/views/sales/index.vue`

**验证标准**
- [ ] 选 memberBalance 但未识别会员时提示
- [ ] 余额不足时友好提示

**依赖任务**: Task-09

**对应验收标准**: AC-R-06

#### 步骤 14.1: 增加 validatePayMethod 函数

**前置条件**: Task-09 完成

**执行动作**
修改 `src/views/sales/index.vue`：

```typescript
// 在 handleCheckout 之前新增
function validatePayMethod(): boolean {
    if (selectedPayMethod.value === 'memberBalance') {
        if (!currentMember.value) {
            message.warning('请先识别会员')
            return false
        }
        if ((currentMember.value.balance || 0) < finalAmount.value) {
            message.warning(
                `会员余额不足，当前余额 ¥${(currentMember.value.balance || 0).toFixed(2)}，` +
                `需要支付 ¥${finalAmount.value.toFixed(2)}`
            )
            return false
        }
    }
    return true
}

// 修改 handleCheckout 入口
async function handleCheckout() {
    if (!validatePayMethod()) return
    // ... 原逻辑
}
```

**预期结果**:
- 选 memberBalance 但未识别会员 → 提示
- 余额不足 → 提示并阻止结算

**验证方法**: 启动应用，测试余额不足场景

**回滚方案**: 删除 validatePayMethod

**输出物**: `sales/index.vue` 新增约 20 行

---

### Task-15: 编写单元测试（8 个测试用例）

**任务说明**
在 `commands/members.rs` 末尾新增 `#[cfg(test)] mod tests`，覆盖 8 个场景。

**涉及文件**: `src-tauri/src/commands/members.rs`

**验证标准**
- [ ] 所有测试通过（8/8）

**依赖任务**: Task-05 ~ 09

**对应验收标准**: 全部 AC

#### 步骤 15.1: 新增测试模块

**前置条件**: Task-05 ~ 09 完成

**执行动作**
在 `commands/members.rs` 末尾新增：

```rust
#[cfg(test)]
mod tests {
    use super::*;
    use crate::db::{run_migrations, Database};
    use rusqlite::Connection;

    fn setup_test_db() -> Database {
        let conn = Connection::open_in_memory().expect("无法打开内存数据库");
        conn.execute_batch("PRAGMA foreign_keys = ON;")
            .expect("无法启用外键约束");
        run_migrations(&conn).expect("运行迁移失败");
        Database::new_for_test(conn)
    }

    fn insert_test_member(conn: &Connection, id: &str, name: &str, balance: f64) {
        conn.execute(
            "INSERT INTO members (id, name, phone, level, balance, is_active, created_at, updated_at)
             VALUES (?, ?, ?, 'normal', ?, 1, datetime('now'), datetime('now'))",
            rusqlite::params![id, name, format!("138{}", id), balance],
        ).expect("插入测试会员失败");
    }

    /// 测试 1: 充值成功
    #[test]
    fn test_recharge_success() {
        let db = setup_test_db();
        let conn = db.get_conn().unwrap();
        insert_test_member(&conn, "m1", "张三", 100.0);

        let input = RechargeInput {
            member_id: "m1".to_string(),
            amount: 200.0,
            payment_method: "cash".to_string(),
            operator: "tester".to_string(),
            remark: Some("测试充值".to_string()),
            bonus_amount: 0.0,
        };
        let result = recharge_member_balance_impl(&conn, input).expect("充值失败");

        assert_eq!(result.new_balance, 300.0);
        let count: i64 = conn.query_row(
            "SELECT COUNT(*) FROM member_balance_logs WHERE member_id = 'm1' AND change_type = 'recharge'",
            [], |row| row.get(0),
        ).unwrap();
        assert_eq!(count, 1);
    }

    /// 测试 2: 充值金额 ≤ 0
    #[test]
    fn test_recharge_invalid_amount() {
        let db = setup_test_db();
        let conn = db.get_conn().unwrap();
        insert_test_member(&conn, "m1", "张三", 100.0);

        let input = RechargeInput {
            member_id: "m1".to_string(),
            amount: 0.0,
            payment_method: "cash".to_string(),
            operator: "tester".to_string(),
            remark: None,
            bonus_amount: 0.0,
        };
        let result = recharge_member_balance_impl(&conn, input);
        assert!(result.is_err());
        assert!(result.unwrap_err().contains("大于 0"));
    }

    /// 测试 3: 退款成功
    #[test]
    fn test_refund_success() {
        let db = setup_test_db();
        let conn = db.get_conn().unwrap();
        insert_test_member(&conn, "m1", "张三", 200.0);

        let input = RefundInput {
            member_id: "m1".to_string(),
            amount: 200.0,
            payment_method: "wechat".to_string(),
            operator: "tester".to_string(),
            remark: "会员申请退款".to_string(),
        };
        let result = refund_member_balance_impl(&conn, input).expect("退款失败");

        assert_eq!(result.new_balance, 0.0);
    }

    /// 测试 4: 退款金额超过余额
    #[test]
    fn test_refund_exceed_balance() {
        let db = setup_test_db();
        let conn = db.get_conn().unwrap();
        insert_test_member(&conn, "m1", "张三", 100.0);

        let input = RefundInput {
            member_id: "m1".to_string(),
            amount: 200.0,
            payment_method: "cash".to_string(),
            operator: "tester".to_string(),
            remark: "测试超额".to_string(),
        };
        let result = refund_member_balance_impl(&conn, input);
        assert!(result.is_err());
    }

    /// 测试 5: 退款备注 < 5 字符
    #[test]
    fn test_refund_short_remark() {
        let db = setup_test_db();
        let conn = db.get_conn().unwrap();
        insert_test_member(&conn, "m1", "张三", 100.0);

        let input = RefundInput {
            member_id: "m1".to_string(),
            amount: 50.0,
            payment_method: "cash".to_string(),
            operator: "tester".to_string(),
            remark: "短".to_string(),
        };
        let result = refund_member_balance_impl(&conn, input);
        assert!(result.is_err());
    }

    /// 测试 6+7: create_sale_order 余额扣款（在 sales.rs 中新增）
    /// ... 见 Task-09 配套测试

    /// 测试 8: 流水查询 + 筛选
    #[test]
    fn test_query_balance_logs_filter() {
        let db = setup_test_db();
        let conn = db.get_conn().unwrap();
        insert_test_member(&conn, "m1", "张三", 0.0);

        // 插入 2 条 recharge + 1 条 refund
        recharge_member_balance_impl(&conn, RechargeInput {
            member_id: "m1".to_string(), amount: 100.0, payment_method: "cash".to_string(),
            operator: "t".to_string(), remark: None, bonus_amount: 0.0,
        }).unwrap();
        recharge_member_balance_impl(&conn, RechargeInput {
            member_id: "m1".to_string(), amount: 50.0, payment_method: "wechat".to_string(),
            operator: "t".to_string(), remark: None, bonus_amount: 0.0,
        }).unwrap();
        refund_member_balance_impl(&conn, RefundInput {
            member_id: "m1".to_string(), amount: 30.0, payment_method: "cash".to_string(),
            operator: "t".to_string(), remark: "测试退款".to_string(),
        }).unwrap();

        // 全部查询
        let all = get_member_balance_logs_impl(&conn, "m1".to_string(), None, None, None).unwrap();
        assert_eq!(all.total, 3);

        // 仅 recharge
        let recharge_only = get_member_balance_logs_impl(
            &conn, "m1".to_string(), None, None, Some("recharge".to_string())
        ).unwrap();
        assert_eq!(recharge_only.total, 2);
    }
}
```

**预期结果**: `cargo test --lib` 全部通过（8/8）

**验证方法**:
```powershell
cd d:\AI\AI\开发\茶叶店管理\tea-manage\src-tauri
cargo test --lib
```

**回滚方案**: 删除 `mod tests`

**输出物**: `commands/members.rs` 末尾新增约 200 行

---

### Task-16: 更新版本号（强制）

**任务说明**
将版本号从 v0.3.0 升级到 v0.3.1，确保所有位置一致。

**涉及文件**
- [package.json](file:///d:/AI/AI/开发/茶叶店管理/tea-manage/package.json)
- [Cargo.toml](file:///d:/AI/AI/开发/茶叶店管理/tea-manage/src-tauri/Cargo.toml)
- [project_config.md](file:///d:/AI/AI/开发/茶叶店管理/specs/project_config.md)

**验证标准**
- [ ] package.json version = "0.3.1"
- [ ] Cargo.toml version = "0.3.1"
- [ ] project_config.md APP_VERSION 一致

**依赖任务**: Task-15

#### 步骤 16.1: 更新 package.json

```json
{
  "name": "tea-manage",
  "version": "0.3.1",  // 原 0.3.0 → 0.3.1
  ...
}
```

#### 步骤 16.2: 更新 Cargo.toml

```toml
[package]
name = "tea-manage"
version = "0.3.1"  # 原 0.3.0 → 0.3.1
```

#### 步骤 16.3: 更新 project_config.md

将"当前版本 v0.3.0"改为"当前版本 v0.3.1"。

**预期结果**: 三处版本号一致为 0.3.1

**回滚方案**: 还原为 0.3.0

**输出物**: 3 个文件版本号更新

---

### Task-17: 更新 changelog.json 和创建 v0.3.1.md

**任务说明**
为 v0.3.1 添加 changelog 条目，并创建发布文档。

**涉及文件**
- `src/data/changelog.json`（如存在）
- `specs/releases/v0.3.1.md`（新建）

**验证标准**
- [ ] changelog.json 包含 v0.3.1 条目
- [ ] v0.3.1.md 创建并包含完整变更说明

**依赖任务**: Task-16

#### 步骤 17.1: changelog.json 新增条目

```json
{
  "version": "0.3.1",
  "date": "2026-07-02",
  "type": "minor",
  "title": "会员储值余额功能",
  "changes": [
    "feat: 会员储值充值（现金/微信/支付宝）",
    "feat: 收银台余额扣款（pay_method=memberBalance）",
    "feat: 会员退款（退剩余余额到原支付方式）",
    "feat: 储值流水查询与筛选",
    "feat: 数据库 schema 升级到 v4，新增 member_balance_logs 表"
  ]
}
```

#### 步骤 17.2: 创建 specs/releases/v0.3.1.md

```markdown
# v0.3.1 发布文档

> **版本号**：v0.3.1
> **发布日期**：2026-07-02
> **版本类型**：minor（新增功能 + 数据库 schema 升级）

## 变更摘要

### 新增
- 会员储值余额（充值/扣款/退款/流水）
- 收银台"会员卡"支付方式
- 数据库 schema 升级到 v4

## 数据库变更
- SCHEMA_VERSION 3 → 4
- 新增 member_balance_logs 表
- 新增 4 个索引

## 验证清单
- AC-R-01 ~ AC-R-16 全部通过
- 8 个单元测试通过
```

**预期结果**: 文档创建完成

**回滚方案**: 删除 v0.3.1.md

**输出物**:
- `changelog.json` 新增 1 个条目
- `specs/releases/v0.3.1.md`（约 50 行）

---

## 四、依赖关系图

```
Task-01 (迁移脚本)
  ↓
Task-02 (升级 SCHEMA_VERSION)
  ↓
Task-03 (新增模型)
  ↓
Task-04 (新建 members.rs 模块)
  ↓
Task-05 (recharge) ──→ Task-06 (refund) ──→ Task-07 (getLogs) ──→ Task-08 (getLastMethod)
                                                          ↓
                                                    Task-09 (改造 create_sale_order)
                                                          ↓
                                                    Task-10 (前端 API)
                                                          ↓
                                                    Task-11 (充值弹窗) ──→ Task-12 (退款弹窗) ──→ Task-13 (流水标签页)
                                                                                                            ↓
                                                                                                      Task-14 (收银台校验)
                                                                                                            ↓
                                                                                                      Task-15 (单元测试)
                                                                                                            ↓
                                                                                                      Task-16 (版本号)
                                                                                                            ↓
                                                                                                      Task-17 (changelog + 发布文档)
```

---

## 五、验收标准映射

| AC 编号 | 对应任务 |
|---------|----------|
| AC-R-01 | Task-11 |
| AC-R-02 | Task-05, Task-11 |
| AC-R-03 | Task-05 |
| AC-R-04 | Task-05, Task-12 |
| AC-R-05 | Task-09, Task-14 |
| AC-R-06 | Task-09, Task-14 |
| AC-R-07 | Task-09（不需特殊处理） |
| AC-R-08 | Task-09（actual_amount 计算） |
| AC-R-09 | Task-09（事务） |
| AC-R-10 | Task-08, Task-12 |
| AC-R-11 | Task-06, Task-12 |
| AC-R-12 | Task-06 |
| AC-R-13 | Task-06 |
| AC-R-14 | Task-07, Task-13 |
| AC-R-15 | Task-07, Task-13 |
| AC-R-16 | Task-11, Task-12, Task-13, Task-14 |

---

## 六、风险提示

| 风险 | 影响 | 应对措施 |
|------|------|---------|
| create_sale_order 改造影响现有逻辑 | 高 | 完整单元测试覆盖（Task-15 测试 6+7），事务回滚保护 |
| 退款时找不到最近一次充值方式 | 低 | 默认 cash，让用户手动调整 |
| 流水表快速膨胀 | 低 | 已建 4 个索引，查询 < 100ms；归档策略后续版本考虑 |
| 多端支付方式扩展 | 中 | 预留 `payment_method` 字段，未来增加 `card`/`mixed` 等 |
| 余额扣款积分计算边界 | 中 | actual_amount 已包含所有抵扣，积分计算代码不动 |

---

## 七、执行检查清单

- [ ] 已阅读需求文档（M06-储值余额功能.md）
- [ ] 已阅读技术方案（M06-储值余额功能_技术方案.md）
- [ ] 已检查前置条件
- [ ] 已准备好回滚方案
- [ ] 已了解验证方法
- [ ] 已确认依赖任务顺序

---

## 八、下一阶段

进入 [feature-implementation](file:///d:/AI/AI/开发/茶叶店管理/.trae/skills/feature-implementation/SKILL.md) 阶段：使用 TDD 流程逐任务实现。
