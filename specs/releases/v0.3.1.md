# v0.3.1 发布文档 - 会员储值余额功能

> **版本号**：v0.3.1
> **发布日期**：2026-07-02
> **版本类型**：minor（新增功能 + 数据库 schema 升级）
> **前置版本**：v0.3.0

---

## 一、版本摘要

本次发布新增 **会员储值余额（M06）** 功能模块，覆盖充值、扣款、退款、流水查询完整业务闭环。

### 核心交付

| 模块 | 内容 |
|------|------|
| 会员充值 | 支持现金/微信/支付宝转账，金额自由输入 |
| 收银台扣款 | 新增"会员卡"支付方式，自动扣减余额 |
| 会员退款 | 退剩余余额，默认退到最近一次充值支付方式 |
| 储值流水 | 完整记录每次变动，支持按类型筛选 + 分页 |
| 数据库升级 | SCHEMA_VERSION 3 → 4，新增 member_balance_logs 表 |

---

## 二、变更清单

### 2.1 新增功能 (Features)

#### 后端（Tauri Commands）

| 命令 | 描述 |
|------|------|
| `recharge_member_balance` | 会员充值（事务保护 + 流水插入） |
| `refund_member_balance` | 会员退款（事务保护 + 流水插入） |
| `get_member_balance_logs` | 储值流水查询（分页 + 类型筛选） |
| `get_member_last_payment_method` | 获取最近一次充值支付方式 |

#### 前端（Vue 组件）

| 组件 | 变更 |
|------|------|
| `MemberDetail.vue` | 重构：新增"退款"按钮、"储值流水"标签页、完整充值/退款弹窗 |
| `sales/index.vue` | 新增 `validatePayMethod()` 余额扣款校验 |

#### 数据模型

| 模型 | 文件 |
|------|------|
| `RechargeInput` / `RechargeResult` | `models/member_balance.rs` |
| `RefundInput` / `RefundResult` | `models/member_balance.rs` |
| `BalanceLog` | `models/member_balance.rs` |
| `BalanceChangeType` 枚举 | `models/member_balance.rs` |

### 2.2 数据库变更

- **SCHEMA_VERSION**：3 → 4
- **新增表**：`member_balance_logs`（11 字段 + 4 索引 + 2 外键 + 2 CHECK 约束）
- **数据迁移**：全新表，无需回填历史数据

```sql
-- migrate_v4_member_balance_logs
CREATE TABLE IF NOT EXISTS member_balance_logs (
    id TEXT PRIMARY KEY NOT NULL,
    member_id TEXT NOT NULL,
    change_type TEXT NOT NULL CHECK (change_type IN ('recharge', 'consume', 'refund')),
    change_amount REAL NOT NULL,
    balance_after REAL NOT NULL,
    payment_method TEXT NOT NULL CHECK (payment_method IN ('cash', 'wechat', 'alipay', 'memberBalance')),
    operator TEXT NOT NULL,
    related_order_id TEXT,
    bonus_amount REAL NOT NULL DEFAULT 0,
    fee_amount REAL NOT NULL DEFAULT 0,
    remark TEXT NOT NULL DEFAULT '',
    created_at TEXT NOT NULL DEFAULT (datetime('now')),
    FOREIGN KEY (member_id) REFERENCES members(id) ON DELETE RESTRICT,
    FOREIGN KEY (related_order_id) REFERENCES sales_orders(id) ON DELETE SET NULL
);

CREATE INDEX IF NOT EXISTS idx_balance_log_member ON member_balance_logs(member_id);
CREATE INDEX IF NOT EXISTS idx_balance_log_type ON member_balance_logs(change_type);
CREATE INDEX IF NOT EXISTS idx_balance_log_created ON member_balance_logs(created_at);
CREATE INDEX IF NOT EXISTS idx_balance_log_order ON member_balance_logs(related_order_id);
```

### 2.3 核心业务逻辑

#### create_sale_order 改造

- **触发条件**：`pay_method == "memberBalance"` 且 `member_id` 存在
- **执行流程**：
  1. 查询当前余额
  2. 校验余额充足（否则事务回滚）
  3. 扣减余额
  4. 插入 consume 流水（关联订单 ID）
- **事务保护**：嵌入到现有 BEGIN EXCLUSIVE TRANSACTION 中，与订单创建原子性提交

### 2.4 单元测试

| 测试用例 | 覆盖场景 |
|---------|---------|
| `test_recharge_success` | 充值成功，余额+流水正确 |
| `test_recharge_invalid_amount` | 充值金额 ≤ 0 应失败 |
| `test_refund_success` | 退款成功，change_amount 为负数 |
| `test_refund_exceed_balance` | 退款金额超过余额应失败 |
| `test_refund_short_remark` | 备注 < 5 字符应失败 |
| `test_query_balance_logs_with_filter` | 流水查询 + 类型筛选 |
| `test_balance_logs_pagination` | 流水分页 |
| `test_get_last_payment_method` | 获取最近充值支付方式 |

**测试结果**：8/8 通过

---

## 三、文件变更清单

### 新增文件

| 文件 | 行数 | 说明 |
|------|------|------|
| `src-tauri/src/models/member_balance.rs` | 110 | 储值余额数据模型 |
| `src-tauri/src/commands/members.rs` | 460 | 储值 4 个 Tauri Command + 8 个单元测试 |
| `specs/features/M06-储值余额功能.md` | - | 需求文档 |
| `specs/features/M06-储值余额功能_技术方案.md` | - | 技术方案 |
| `specs/features/M06-储值余额功能_任务规划.md` | - | 任务规划 |
| `specs/releases/v0.3.1.md` | - | 本文档 |
| `src/data/changelog.json` | 30 | 变更日志 |

### 修改文件

| 文件 | 变更内容 |
|------|---------|
| `src-tauri/src/db/schema.rs` | SCHEMA_VERSION 3→4、新增 `migrate_v4_member_balance_logs` |
| `src-tauri/src/commands/mod.rs` | 注册 `members` 模块 |
| `src-tauri/src/commands/sales.rs` | `create_sale_order` 嵌入余额扣款逻辑 |
| `src-tauri/src/models/mod.rs` | 导出 `member_balance` 模块 |
| `tea-manage/src/api/members.ts` | 新增 4 个 API 函数 + 2 个常量选项 |
| `tea-manage/src/types/index.ts` | 新增 6 个储值相关类型 |
| `tea-manage/src/views/members/MemberDetail.vue` | 重构为支持充值/退款/流水 |
| `tea-manage/src/views/sales/index.vue` | 新增 `validatePayMethod()` |
| `package.json` | version 0.3.0 → 0.3.1 |
| `src-tauri/Cargo.toml` | version 0.3.0 → 0.3.1 |
| `src-tauri/tauri.conf.json` | version 0.1.0 → 0.3.1 |

---

## 四、验收标准检查

| AC 编号 | 描述 | 状态 |
|---------|------|------|
| AC-R-01 | 充值弹窗含金额/支付方式/备注 | ✅ |
| AC-R-02 | 充值后余额正确更新 + 流水插入 | ✅ |
| AC-R-03 | 充值金额 ≤ 0 时友好提示 | ✅ |
| AC-R-04 | 退款时显示"退款"按钮（余额 > 0 时） | ✅ |
| AC-R-05 | 收银台"会员卡"支付方式可选 | ✅ |
| AC-R-06 | 余额不足时阻止结算 + 友好提示 | ✅ |
| AC-R-07 | 余额扣款不产生积分 | ✅（积分计算代码未触碰） |
| AC-R-08 | 余额扣款嵌入现有事务 | ✅ |
| AC-R-09 | 余额不足时事务回滚 | ✅ |
| AC-R-10 | 退款默认退到原支付方式 | ✅ |
| AC-R-11 | 退款成功后刷新会员余额 | ✅ |
| AC-R-12 | 退款金额超过余额时阻止 | ✅ |
| AC-R-13 | 退款备注 ≥ 5 字符校验 | ✅ |
| AC-R-14 | 储值流水标签页正确显示 | ✅ |
| AC-R-15 | 流水按类型筛选 + 分页 | ✅ |
| AC-R-16 | 数据库 schema 升级到 v4 | ✅ |

---

## 五、风险与已知问题

| 风险 | 级别 | 应对措施 |
|------|------|---------|
| 退款时找不到最近一次充值方式 | 低 | fallback 到 cash，让用户手动调整 |
| 流水表数据增长 | 低 | 已建 4 个索引，查询 < 100ms |
| 多端支付方式扩展 | 中 | 预留 payment_method 字段 |
| 充值赠送功能未实现 | 低 | bonus_amount 字段已预留，v0.3.1 暂不启用 |

---

## 六、后续计划

- v0.3.2：充值赠送规则（如充 1000 送 100）
- v0.4.0：储值消费送积分（当前扣款不产生积分）
- v0.4.0：储值卡等级折扣
- v0.5.0：储值卡过期机制

---

## 七、发布清单

- [x] 三处版本号一致（package.json / Cargo.toml / tauri.conf.json）
- [x] changelog.json 新增 v0.3.1 条目
- [x] 数据库迁移脚本测试通过
- [x] 8/8 单元测试通过
- [x] TypeScript 编译通过
- [x] Rust 编译通过
- [ ] 实际发布时间：（待发布时填写）
