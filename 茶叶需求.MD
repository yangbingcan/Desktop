这是一份为你量身定制的**茶叶店管理系统需求规格说明书（精简版 PRD）**，聚焦你列出的 7 个核心重点，并融入了茶叶行业的特殊业务逻辑。

---

# 茶叶店管理系统 需求规格说明书（PRD）

## 一、项目概述

| 项目 | 说明 |
|:---|:---|
| **系统名称** | 茶易管（TeaManage） |
| **目标用户** | 中小型茶叶零售店、茶庄、茶空间 |
| **应用形态** | Windows/macOS 桌面端（EXE/MSI），离线单机或局域网部署 |
| **技术栈** | 前端：Vue 3 + Element Plus；桌面壳：Tauri 2.x；后端逻辑：Rust（Tauri Command）；数据库：SQLite（本地嵌入式） |
| **核心目标** | 轻量（安装包 < 10MB）、稳定、贴合茶叶行业"多单位/散茶/口味偏好"的特殊需求 |

---

## 二、核心功能模块详细需求

### 模块 1：商品档案（商品录入）

**业务背景**：茶叶既有按"克/斤"卖的散茶，也有按"罐/盒/件"卖的礼盒，还有茶具等计件商品，必须支持灵活的多规格定义。

#### 1.1 基础字段
| 字段 | 说明 | 示例 |
|:---|:---|:---|
| 商品编码 | 系统自动生成（SP+年月日+序号）或手动输入 | SP20260101001 |
| 商品名称 | 中文名 | 牛栏坑肉桂 |
| 商品分类 | 一级：茶类（青茶/红茶/普洱…）；二级：细分 | 青茶 → 武夷岩茶 |
| 商品类型 | **称重类 / 计件类**（决定库存计算方式） | 称重类 |
| 产地 / 年份 / 等级 | 自由文本 | 武夷山 / 2023春 / 特级 |
| 发酵程度 / 焙火程度 | 下拉选择 | 中足火 |
| 商品图片 | 支持上传1张主图 | — |

#### 1.2 多单位与价格定义（核心）
每个商品必须设定一个**基准单位**（称重类固定为"克/g"，计件类固定为"个/件"），然后可添加多个**销售单位**及换算关系：

| 单位名称 | 换算为基准单位 | 零售价（元） | 会员价（元） |
|:---|:---|:---|:---|
| 克（基准） | 1 g | 0.8 | 0.75 |
| 两（50g） | 50 g | 40 | 37 |
| 斤（500g） | 500 g | 380 | 350 |
| 罐（250g） | 250 g | 200 | 180 |
| 件（20罐） | 5000 g | 3800 | 3600 |

> **规则**：前台销售时按销售单位展示和计价，但底层库存全部折算为基准单位（克/个）累加或扣减。

---

### 模块 2：出入库单管理

#### 2.1 采购入库单
- **流程**：选择供应商 → 添加商品（扫码/搜索）→ 选择入库单位（如"件"）→ 输入数量 → 系统自动按换算关系算出**增加的总克数** → 填入进价 → 保存/提交。
- **批次号**：每笔入库自动生成批次号（RK+日期+序号），用于追踪每批茶的成本和保质期。
- **字段**：单据编号、日期、供应商、经手人、商品明细（含批次、单位、数量、进价、金额）、备注、总计金额、付款状态（未付/部分/已付）。

#### 2.2 其他出入库
- **退货出库**（退给供应商）、**报损出库**（茶叶变质）、**盘点调整单**（盘盈/盘亏）。
- 所有单据提交后**不可逆地修改库存基准数量**，并生成库存流水记录。

---

### 模块 3：简单收银（零售开单）

#### 3.1 收银界面布局
```
┌──────────────────────────────────────┐
│ [扫码区/搜索框]   [会员识别按钮]      │
├───────────────────┬──────────────────┤
│                   │  购物清单         │
│  商品快捷按钮区   │  铁观音 50g ×2   │
│  （岩茶/普洱/绿） │  大红袍 1罐      │
│                   │ ─────────────── │
│                   │  合计：¥ 580     │
│                   │  [挂单] [结算]   │
└───────────────────┴──────────────────┘
```

#### 3.2 核心功能
- **商品录入**：扫码枪扫描商品条码 / 手动搜索（支持拼音首字母，如"NLKRG"→牛栏坑肉桂）/ 触屏点击快捷按钮。
- **会员识别**：输入手机号后4位或刷会员码，自动带出会员姓名、折扣率、积分余额。
- **数量/重量修改**：称重类商品可直接输入克数，系统实时计算金额。
- **支付方式**：现金（带找零计算）、微信/支付宝（手动确认到账）、会员余额扣款、组合支付。
- **小票打印**：结算成功后自动调用打印模板打印顾客联。

---

### 模块 4：库存增加减少逻辑与单位换算（核心稳固逻辑）

> ⚠️ **这是整个系统最关键的算法层，必须在需求阶段锁死规则，防止后期库存出现负数或错乱。**

#### 4.1 底层存储规则
- **称重类商品**：库存表只存一个字段 `stock_grams`（整数，单位：克）。所有入库、出库、盘点，最终都转化为"克"的增减。
- **计件类商品**：库存表存 `stock_units`（整数，单位：个）。

#### 4.2 入库换算逻辑
```
用户操作：采购入库"牛栏坑肉桂" 2件
↓
系统读取换算关系：1件 = 20罐，1罐 = 250g → 1件 = 5000g
↓
实际入库克数 = 2 × 5000 = 10000g
↓
UPDATE products SET stock_grams = stock_grams + 10000 WHERE id = xxx
↓
同时写入 inventory_batches 表（批次号、入库价、剩余克数）
```

#### 4.3 出库（销售/报损）换算逻辑
```
用户操作：零售卖出"牛栏坑肉桂" 半斤（250g）
↓
校验：SELECT stock_grams FROM products WHERE id = xxx → 假设当前 8000g
↓
校验通过（8000 >= 250）
↓
扣减：UPDATE products SET stock_grams = stock_grams - 250
↓
批次扣减：按 FIFO（先进先出）从最早批次的 remaining_grams 中扣除 250g
↓
写入 stock_flow 流水表（类型=销售出库，变更=-250g，结余=7750g）
```

#### 4.4 库存显示逻辑
- 前台展示时，根据商品设置的"默认销售单位"反向换算显示：
  - 库存 7750g ÷ 5000g/件 = **1件 + 2750g（即5罐+1两+50g）**
  - 界面可配置显示为"约 X 件 X 罐 X 克"或仅显示"7750g"

#### 4.5 防超卖保护
- 销售下单时加**数据库行级锁**（SQLite 的 BEGIN EXCLUSIVE TRANSACTION），防止并发操作导致库存扣成负数。
- 若库存不足，前端弹窗提示"当前库存仅剩 X g，是否继续？"由店员决定是否允许欠库存（可配置开关）。

---

### 模块 5：会员管理（重点：会员档案与口味偏好）

#### 5.1 会员基础档案
| 字段 | 说明 |
|:---|:---|
| 姓名 / 昵称 | — |
| 手机号 | 唯一标识，用于快速检索 |
| 性别 / 生日 | 用于生日营销 |
| 会员等级 | 普通 / 银卡 / 金卡（不同等级不同折扣） |
| 积分余额 | 消费1元=1积分，积分可抵现（100积分=1元） |
| 储值余额 | 充值记录与消费扣款 |
| 累计消费金额 / 次数 | 系统自动统计 |

#### 5.2 口味偏好档案（核心差异化功能）
专门设计一张 `member_preferences` 表，记录：

| 偏好维度 | 类型 | 说明 |
|:---|:---|:---|
| 偏好茶类 | 多选标签 | 岩茶、单丛、熟普、白毫银针… |
| 口感倾向 | 多选标签 | 浓醇厚重 / 清香甘甜 / 苦后回甘 / 顺滑柔和 / 花果香… |
| 禁忌/不喝 | 文本备注 | "不喝生普"、"怕失眠晚上不喝茶" |
| 冲泡习惯 | 文本备注 | "喜欢100°C沸水"、"每次泡8g" |
| 消费场景 | 单选/多选 | 自饮 / 送礼 / 办公接待 |
| 常购清单 | 系统自动+人工标记 | 最近3个月买了什么、店员标记为"最爱"的商品 |
| 到店频率 | 系统计算 | 平均多少天来一次 |

> **应用场景**：会员到店或来电时，店员打开会员档案，一眼看到"张先生，喜欢足火岩茶，不喝生普，上次买了牛肉250g，可以推荐今年的新茶"。这是茶叶店做**私域复购**的核心武器。

#### 5.3 消费记录
- 每次零售结算后，自动关联会员ID，记录购买了哪些商品、克数、金额、积分变动。
- 会员端（未来对接小程序）可查看自己的消费流水。

---

### 模块 6：零售和出库打印模板预览

#### 6.1 零售小票模板（顾客联）
```
══════════════════════
       【茗香茶叶】      
    地址：XX路XX号      
    电话：138-XXXX-XXXX 
──────────────────────
商品           数量  金额
铁观音(50g)     2    ¥80
牛栏坑肉桂(罐)   1    ¥380
──────────────────────
合计：¥460.00
会员：张先生(积分+460)
支付：微信支付 ¥460.00
──────────────────────
  感谢惠顾，欢迎再次光临！
  扫码关注公众号领优惠券  
══════════════════════
```

#### 6.2 出库/入库单模板（内部留存）
- 包含：单据编号、日期、操作员、供应商/客户、商品明细（含批次号、单位、数量、单价、金额）、合计、签字栏。

#### 6.3 模板管理
- **模板编辑器**：Vue 前端实现所见即所得编辑，可拖拽调整字段位置、字体大小。
- **预览**：编辑时实时预览，调用浏览器打印预览（window.print）模拟效果。
- **实际打印**：通过 Tauri Command 调用 Rust 侧，将 HTML 渲染为 PDF 或直接发送 ESC/POS 指令给小票打印机。

---

### 模块 7：条码生成打印

#### 7.1 条码规则
- **零售商品条码**：`商品编码 + 销售单位编码`，如 `SP20260101001-250g`，生成 Code128 一维码。
- **批次追溯条码**：`商品编码 + 批次号`，生成二维码，扫码可查看产地、入库日期、焙火信息（未来对接小程序展示）。

#### 7.2 条码标签打印
- **标签设计**：设定标签纸张尺寸（如 40mm×30mm、60mm×40mm），选择内容（商品名、产地、重量、零售价、条码）。
- **批量打印**：勾选多个商品或多个批次，输入打印份数，一键批量输出到标签打印机。
- **单个打印**：收银时若顾客需要，可即时打印该商品的便携标签贴。

#### 7.3 技术实现
- 前端用 `JsBarcode`（一维码）/ `qrcode` 库生成图片。
- 将图片和文字排版为 HTML → 通过 Tauri 发送给标签打印机（调用 Rust 的 `printpdf` crate 或系统打印 API）。

---

## 三、非功能性需求

| 项目 | 要求 |
|:---|:---|
| 离线运行 | 全程无网可用，数据存本地 SQLite |
| 数据备份 | 一键备份 .db 文件到指定路径/U盘 |
| 操作响应 | 商品搜索 < 200ms（万级数据） |
| 安装包体积 | Tauri 打包 EXE < 10MB |
| 权限 | 店员（只能收银/查库存）、店长（全部权限+报表） |
| 数据安全 | SQLite 文件可设置密码加密（SQLCipher） |

---

## 四、核心数据表概要（供开发参考）

```
products          — 商品表（含基准单位、类型）
unit_conversions   — 单位换算关系表（商品ID、单位名、换算系数）
inventory_batches  — 库存批次表（商品ID、批次号、入库价、剩余克数）
stock_flow        — 库存流水表（单据ID、商品ID、变更克数、结余、类型）
sales_orders      — 销售单据头
sales_items       — 销售单据明细
purchase_orders   — 采购入库单头
purchase_items    — 采购入库明细
members           — 会员表
member_preferences — 会员口味偏好表
member_consumption — 会员消费记录
barcode_rules     — 条码规则配置
```
---