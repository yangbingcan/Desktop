# 功能验收报告 - M04 出入库管理

## 一、验收概览

| 项目 | 内容 |
|------|------|
| 功能名称 | M04 出入库管理（供应商档案 + 退货出库 + 采购入库完善） |
| 验收时间 | 2026-07-02 |
| 验收标准总数 | 17 |
| 通过数量 | 13 |
| 失败数量 | 4 |
| 验收结果 | ❌ 未完全通过（需修复 4 项后重新验收） |

---

## 二、验收详情

### 5.1 供应商档案（7/7 通过）

#### AC-S-01：可以新增供应商
- **状态**: ✅ 通过
- **验证方法**: 阅读后端 `create_supplier` Command（[suppliers.rs:160-206](file:///d:/AI/AI/开发/茶叶店管理/tea-manage/src-tauri/src/commands/suppliers.rs#L160-L206)）
- **证据**:
  - Tauri Command `create_supplier` 已实现
  - 输入校验：`validate_supplier_name` + `validate_phone`
  - UUID 主键、`is_active=1` 默认启用
  - 前端 `SupplierForm.vue` 提交调用此 Command
- **备注**: 无

#### AC-S-02：可以编辑供应商信息
- **状态**: ✅ 通过
- **验证方法**: 阅读后端 `update_supplier` Command
- **证据**: [suppliers.rs:210-258](file:///d:/AI/AI/开发/茶叶店管理/tea-manage/src-tauri/src/commands/suppliers.rs#L210-L258) 完整实现
- **备注**: 无

#### AC-S-03：可以软删除供应商
- **状态**: ✅ 通过
- **验证方法**: 阅读后端 `delete_supplier` Command
- **证据**: [suppliers.rs:260-300](file:///d:/AI/AI/开发/茶叶店管理/tea-manage/src-tauri/src/commands/suppliers.rs#L260-L300) `UPDATE suppliers SET is_active = 0`
- **备注**: 无

#### AC-S-04：已有进货记录的供应商不能删除
- **状态**: ✅ 通过
- **验证方法**: 阅读 `delete_supplier` 中的检查逻辑
- **证据**: [suppliers.rs:269-287](file:///d:/AI/AI/开发/茶叶店管理/tea-manage/src-tauri/src/commands/suppliers.rs#L269-L287) 检查 `purchase_orders` + `return_orders` 引用计数
- **备注**: 无

#### AC-S-05：供应商列表按名称/联系人搜索
- **状态**: ✅ 通过
- **验证方法**: 阅读 `get_suppliers` Command 关键词搜索
- **证据**: [suppliers.rs:55-81](file:///d:/AI/AI/开发/茶叶店管理/tea-manage/src-tauri/src/commands/suppliers.rs#L55-L81) `WHERE name LIKE ? OR contact_person LIKE ?`
- **备注**: 无

#### AC-S-06：采购入库时可选择供应商
- **状态**: ✅ 通过
- **验证方法**: 阅读 `PurchaseForm.vue` 中供应商下拉加载
- **证据**: [PurchaseForm.vue:251-258](file:///d:/AI/AI/开发/茶叶店管理/tea-manage/src/views/purchase/PurchaseForm.vue#L251-L258) `loadSupplierOptions` 调用 `supplierStore.loadActiveSuppliers`
- **备注**: 无

#### AC-S-07：退货出库时可选择供应商
- **状态**: ✅ 通过
- **验证方法**: 阅读 `ReturnForm.vue` 中供应商下拉加载
- **证据**: [ReturnForm.vue:174-181](file:///d:/AI/AI/开发/茶叶店管理/tea-manage/src/views/returns/ReturnForm.vue#L174-L181) `loadSuppliers` 调用 `supplierStore.loadActiveSuppliers`
- **备注**: 无

---

### 5.2 退货出库单（6/7 通过，1 项缺失）

#### AC-R-01：可以创建退货出库单
- **状态**: ✅ 通过
- **验证方法**: 阅读 `create_return_order` Command
- **证据**: [return_order.rs:97-281](file:///d:/AI/AI/开发/茶叶店管理/tea-manage/src-tauri/src/commands/return_order.rs#L97-L281) 完整事务流程
- **备注**: 无

#### AC-R-02：退货数量校验：原批次剩余 >= 退货克数
- **状态**: ✅ 通过
- **验证方法**: 阅读 `create_return_order` 中的库存校验
- **证据**: [return_order.rs:160-168](file:///d:/AI/AI/开发/茶叶店管理/tea-manage/src-tauri/src/commands/return_order.rs#L160-L168) `if remaining < grams { return Err(...) }`
- **备注**: 无

#### AC-R-03：退货后 `products.stock_grams` 正确扣减
- **状态**: ✅ 通过
- **验证方法**: 阅读 `create_return_order` 中商品库存扣减
- **证据**: [return_order.rs:177-180](file:///d:/AI/AI/开发/茶叶店管理/tea-manage/src-tauri/src/commands/return_order.rs#L177-L180) `UPDATE products SET stock_grams = stock_grams - ?`
- **备注**: 无

#### AC-R-04：退货后 `inventory_batches.remaining_grams` 正确扣减
- **状态**: ✅ 通过
- **验证方法**: 阅读 `create_return_order` 中批次扣减
- **证据**: [return_order.rs:171-174](file:///d:/AI/AI/开发/茶叶店管理/tea-manage/src-tauri/src/commands/return_order.rs#L171-L174) `UPDATE inventory_batches SET remaining_grams = remaining_grams - ?`
- **备注**: 无

#### AC-R-05：库存流水记录正确生成（`flow_type = 'return_out'`）
- **状态**: ✅ 通过
- **验证方法**: 阅读 `create_return_order` 中流水插入
- **证据**: [return_order.rs:191-205](file:///d:/AI/AI/开发/茶叶店管理/tea-manage/src-tauri/src/commands/return_order.rs#L191-L205) `INSERT INTO stock_flow ... flow_type='return_out'`
- **备注**: 无

#### AC-R-06：退货单按日期/供应商/退货原因筛选
- **状态**: ⚠️ 部分通过
- **验证方法**: 阅读 `get_return_orders` Command
- **证据**:
  - ✅ 供应商筛选：[return_order.rs:301-304](file:///d:/AI/AI/开发/茶叶店管理/tea-manage/src-tauri/src/commands/return_order.rs#L301-L304) `ro.supplier_id = ?`
  - ✅ 退货原因筛选：[return_order.rs:305-308](file:///d:/AI/AI/开发/茶叶店管理/tea-manage/src-tauri/src/commands/return_order.rs#L305-L308) `ro.return_reason = ?`
  - ❌ 日期范围筛选：**缺失**（`get_return_orders` 无 `return_date_start` / `return_date_end` 参数）
- **问题描述**: 后端 `get_return_orders` Command 不支持日期范围筛选
- **修复建议**:
  1. 在 `get_return_orders` 中增加 `date_start: Option<String>` 和 `date_end: Option<String>` 参数
  2. 构建 WHERE 子句增加 `ro.return_date >= ? AND ro.return_date <= ?`
  3. 前端 [ReturnList.vue](file:///d:/AI/AI/开发/茶叶店管理/tea-manage/src/views/returns/ReturnList.vue) 增加日期范围选择器

#### AC-R-07：退货单金额计算正确（数量 × 单价）
- **状态**: ✅ 通过
- **验证方法**: 阅读 `create_return_order` 中金额计算
- **证据**: [return_order.rs:208](file:///d:/AI/AI/开发/茶叶店管理/tea-manage/src-tauri/src/commands/return_order.rs#L208) `let subtotal = purchase_price * item.quantity as f64;`
- **备注**: 无

---

### 5.3 采购入库单完善（1/3 通过，2 项严重缺失）

#### AC-P-01：入库时强制选择供应商
- **状态**: ✅ 通过
- **验证方法**: 阅读 `PurchaseForm.vue` 的 `handleSubmit`
- **证据**: [PurchaseForm.vue:216-220](file:///d:/AI/AI/开发/茶叶店管理/tea-manage/src/views/purchase/PurchaseForm.vue#L216-L220) `if (!form.supplierId) { message.warning('请选择供应商'); return }`
- **备注**: 无

#### AC-P-02：入库单列表显示供应商名称
- **状态**: ❌ 失败
- **验证方法**: 阅读 `PurchaseList.vue` 和后端
- **问题描述**:
  1. **后端缺失 `get_purchase_orders` Command** — 在 `commands/` 目录下没有此 Command
  2. **数据库缺失 `purchase_orders` / `purchase_items` 表** — [schema.rs](file:///d:/AI/AI/开发/茶叶店管理/tea-manage/src-tauri/src/db/schema.rs) 中未定义这两张表
  3. **`purchase_in` 不写主单** — [inventory.rs:260-404](file:///d:/AI/AI/开发/茶叶店管理/tea-manage/src/views/purchase/PurchaseForm.vue) 中生成的 `order_id` 和 `order_no` 仅用于流水表的关联，**没有保存到任何单据表**
  4. **前端是占位** — [PurchaseList.vue:92-100](file:///d:/AI/AI/开发/茶叶店管理/tea-manage/src/views/purchase/PurchaseList.vue#L92-L100) `loadOrders` 直接 `orders.value = []`，并提示「采购入库单列表查询功能开发中」
- **修复建议**（涉及需求变更，建议作为 v0.3.0 任务）:
  1. 在 [schema.rs](file:///d:/AI/AI/开发/茶叶店管理/tea-manage/src-tauri/src/db/schema.rs) 中增加 `purchase_orders` 和 `purchase_items` 表（SCHEMA_VERSION 升级到 3）
  2. 在 [commands/](file:///d:/AI/AI/开发/茶叶店管理/tea-manage/src-tauri/src/commands/) 中新增 `get_purchase_orders` Command
  3. 修改 [inventory.rs:260](file:///d:/AI/AI/开发/茶叶店管理/tea-manage/src-tauri/src/commands/inventory.rs#L260) 的 `purchase_in` 函数，在事务中插入主单和明细
  4. 完善前端 `PurchaseList.vue`，调用 API 展示列表

#### AC-P-03：入库单按供应商筛选
- **状态**: ❌ 失败
- **验证方法**: 阅读 `PurchaseList.vue`
- **问题描述**: 同 AC-P-02，列表本身是占位，筛选条件 `filters.supplierId` 未传递给任何 API
- **修复建议**: 同 AC-P-02，需先实现列表 API 才能完成筛选

---

## 三、问题汇总

| 编号 | 验收标准 | 问题描述 | 严重程度 | 修复建议 |
|------|---------|---------|---------|--------|
| 1 | AC-R-06 | 退货单列表缺少日期范围筛选（后端 Command 不支持） | 中 | 增加 `date_start` / `date_end` 参数到 `get_return_orders` |
| 2 | AC-P-02 | 采购入库单列表完全不显示（数据库缺表 + 后端缺 Command + 前端是占位） | 高 | 需要 schema 升级 + 新增 Command + 补全前端 |
| 3 | AC-P-03 | 采购入库单按供应商筛选无法使用（同上） | 高 | 同 AC-P-02 |

---

## 四、结论

- [ ] 验收通过，功能完成
- [x] **部分通过，需要修复后重新验收**
- [ ] 验收失败，需要重新开发

### 4.1 通过项（13/17）

- 供应商档案 7/7 全部通过
- 退货出库单 6/7 通过（仅缺日期筛选）
- 采购入库供应商必填 1/3 通过

### 4.2 阻塞项（3 项需修复）

| 修复方向 | 涉及文件 | 工作量 |
|---------|---------|--------|
| 退货单日期筛选 | `return_order.rs` + `ReturnList.vue` | 小（1-2 小时） |
| 采购入库主单持久化 | `schema.rs` + `inventory.rs`（新增表 + 修改 purchase_in） | 中（半天） |
| 采购入库列表 API | `commands/inventory.rs`（新增 get_purchase_orders） + `PurchaseList.vue` | 中（半天） |

### 4.3 建议处理顺序

1. **优先**：退货单日期筛选（小改动，独立）— 半天内完成
2. **次优**：采购入库主单持久化 + 列表 API（涉及 schema 升级）— 建议并入 v0.3.0 一起处理

---

## 五、附加发现

### 5.1 架构性问题

- `purchase_in` 函数生成的 `order_id` 仅用于 `stock_flow.order_id` 关联，**没有任何主单表保存**。这导致采购入库后无法查询历史单据
- 数据库版本号 `SCHEMA_VERSION = 2` 在 v0.2.0 任务文档中提到，但 `purchase_orders` 表完全不存在 — 这是 v0.1.0 时期的遗留问题

### 5.2 建议

- v0.2.0 本期修复退货日期筛选（**独立、低风险**）
- v0.2.0 修复 OR 推迟到 v0.3.0：采购入库主单持久化（**需要新表、影响 v1 升级路径**）
- v0.3.0 补充单元测试覆盖

---

**报告生成时间**: 2026-07-02
**验收人**: Claude (Trae IDE)
