# 代码审核报告 — 茶易管（TeaManage）v0.5.5

> 审核方法：comprehensive-code-review（10 维度框架）· 全量审核（发版前门禁）
> 审核日期：2026-07-10
> 审核人：WorkBuddy（AI 辅助 + 2 个专项并行审查代理交叉验证）

## 一、审核元信息

| 项目 | 内容 |
|------|------|
| 项目名称 | 茶易管（TeaManage）茶叶店管理系统 |
| 应用形态 | 桌面端（Windows/macOS）Tauri 2 壳 |
| 技术栈 | Vue 3.5 + Vite 6 + Naive UI + UnoCSS + Pinia + Vue Router 4（前端）<br>Rust + Tauri 2 + rusqlite 0.32（bundled）+ SQLite 3（后端/数据） |
| 代码版本 | package.json / Cargo.toml / tauri.conf.json 均 = **v0.5.5** |
| 源码规模 | 31 个 `.vue` + 27 个 `.ts`（前端，约 4k 行） + 23 个 `.rs`（后端，约 8k 行） |
| 测试规模 | Rust `#[cfg(test)]` 单测（覆盖 products/sales/inventory/members/suppliers/return_order 等）<br>前端 vitest 单测（api/stores/utils/views）+ contract 测试 + Playwright e2e（inventory/product/sales 流程） |
| 审核范围 | 全量（tea-manage/src + tea-manage/src-tauri/src） |

> ⚠️ **重要说明**：项目文档（`specs/project_config.md` 写 v0.3.0 / Vite 5；`specs/目录结构.md`、`specs/代码规范.md` 描述 `services/`、`db/migrations/` 目录与独立 FIFO 服务）与实际代码**严重不符**。实际 Rust 结构为扁平式（commands / db / models 三层，无 services、无 migrations 目录）。本报告以**实际代码**为准。

---

## 二、审核摘要

| 严重程度 | 数量 | 占比 |
|---------|------|------|
| 🔴 严重（Critical） | 3 | 20% |
| 🟡 重要（Major） | 8 | 53% |
| 🟢 一般（Minor） | 4 | 27% |
| **合计** | **15** | **100%** |

**发版门禁结论：❌ 未通过**
- 整体质量评分 **6.5 / 10**（要求 ≥ 7.0）
- 存在 **3 个 🔴 严重问题**（要求 0 个）
- 必须修复 CR-001 / CR-002 / CR-003 后方可发布

---

## 三、维度评估

| 维度 | 评分(1-10) | 关键发现 |
|------|-----------|---------|
| 1. 代码完整性 | 6 | 设置模块整体为占位空实现（后端 3 命令未实现），仪表盘为硬编码 Mock 数据 |
| 2. 代码冗余 | 7 | FIFO 批次扣减逻辑在 `inventory.rs` 与 `sales.rs` 中重复实现 |
| 3. 异常处理 | 6 | 手动事务在 panic 路径不回滚（严重）；其余 catch 处理较好；生产 unwrap 极少 |
| 4. 业务逻辑 | 6 | 库存增减方向正确、FIFO 正确；但金额全程 f64，存在浮点舍入与积分截断风险 |
| 5. 架构设计 | 6 | 扁平结构合理但偏离文档；全局单 `Mutex<Connection>` 串行设计扩展性受限 |
| 6. 代码质量 | 7 | TS strict 开启、文件头注释普遍；但 `any` 滥用、文档漂移 |
| 7. 性能与安全 | 6 | 无 SQL 注入/XSS；WAL+外键已开启；但 `csp:null` 关闭内容安全策略 |
| 8. 可测试性 | 8 | Rust 与前端测试覆盖较完善（单测/契约/e2e），是本项目的显著优点 |
| 9. 前后端一致性 | 8 | 已实现功能的 invoke 命令与 `#[tauri::command]` 全部对齐；仅设置功能缺后端 |
| 10. 数据库专项 | 7 | Schema 设计良好（FK/CHECK/索引/版本化迁移）；金额用 REAL；缺 busy_timeout |

**加权公式得分** = 6×0.10 + 7×0.06 + 6×0.12 + 6×0.16 + 6×0.10 + 7×0.08 + 6×0.14 + 8×0.06 + 8×0.10 + 7×0.08 = **6.54 ≈ 6.5**

---

## 四、问题清单

### 🔴 严重问题（P0 — 阻塞发布）

| 编号 | 维度 | 文件路径 | 行号 | 问题描述 | 影响范围 | 修复建议 | 优先级 |
|------|------|---------|------|---------|---------|---------|-----------|
| CR-001 | D3/D10 | `src-tauri/src/db/mod.rs`、`commands/*.rs`（sales/return_order/inventory/members/products/suppliers/supplier_payments/dev_data） | mod.rs:14-16,49-52；sales.rs:188,191-194；各 BEGIN/COMMIT 块 | 全局仅一个 `Mutex<Connection>` 共享连接，所有写操作用**手动** `BEGIN EXCLUSIVE TRANSACTION` + `rollback_on_err` 闭包。回滚**仅**在显式 `Err` 返回时触发；一旦命令 **panic**（任何后续 `.unwrap()`、逻辑错误、序列化失败），回滚闭包不执行，EXCLUSIVE 事务悬挂在共享连接上 → 后续所有命令报 "cannot start a transaction within a transaction" / "database is locked"，**直至重启**。另：`inventory.rs:437/523/586`、`damage_out`/`adjust_stock` 的 `COMMIT` 即便失败也只 `?` 返回而未 ROLLBACK，事务同样悬挂。 | 数据完整性 + 应用可用性：极端情况下整库写锁死、需重启；并发或异常时可能数据不一致 | 将全部手动 `BEGIN/COMMIT` 替换为 rusqlite 的 `conn.transaction()` / `.unchecked_transaction()`，利用 Drop 语义**保证 panic/错误均自动回滚**。这是最高优先级修复 | P0 |
| CR-002 | D4/D10 | `src-tauri/src/db/schema.rs`、`commands/sales.rs`、`src/utils/price.ts` | schema.rs:571-572,593,718-722,765,798,282-283,672-673；sales.rs:213,230,334-370；price.ts 全文 | 金额（单价/小计/总额/折扣/实付/找零/余额/积分金额）全程使用 `f64`/`REAL` 存储与计算。`points_earned = actual_amount as i64`（sales.rs:370）直接截断小数积分；多商品 `subtotal` 累加、`discount_amount = total*(1-rate)` 等浮点运算存在舍入误差累积风险（典型如 0.1+0.2≠0.3）。 | 财务正确性：零售 POS 对分/角/厘敏感，浮点误差可能导致对账不平、积分计算偏差 | Rust 侧切换到整数「分」或 `rust_decimal`/`decimal` crate；前端 `price.ts` 改用整数分或 `decimal.js`；序列化边界统一为分。属设计级风险，上线前必须整改 | P0 |
| CR-003 | D1 | `src/stores/settings.ts`、`src/views/settings/index.vue` | settings.ts:27-57；settings/index.vue:134-141 | 「系统设置」功能整体未完成：后端 `get_settings`/`save_settings`/`backup_database` 三个命令**不存在**（`lib.rs` 未注册）；前端 store 的 `loadSettings` 为空操作、`saveSettings` 仅改本地 ref、`backupDatabase` 为空函数；设置页「备份」按钮仅 `message.info('备份功能开发中')`、「初始化数据库」按钮为 `// TODO` 空函数（且不调用已存在的 `clear_all_data`）。 | 功能闭环：设置无法持久化（重启即丢失），备份不可用，属典型「半拉子功能」 | 补齐 3 个后端命令（持久化到表或配置文件）+ 前端 store/api 接线；或在该版本明确标注「设置/备份为后续版本特性」并在 UI 禁用 | P0 |

### 🟡 重要问题（P1 — 建议本版本修复）

| 编号 | 维度 | 文件路径 | 行号 | 问题描述 | 影响范围 | 修复建议 | 优先级 |
|------|------|---------|------|---------|---------|---------|-----------|
| MJ-001 | D2 | `src-tauri/src/commands/inventory.rs`、`commands/sales.rs` | inventory.rs:597-626；sales.rs:253-275 | FIFO 批次扣减逻辑被**重复实现**两处（`deduct_from_batches` 与 `create_sale_order` 内联）。两处逻辑若后续一方修改易发散，导致扣减行为不一致。 | 可维护性 + 潜在正确性分歧 | 抽取为唯一 `deduct_from_batches(conn, product_id, grams)` 函数，两处统一调用 | P1 |
| MJ-002 | D7 | `src-tauri/src/tauri.conf.json` | tauri.conf.json:28（`"csp": null`） | 内容安全策略被显式关闭（null）。虽然本应用 webview 仅加载本地资源、无远程内容，但关闭 CSP 削弱了 XSS/注入的纵深防御（OWASP A05）。 | 安全配置：丧失 WebView 层最后一道 XSS 防线 | 设置严格的 `csp`（如 `default-src 'self' 'unsafe-inline'; img-src 'self' data: blob:` 等），仅放行必需的 scheme | P1 |
| MJ-003 | D1 | `src/views/dashboard/components/TodayStats.vue`、`DashboardHero.vue` | TodayStats.vue:74-79；DashboardHero.vue:90 | 仪表盘 4 张统计卡为**硬编码字面量**（128 / ¥3,580 / 3 / 5），注释明确 "Mock 演示数据"；天气为占位 `'晴 24°C'`。组件未调用任何 API。 | 展示真实性：演示/交付时易误导，非真实业务数据 | 接入真实统计接口（或明确标注为演示占位并去除硬编码数字） | P1 |
| MJ-004 | D5/D6 | `specs/project_config.md`、`specs/目录结构.md`、`specs/代码规范.md` | — | 文档严重漂移：① 版本写 v0.3.0（实际 0.5.5）；② 构建工具写 Vite 5（实际 6）；③ 描述 `src-tauri/src/services/`（inventory_service/price_service/barcode_service）与 `db/migrations/` 目录，**实际不存在**；④ 代码规范示例的 `services/inventory_service.rs` 无对应实现。 | 文档可信度：违反「docs/ 是项目真实状态记录」原则，误导后续开发 | 以实际代码为准重写上述三份文档（版本、目录、技术栈、架构图） | P1 |
| MJ-005 | D6 | `src/views/products/ProductList.vue`、`src/components/layout/TitleBar.vue`、`ThemeSettings.vue` 等多处 | ProductList.vue:371,383,396,409,480,500；TitleBar.vue:109；ThemeSettings.vue:187 | `any` 类型滥用：`(row as any).unitCount/retailPrice/...`、`catch (e: any)`、`let appWindow: any`、`updateSettings = (partial: any)`。其中 `ProductList.vue:500` `(productStore as any).getProductStock?.(p.id)` 调用**不存在的方法**，因 `?.` 静默返回 null 掩盖了死代码路径。 | 类型安全：削弱 TS strict 保护，隐藏潜在运行时错误 | 为表格行定义 `DataTableColumns<Product>` 类型；`appWindow` 用 `Window \| null`；`partial` 用 `Partial<UISettings>`；删除/修复死代码 `getProductStock` | P1 |
| MJ-006 | D3 | `src/views/sales/index.vue` | sales/index.vue:561-564 | 小票打印失败时 `try { await printReceipt(...) } catch (e) { console.error(...) }` 仅打印日志，**无用户反馈**。销售成功但打印失败，店员无从知晓。 | 用户体验 + 异常可见性 | catch 中增加 `message.error('小票打印失败：' + e.message)` 等用户提示 | P1 |
| MJ-007 | D4 | `src-tauri/src/commands/sales.rs` | sales.rs:185（`order_no = format!("XS{}{:03}", ...(ts.timestamp_millis() % 1000))`) | 订单号 = 日期时间秒 + 毫秒%1000。同一毫秒内两笔下单将生成**相同 order_no**，触发 `sales_orders.order_no` UNIQUE 约束冲突导致下单失败（返回错误）。 | 健壮性：高频/快速连续下单极端场景可能偶发失败 | 使用 UUID 或 `order_no` 自增序列 + 时间戳，或加随机/进程内计数器避免碰撞 | P1 |
| MJ-008 | D10 | `src-tauri/src/db/mod.rs` | mod.rs:29-42（new 内未设 busy_timeout） | 未配置 `PRAGMA busy_timeout`；且架构为单连接 + `Mutex` 串行，所有写操作互斥。当前单用户单机可接受，但未来若引入第二连接（如备份/导出并发读）易死锁/锁等待。 | 并发安全/扩展性 | 设置 `PRAGMA busy_timeout = 5000`；若需并发读写，评估连接池或只读从连接 | P1 |

### 🟢 一般问题（P2 — 后续迭代）

| 编号 | 维度 | 文件路径 | 行号 | 问题描述 | 影响范围 | 修复建议 | 优先级 |
|------|------|---------|------|---------|---------|---------|-----------|
| MN-001 | D3 | `src-tauri/src/commands/inventory.rs` | inventory.rs:45（`category_id.clone().unwrap()`） | 生产代码（非测试）存在一处 `unwrap()`，但上游 `if category_id.is_some()`（:42）已守卫，实际不会 panic。 | 健壮性（低风险） | 改为 `if let Some(cid) = category_id { ... }` 消除 unwrap | P2 |
| MN-002 | D2/D6 | `commands/sales.rs`、`commands/return_order.rs`、`commands/inventory.rs` | 文件级（sales.rs ~1900 行、return_order.rs ~1700 行、inventory.rs ~1400 行） | 命令文件过大，单文件承载过多业务（创建/挂单/会员/退货等），不利维护与审查。 | 可维护性 | 按业务（销售/退货/采购/库存）拆分为子模块，命令文件仅做编排 | P2 |
| MN-003 | D4 | `src-tauri/src/commands/inventory.rs` | inventory.rs:597-626（`deduct_from_batches`） | 函数本身不校验「批次总余量是否 ≥ 所需扣减量」，仅在其唯一调用方 `damage_out`（:486 已校验 `stock_grams < grams`）保护下安全。属防御性缺口。 | 健壮性（当前不可触发） | 函数内增加「扣减后 remaining>0 则返回 Err(库存不足)」，与 `sales.rs` 内联实现保持一致 | P2 |
| MN-004 | D3/D6 | `src/components/layout/TitleBar.vue`、`src/views/*` 部分文件 | TitleBar.vue:128（`catch {} /* 忽略查询失败 */`）；部分组件缺导出函数注释 | 个别空 catch 静默吞错；部分前端文件未完全落实「导出函数注释 / 行内注释解释为什么」规范。 | 代码规范一致性 | 空 catch 改为 `catch (e) { console.warn(...)}`；补齐关键函数注释 | P2 |

---

## 五、修复优先级排序

### P0 — 阻塞发布（必须立即修复）
- **CR-001**：事务改 rusqlite `transaction()` 自动回滚（数据完整性/可用性生命线）
- **CR-002**：金额改为整数分 / decimal（财务正确性）
- **CR-003**：设置模块补齐后端命令或直接标注为未实现并禁用 UI

### P1 — 本版本修复
- MJ-001 FIFO 去重 · MJ-002 配置 CSP · MJ-003 仪表盘接真实数据 · MJ-004 文档对齐 · MJ-005 清理 any/死代码 · MJ-006 打印失败提示 · MJ-007 订单号防碰撞 · MJ-008 busy_timeout

### P2 — 后续迭代
- MN-001 去 unwrap · MN-002 大文件拆分 · MN-003 deduct_from_batches 防御校验 · MN-004 注释/空 catch 规范

---

## 六、整体评估

| 项目 | 内容 |
|------|------|
| 整体质量评分 | **6.5 / 10**（一般 — 需显著改进） |
| 主要优点 | 1. **测试覆盖完善**：Rust 单测 + 前端 vitest 单测 + IPC 契约测试 + Playwright e2e，远超同类桌面项目<br>2. **数据库 Schema 设计扎实**：外键约束、CHECK 约束、`user_version` 版本化迁移（v1→v6）、复合索引到位<br>3. **SQL 全部参数化**：无字符串拼接注入；`format!()` 仅用于内部列名（安全）<br>4. **前后端契约对齐好**：已实现功能的 67 个 `invoke` 命令均有对应 `#[tauri::command]`<br>5. **启动即开启 WAL + 外键**；TS `strict` 开启；文件头注释普遍 |
| 核心风险 | 1. **事务在 panic 路径不回滚 → 共享连接锁死**（CR-001，最危险）<br>2. **金额浮点存储/计算**（CR-002，财务隐患）<br>3. **设置功能整体缺失**（CR-003，功能不完整）<br>4. **安全配置 `csp:null`**（MJ-002）<br>5. **文档与代码严重漂移**（MJ-004，误导后续开发） |
| 优先修复清单 | CR-001 → CR-002 → CR-003 → MJ-002 → MJ-004 → MJ-001 → 其余 P1 |
| 改进建议 | 1. 引入 rusqlite `Transaction` 封装 + 统一错误处理宏，杜绝手动 BEGIN/COMMIT<br>2. 建立「金额一律整数分」的编码约定并在 code review 中强制<br>3. 每次发版前用脚本校验 `specs/` 文档版本/技术栈/目录与实际一致（可做成 CI 检查）<br>4. 为 Tauri 设置生产级 CSP；capabilities 当前已较精简（良好），保持最小权限原则<br>5. 将 FIFO、价格计算等核心逻辑收敛为唯一实现（消除重复），提升可测试性 |

---

## 七、审核范围与方法说明

- **自动化扫描**：TODO/FIXME、`.unwrap()`/`.expect()`、SQL 字符串拼接、XSS（`dangerouslySetInnerHTML`）、`any` 类型、事务块、PRAGMA 配置。
- **人工精读**：`db/schema.rs`、`db/mod.rs`、`lib.rs`、`commands/sales.rs`（事务+金额）、`commands/inventory.rs`（FIFO+事务）、`utils/price.ts`、`stores/settings.ts`。
- **并行交叉验证**：2 个专项审查代理（Rust 后端安全/数据完整性、前端质量/契约）独立取证，结论与人工精读一致。
- **生产 vs 测试代码区分**：代码中大量 `.unwrap()`/`.expect()` 位于 `#[cfg(test)]` 测试（使用 `Connection::open_in_memory()` 夹具），已正确排除，不计入生产严重项；生产 unwrap 仅 1 处（MN-001，已守卫）。
- **未覆盖**：因无网络/`cargo` 运行环境，未执行 `cargo clippy` / `npm audit`（registry 返回 NOT_IMPLEMENTED）；建议后续在构建机上补充静态分析与依赖漏洞扫描。
